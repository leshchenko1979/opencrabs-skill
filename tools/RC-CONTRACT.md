# tools/ RC contract (C-#3, v0.4.78)

Fleet-wide conventions, then the per-tool register. The battery
(`tools/tests/run.sh`, section "rc contract --help=0 fleet-wide") asserts the
`--help` row live: every `oc-*` tool must answer `--help` with rc **0**
(output content is not asserted — lens A L3, v0.4.79). Usage errors print WHY (C-#1) — a bare
code with no diagnostic is a bug.

Fleet conventions:

- `--help` / `-h` → **0** (usage text on stdout).
- Usage/argument error → **2** on every tool EXCEPT the legacy registers
  noted below (usage≠2: `oc-deploy` 1, `oc-artifact-verify` 1,
  `oc-job-verify` 1, `oc-order-validate` 1, `oc-pr-atomicity` 1, `oc-seal-state` 1/2/3, `oc-ship-chain` 3/7,
  `oc-waiter` 1 (retired) — long-documented, selftest-asserted vocabularies;
  changing them would break every lane keyed on the old codes).
- Verdict codes are per-tool and documented here + in each tool header.
- `oc-prchecks` rc=2 carries a diagnostic + a same-args retry backoff
  (2s..10s inside 120s).

| Tool | help | usage | Verdict codes |
|------|------|-------|---------------|
| oc-artifact-verify | 0 | 1 | 0 PASS / 1 invocation / 2 NOT-ELF-MISSING / 3 MARKER-MISSING / 4 SHA-PROVENANCE-MISMATCH / 5 VERSION-MISMATCH · embed-source sha compares exact OR first-12-hex prefix (old-embed truncation tolerance) |
| oc-attrib | 0 | 2 | 0 ok / 3 git-fail / 4 empty-range / 5 markers-missing · **`--novel` / `--no-novel` (#407):** keep only commits whose change is NOT already present in the baseline (patch-id equivalence via `git cherry <baseline> <tip>`); `--deployed` IMPLIES `--novel`, an explicit `--no-novel` always wins, and `--range` alone stays RAW (unchanged for every pre-#407 caller). MERGE commits pass through UNFILTERED (measured: `git cherry` omits merges — a merge has no patch-id — so a membership test would silently drop them and can empty the result). EMPTY commits (non-merge, no changed paths) are excluded EXPLICITLY: `git patch-id` prints nothing for an empty, yet `git cherry` still matches empty-against-empty, so an untwinned empty would otherwise sail through as a contributor. Novelty unavailable (baseline unreadable / `git cherry` failed) → FAIL OPEN, unfiltered, loud stderr warning — never an empty result. `--trailers-only` is ALWAYS raw (oc-harvest-sweep Leg T must still SEE empty commits to report them MISSING Session-Id). Accounting on STDERR (`N raw, R replayed, E empty, K contributing`), so TSV/JSON stdout consumers are unaffected. rc 4 is bound to the RAW range count, not the filtered one. |
| oc-branch-sweep | 0 | 2 | 0 nothing-deleted / 1 deletions / 3 git-fail · MERGED branches held by an active worktree emit action=held-by-worktree rather than delete-failed · UNSTARTED — a branch in the merged set whose reflog holds nothing but its own creation (`^branch: Created from `) is a lane that never committed: report-only, NEVER archive-deleted (issue #303); an EMPTY reflog (expiry) keeps the MERGED path |
| oc-carrier-features | 0 | 2 | 0 set / 3 yml-unfetchable / 4 no-features-input |
| oc-census | 0 | 2 | 0 census produced / 1 refused (repo or base unresolvable) / 3 invariant-break (internal) · READ-ONLY pending-branch census (runbook Step 7 step 0, #422): DERIVED branch list via for-each-ref refs/heads, per-branch plus decomposition from git cherry with the enforced identity plus_total == plus_own + plus_untrailered + plus_other, tip-lane resolution from the branch TIP's Session-Id trailer · output is an OBJECT {meta{repo,base,base_sha,branch_glob,jobs,branches,totals,invariant}, rows[]} — the ref-state travels beside the rows because a census figure without its ref-state is unreproducible · `%oc-census%` is the search predicate — `%census%` also matches oc-harvest-census |
| oc-claims-single-source | 0 | 2 | 0 clean / 1 HARD finding (a private copy of the claim-closure predicate under tools/) / 2 usage-or-IO · battery guard (tools/tests/run.sh section 72): the claim-closure predicate has exactly ONE home (`tools/lib/oc_claims.py`), and a re-added private copy must FAIL the battery rather than drift unnoticed — it drifted FIVE times before this guard existed (#305/#313 oc-ledger, #309 oc-issue-dispatch, #424 oc-roster, #425 oc-harvest-dispatch, #307 oc-harvest-census), every divergence found only AFTER it had already produced wrong claim state. Keys on what a copy must RE-IMPLEMENT, NEVER on a name: **ARM A (HARD)** a loop over the SUBSEQUENT slice of an event list (`X[i + 1:]`) that reads the kind dimension and yields a closed/open verdict; **ARM B (HARD)** a module-level ALL-CAPS tuple/set literal whose VALUE set reconstructs CLOSING_KINDS or LANDED_KINDS. Keyed on VALUES, so a rename still fires, and the module-level restriction is what excludes every fixture dict and every inline `kind in (...)` comparison. Name-keying was the defect this replaces: the two deleted copies were called `claims_index` and `claim_is_closed`, and the constant was CLOSERS in oc-roster against _CLOSING_KINDS in oc-harvest-dispatch, so a name-keyed guard misses both. **Scan units:** every PYTHON UNIT under `tools/` — a whole .py or python oc-* file, AND (for a shell tool) every python blob it embeds via `python3 -c` or a `python3 -` heredoc. An enumerated-but-unparseable unit is still printed as a `NOTE ... coverage hole` line on stdout and still does NOT change rc (only a HARD finding returns 1), so a green carrying a NOTE means the scan was PARTIAL — never read it as full coverage. **Correction (#496, `02246264`):** the partial figure this row was originally written from (19 declared / 14 scanned / 5 NOTEs) was an EXTRACTOR artefact, not a real coverage hole — blobs were fenced by scanning forward to the next line STARTING with a quote, so a single-line `python3 -c` blob had no terminator and the scan ran on through the rest of its file. Post-fix the live tree declares and scans **28 of 28 with 0 holes**, confirmed by the tool's own run (its ok line prints both figures), and the battery's live-tree leg now asserts `declared == parsed` AND no coverage hole — it previously asserted `declared >= parsed`, which a hole SATISFIES, so the guard could not fail on the defect it existed to catch. |
| oc-commit | 0 | 2 | 0 committed / 3 gate-fail / 4 git-fail / 5 comment-fail · **Defect #335 (2026-09-18, Toolsmith):** Issue-Ref derivation now reads `oc-ledger claim-ref <actor> --open` — a claim whose issue already carries a done/close row is RELEASED, and deriving the ref from it posted the implementation comment onto a FINISHED issue. rc 3 now also covers "latest claim is CLOSED"; an explicit `--issue <N>` still overrides. Selftest gains case 6b; negative control on the pre-fix tool fails it (`closed-claim derivation != 3`). **Defect #428 (2026-09-20, Toolsmith):** the caller's `-m` bodies are scanned for a GitHub **close-keyword shape** (`close[sd]` / `fix(e[sd])` / `resolve[sd]`, optional `:`, then `#N` — case-insensitive) and a hit WARNS on stderr naming the matched shape, classified `class=negated` or `class=genuine` by a negation marker within the six words preceding the shape on its own line. **The scan is ADVISORY: it NEVER changes the exit code** — `rc` stays 0 and the commit always lands (shape 1 of the #428 disposition, scoped). Origin: honest-limit prose the process itself requires ("Part B alone does NOT fix #321") is a close keyword TO GitHub's parser, which cannot read negation — it closed fork issue #321 **11 s** after the ff-merge, with no upstream PR, silently. Both classes warn; a negation idiom outside the list still warns as `genuine`, so nothing passes silently. Limits, stated: the negation window is the SAME LINE; ASCII keyword families only; **a code span is NOT exempted** — shape 3 remains an unprobed hypothesis about the parser and is deliberately unencoded. A blocking mode (shape 2) exists only as a mutation-guard mutant, never as shipped behaviour. Selftest gains legs 8a–8c; `tools/tests/negctl-428-mutation-guard.sh` proves 4 mutants (negation dropped / classes collapsed / shape unnamed / blocking) each die on their aimed leg. |
| oc-deploy | 0 | 1 | 0 ok-noop / 2 rebase-gate-push-verify-rollback AND usage/validation diags (flag-validation `die 2`, e.g. poll `--wait` non-integer, `--notify-session` empty; ship rc-2 gate deaths carry machine-readable `OC_DEPLOY_GATE=<cause>` tokens: missing-sha / wait-plan-mode / notify-poll-only / rebase-needed / feature-mismatch / fetch-fail) AND ship FEATURE-DEFAULT explicit-mismatch (`--features` != deployed.meta.json, refused pre-dispatch; default path auto-adopts deployed set) / 3 fan-out infrastructure failure — the ONLY rc-3 producers are the fanout sites (oc-attrib backend failure or stderr-capture creation, gh API run fetch, fan-out lock open, built-sha decode, or commit-range resolution); a deployed range that yields no contributing rows after the #407 novelty filter is rc 0 with "no novel commits in range", NEVER rc 3 / 4 stage-gate-launch + ship features-compat gate (silent feature-drop refused pre-dispatch; `--allow-features-drop` override) / 5 poll-wait-timeout + ship `--wait` timeout (ship --execute --wait N bounded-polls dispatched run: GREEN rc 0, timeout rc 5, terminal-RED rc 6; plan-mode `--wait` refuses rc 2) / 9 kill-file · `status [--json]`: inspects running binary vs on-disk target sha, uptime, and last-swap metadata; rc 0 running matches disk / rc 1 mismatch-or-unswapped / rc 2 usage · `contributors` verb RETIRED (rc 1 + loud pointer to `oc-attrib --contributors`) · **Carrier Build Coalescence & Ancestry Matching:** In GitHub Actions, carrier workflow `ci/quick-build-linux` uses `concurrency: group: quick-build-linux` (1 active, 1 pending; 3rd push cancels/replaces pending 2nd). `oc-deploy ship/poll` uses **ancestry-aware matching** (`git merge-base --is-ancestor "$SHA" "$CAND_SHA"`): if a carrier run finishes building a descendant commit that includes `$SHA`, the run is accepted as a valid build for `$SHA`. `swap-execute` acquires host-wide `flock -x $STATE_DIR/host-swap.lock` around `swap_execute()` and enforces **rebase-aware lineage monotonicity** to prevent stale binary overwrites. **Lineage guard (v0.4.148 ancestry-only; REBASE-AWARE v0.4.151 — the v0.4.148 form refused EVERY post-rewrite swap, fleet-blocking, reported 2026-09-12):** three-tier verdict over `OC_DEPLOY_REPO` — (1) **monotonic** `git merge-base --is-ancestor "$PREV_SHA" "$SHA"` rc 0 → accept, silent, NO journal row (fast path, unchanged from v0.4.148); (2) **rewritten** PREV orphaned by a fork-main rewrite, but a commit in `merge-base(PREV,SHA)..SHA` carries the SAME `git patch-id --stable` (identical change, new sha) → accept, journal `reason:"rewrite-equivalent-swap"` + `rewritten_twin:<sha>` + stderr note; (3) **absent** both objects exist and PREV's change is genuinely NOT in the incoming lineage → `die 2`, journal `reason:"non-monotonic-swap"`, machine-readable stderr token `OC_DEPLOY_SWAP_GUARD=stale-build` (so a genuine stale/delayed-build overwrite stays distinguishable from an orphaned-by-rewrite refusal). Verdict **unknown** (either object not in the repo) → guard SKIPPED, preserving the pre-v0.4.151 "if both exist" behaviour. Patch-id work runs ONLY on the tier-1 failure path, so the happy path still costs one `--is-ancestor`. `--allow-rewritten-lineage "<justification>" [--actor A]` is the audited escape hatch for a genuinely-absent change: justification MANDATORY (bare flag → `die 2` + `OC_DEPLOY_GATE=override-needs-justification`), acceptance journaled `reason:"rewritten-lineage-override"` with `justification` + `actor` (default `$OC_DEPLOY_ACTOR`/`id -un`). Read-only diagnosis verb `lineage-check [--prev P] [--sha S] [--repo R] [--json]` → rc 0 monotonic|rewritten, rc 2 absent|unknown, printing verdict + twin + next step. **Post-rewrite lane runbook:** a rebase orphans the deployed sha but no longer blocks swaps — re-run the same `oc-ship-chain` leg and it is accepted as `rewrite-equivalent-swap`; NEVER hand-edit `deployed.sha` to re-point around a refusal;  **Restart survival / cgroup safety (v0.4.153):** a DIRECT `swap-execute` inherits the launcher's cgroup; when that launcher is a session hosted by the unit being bounced (`opencrabs-ops.service`), step 8's `systemctl --user restart` SIGKILLs the swap AT the restart line, so steps 9-12 (post-verify, sidecar-sync, receipt, fanout) never run and - because the swap-result stamp is deliberately LAST (F-M1: receipt evidence must precede the stamp) - NO ledger row is written at all: the host is swapped and the ledger is SILENT (journal `swap-acb8e205-...` stops at seq 7 marker-write, mtime == swapped_at second, 2026-09-12). swap-execute now re-execs under a transient scope outside the bounce cgroup BEFORE any work (pre-lock); the detach command MUST BLOCK and pass the child's rc through (`systemd-run --user --scope`, `setsid -w`) - a non-blocking launcher returns 0 immediately and a guard refusal would read as success. Read-only repair verb `recover-receipt [--journal J] [--run R] [--sha S] [--dry-run] [--historical] [--json]`: stamps the missing `swap-result` row ONLY when the journal shows install exit=0 AND marker-write exit=0, carries NO receipt step, the sha resolves, and no existing row already quotes sha+run (idempotent - a re-run is a no-op); **rc 0 = handled** (recovered, or correctly nothing to do) / **rc 2 = refused** (the journal does not describe a recoverable real swap); `--historical` records a swap later SUPERSEDED instead of refusing; fan-out is NOT replayed (use `oc-deploy fanout --run <id>`). Selftest section 22 covers parent-kill survival, the negative control, and recovery idempotency. |
| oc-drift-check | 0 | 2 | 0 no-drift / 1 DRIFT **or NO-HISTORY** / 3 ledger-skilldir-fail · skill-dir resolution v0.4.130 ruling n=2369: `--skill-dir` flag > `OC_SKILL_DIR` env > CANONICAL profile copy (`~/.opencrabs/profiles/ops/skills/opencrabs-dev`) — the invoking script's location is NEVER self-resolved (worktree copies produced contradicting same-day verdicts, 2026-09-10) · **D-5 omit-arg mode (HQ d72bd52d, 2026-09-13):** with NO claimed-version the comparison is made against the lane's RECORDED `last_acked` (read from the roster via `oc-ledger roster --json`, the schema owner) instead of argv. Passing the LIVE version as `claimed` — which is what every RELOAD brief did — made the verdict an ARGV ECHO: `${version}` in the brief IS the version the wave announces, so argv == live BY CONSTRUCTION, the verdict was ALWAYS NO-DRIFT, and the sensor could never fire on the exact invocation every brief prescribes. A lane with NO history got NO-DRIFT too (reproduced on a synthetic uuid) and cited it as compliance evidence. Omit-arg makes the verdict a pure function of state on disk (Reviewer J lens, v0.4.162). A lane with NO `last_acked` gets its OWN verdict — `NO-HISTORY`, **rc 1**, the same required action as DRIFT — and is NEVER folded into NO-DRIFT. The roster's `--json` projection renders an ABSENT `last_acked` as the sentinel `"-"`, not null (verified live); both `"-"` and empty mean no record, because reading the sentinel as a version printed a confusing `DRIFT: claimed - != live <cur>`. `--ack` still stamps the LIVE version on BOTH paths (unchanged — that behaviour was always correct; only the VERDICT was broken). Selftest 16 -> 27 (+11 assertions); four negative controls each with an exact named delta — the killer arm makes omit-arg echo the live version (the defect itself) and reddens 8. |
| oc-harvest-census | 0 | 2 | 0 ELIGIBLE / 1 REFUSED (already MERGED or IN_FLIGHT upstream, within 24h feature soak window, HELD_DEPENDENT_SOAK path overlap with soaking feature, or HELD_IN_FLIGHT_LANE path overlap with active editor claim) / 3 API-or-git-fail · `check` enforces 24h feature soak gating (`feat/*` or `feat(...)` < 24h old from deployment/swap timestamp `deployed.ts` = REFUSED exit 1, Owner 2026-09-15 amendment; CLOSED PR rework still gated by soak window), base-freshness against upstream, and dynamic file path overlap checks against soaking ahead features (`HELD_DEPENDENT_SOAK`, filtering module declaration registries such as `src/tests/mod.rs`) and active editor lane claims (`HELD_IN_FLIGHT_LANE`) · target matching supports issue numbers, branch slugs, and commit SHAs · `record` was write-only AND non-durable: the registry write dumped a literal two-key dict, so every `sync`/`scan` ERASED `manual_records`. The write now MERGES into the loaded dict, preserving foreign keys; `check` consults `manual_records` before declaring a unit unharvested and refuses with rc 1 `REFUSED ... manually recorded as filed in PR #<n> (unit <u>)`. Selftest 6/6 -> 14/14 (+8 assertions); negative control 4 arms, each reddening exactly one named assertion · **#307 anchored Issue-Ref fence:** the `HELD_IN_FLIGHT_LANE` footprint was resolved by an UNANCHORED `git log --grep=#N`, so commit PROSE read as work on the issue — upstream `1e34378050` (subject `(#478)`, body prose `(#300)`) read as fork work on #300 and fenced #291's harvest on files #291 never touched. Resolution now delegates to `oc_claims.open_claims` / `oc_claims.resolve_issue_files` (the single owner of the closure + footprint predicates); anchoring is the machine-written `Issue-Ref` TRAILER `oc-commit` derives from the actor's ledger claim, and a foreign-slug trailer (`adolfousier/opencrabs#N`) does not fence a fork issue. Selftest 25 -> 26 (+1 assertion — the prose-only leg, which reddens under the mutation guard); `tools/tests/negctl-307-anchored-fence.sh` on #297 measures old 7 commits / 30 files vs new 1 commit / 1 file, and `tools/tests/negctl-307-mutation-guard.sh` reverts the matcher on a COPY and requires census + dispatch + the §00c lib cases all RED. · **#365 gate 4 decides on the fix's SUBJECT, not on file existence:** a fork-only base-fault fix touches files that exist upstream while the fix's SUBSTANCE is fork-only, so a pure file-existence test read it as `UPSTREAM_VOID` and false-blocked the harvest (live instance #341). Gate 4 class 3 now derives the fix's SUBJECT symbols from the target diff — the enclosing fn of each added line, else the file's fn declarations, else a back-scan over the file's old names — and requires at least one to be present upstream at the base ref, resolved FILE-SCOPED via `git show <base_ref>:<file>` (a global grep false-positives across files). Refusal is on POSITIVE evidence: symbols WERE derived and every one is absent upstream ⇒ `REFUSED ... [UPSTREAM_VOID: tested <syms>]`, and the declared-subject fallback is deliberately NOT consulted. An EMPTY derivation is inconclusive, not a verdict, so a DECLARED subject may resolve the unit — `--subject <sym>` (argv[6]) else a `Subject:` trailer on the fix's own commits — and it must still grep present upstream, or the refusal names why (`; no declared subject` / `; declared subject '<s>' not present in <ref>`). Fail-closed both ways: an absent or unverifiable declaration refuses exactly as before it existed. Class 2 gained a sub-rule: an absent path whose introducing issue is ALREADY HARVESTED falls through to class 3 with `ADVISORY: orphan path <p> (absent upstream) — parent #<n> already harvested; port decision required` instead of refusing, so a harvested parent no longer blocks its own orphan cleanup. Every per-hunk decision emits `ADVISORY: hunk inside fork-only symbol — dropped at packaging (<file> :: <sym>)`, making the packaging drop list auditable. Selftest 26 -> 36 (+10 assertions: the four gate-4 shapes — #341 orphan-path-plus-upstream-subject, #253 files-upstream-plus-subject-absent, parent-harvested, inconclusive — plus the four declared-subject legs and their two negative controls). Each of those legs carries an ENV-LEAKAGE GUARD (`OC_HARVEST_MOCK_COMMITS=''` / `_SQUASHED=''` / `_PRS='[]'` / a private empty mock ledger): the suite EXPORTS a mock commit list, which wins over the fixture repo inside `get_ahead_commits`, empties `matching_commits` and skips gate 4 entirely — so without the guard any leg passes VACUOUSLY. All four shape legs redden under `tools/tests/negctl-307-mutation-guard.sh` §4, one mutation per shape, each required to be the FIRST failing leg on its own mutant. · **RUNTIME — a slow run is NOT a hang (measured 2026-09-19, Toolsmith):** `check` makes LIVE network calls (`gh pr list` against the upstream repo, plus `git cat-file`/`git show` against the base ref for the gate-4 subject test), so its wall time is dominated by network latency and is NOT bounded by the size of the local tree. The distribution is wide and right-skewed — measured over the 964 `check` invocations in `opencrabs-dev/tools.log` (predicate: rows with `tool=oc-harvest-census` whose `args` begins `check `, taking the tool's own `secs` field): **p50 3.2s · p75 24.5s · p90 47.1s · p95 72.9s · p99 133.1s · max 199.4s**. The figure most often quoted — 43.3s, row `2026-09-19T05:55:19Z check 352 exit 1` (lane reported by `40427d4f`) — is ONE sample sitting near p90: it is neither the typical run (the median is 3.2s, an order of magnitude faster) nor a ceiling (the max is 4.6x it). Wrapping the tool in a short `timeout` therefore truncates a legitimate run and yields rc 124/143 — 14 invocations in that same log were killed by a timeout or signal (exit 143 SIGTERM x12, exit 120 x2), which is exactly the shape that reads as "the gate HUNG" when the gate was merely slow. **Budget >=300s for a `check` and never wrap it in `timeout 60`.** The tool prints NO progress line before its verdict, so an empty stdout is NOT evidence of a hang: distinguish the two from the `secs` field in `tools.log`, never from silence. |
| oc-harvest-dispatch | 0 | 2 | 0 APPROVED / 1 REJECTED (already merged, empty diff, superseded, within 24h feature soak window, HELD_DEPENDENT_SOAK, HELD_IN_FLIGHT_LANE, HELD_PARENT_UNHARVESTED, or HELD_BLOCKED_BY_DEPENDENCY) / 2 usage / 3 git-or-api-fail / 4 CAPACITY_EXHAUSTED · `vet` candidate enforces 24h feature soak gating on novel `feat` commits before approval (age >= 24h from deployment/swap timestamp `deployed.ts`, Owner 2026-09-15 amendment), subsystem cohesion (`HELD_DEPENDENT_SOAK`, filtering module declaration registries such as `src/tests/mod.rs`), active claim fencing (`HELD_IN_FLIGHT_LANE`), native sub-issue parent verification (`HELD_PARENT_UNHARVESTED`), and dependency resolution (`HELD_BLOCKED_BY_DEPENDENCY`, Issue #255) · `dispatch` supports `--draft` flag to generate and wire draft pull request contracts · **rebase-aware soak clock (#276):** `get_commit_deploy_ts()` bounds its ancestor binary search to the contiguous sha-lineage suffix of the swap history and, below that rewrite boundary, falls back to **patch-id twin** resolution — the earliest swap whose introduced range carries a commit with the same `git patch-id --stable` id — so a commit replayed by a fork rebase anchors on the swap that first deployed its CONTENT instead of collapsing onto the newest swap. Two silent disablers are guarded by selftest 4c: the twin lookup and `get_patch_id()` must both use `--stable` (the default unstable hash is file-order sensitive and disagrees on any multi-file diff), and `load_swap_history()` de-duplicates by sha keeping the earliest ts (a `deployed.meta.json` row re-recording the newest swap otherwise makes `swaps[-2] == swaps[-1]`, which reads as an intact lineage and skips the fallback entirely) · **lane spread (#276):** auto-selection rotates round-robin across idle editor lanes via a cursor file beside the ledger, advanced only on live delivery, replacing the stateless `avail[0]` that pinned every unit of a cycle onto one editor · **#307 anchored Issue-Ref fence:** `resolve_issue_commits` and the `HELD_IN_FLIGHT_LANE` footprint now delegate to `oc_claims` (the shared resolver, single owner of the anchoring predicate) instead of a local unanchored `git log --grep=#N` copy that read commit PROSE as work on the issue; the resolver's `--all` index is memoized per repo (`_issue_ref_index`) because `get_active_claims` runs up to 4x per cycle and each index is a full `git log --all` pass. Selftest 24 -> 26 (+2 legs: 11e prose-only mention yields an EMPTY footprint, 11f its positive twin, so a resolver that always returned `[]` could not pass); both legs redden under `tools/tests/negctl-307-mutation-guard.sh`. |
| oc-harvest-sweep | 0 | 2 | 0 clean / 1 findings / 3 git-fail |
| oc-health | 0 | 2 | 0 clean / 1 findings (warn-class present) / 3 measurement-failure (a check could not be measured — never silently treated as clean) · a THIRD output channel — NOTES (#426) — is informational only: emitted in both `--json` (`"notes":n,"notes_detail":[{"slug","message"}]`) and text (`, n note(s)` plus `note <slug> — <msg>` lines), it changes NEITHER `findings` NOR rc, so an observation the check cannot turn into a verdict is never paged to the owner as his own defect (precedent: the `measure_failures` channel) · live note slug: `scheduler-direction-unset` (class 5, `$STATE/pacemakers-off` absent — the direction is UNSET and a disabled law-carrying patrol is observed, not asserted; the marker PRESENT direction still finds `cron-enabled-against-order`) · OWNER-ORDERED 8-class rotating sweep (2026-09-11): base invariants (disk headroom ≥85%, live deadlock locks, dead mirror reaping) + 1 rotating class per hourly run (filesystem, runtime_procs, persistence, git_vcs, schedulers, fleet_lanes, logs, issue_portfolio) · `--class <name>` audits a specific class, `--all` audits all 8 classes in a single full pass, `--rotate` advances `health-rotation.json`, `--status` displays current rotation state · `--reap` applies ONLY the provably-dead SAFE class (stale locks, backups beyond keep older than 24h, stale tmp `/tmp/oc-*`, leaked `mktemp` WORKDIRs `/tmp/tmp.*` matching the oc-deploy signature); worktrees are NEVER auto-removed (`oc-wt remove` has a dirty-tree gate a sweep must not bypass) · `--json` is the whole stdout contract (no chatter on stdout) · class & check catalog: `tools/HEALTH-CLASSES.md` and `tools/HEALTH-CHECKS.md` · overrides for hermetic testing: `OC_HEALTH_STATE|FORK|DB|TMP_GLOB|TMP2_GLOB|SKILL_DIR|WT_PATTERNS|SKIP_VERSION|LOG|ROTATION_FILE|DETACHED_DIR|WATCHER_WINDOW_H` · `--selftest` = 60 offline fixture assertions |
| oc-issue-dispatch | 0 | 2 | 0 success-dispatched / 1 vetting-failed / 2 usage / 3 notify-failed / 4 capacity-exhausted (no idle lanes) / 5 claim-gated (targeted without --force) / 6 sent-receipt-unverified (the send may have landed and no receipt was seen yet — the dispatch IS recorded, so do NOT re-send; rc 2 is the ONLY code that proves nothing was sent) / 8 targeted-landed-refused / 9 targeted-already-dispatched (both #370: the TARGETED path now runs the same eligibility arms as `--auto`, so it refuses an issue whose fix has already landed (8) or one already wired to a lane and not yet acked (9). They are DISTINCT from 6 because 6 means the dispatch IS recorded and must NOT be re-sent, whereas 8/9 prove NOTHING was sent — opposite operator actions. Overrides: `--allow-landed` / `--redispatch`) · mechanized fork issue vetting, claim-gate awareness, lane selection, topic-affinity scoring, and zero-ack wire dispatch (v0.4.169 Zero-Ack compliant) · `--auto` scans open unclaimed issues, filters claim-gated/parked/idea-box items, filters LANDED work (#337: two independent arms — a fork-space `Issue-Ref`/subject commit on origin/main, OR a `done`/`close` ledger row; 193 + 103 arms, 40 ledger-only), and remembers DISPATCHES inside a 7-day window (#337 dedup: an unacked dispatch is not re-wired to a second lane) · `--allow-landed` disables the landed filter · `--redispatch` disables the dedup window · matches to idle editor lanes · targeted `<issue-number> [--to <uuid>] [--force]` · `--dry-run` previews without state mutations · `--selftest` = 107 hermetic offline assertions · a dispatch is stamped on BOTH paths (an unverified dispatch is still recorded, so the 7-day dedup window suppresses a re-dispatch) · the notify-rc classifier is a FAIL-CLOSED ALLOWLIST behind the pure predicate `classify_notify_rc`: only rc 2 (`no_route`) proves non-delivery — every other non-zero rc (124 timeout, 4 transport catch-all, negative signal-death) is UNVERIFIED + STAMPED · `--budget-secs N` caps the whole `--auto` run, each notify's timeout shrinking to what remains |
| oc-issue-log | 0 | 2 | 0 posted **OR already-present** / 3 gh-fail / 4 no repo at --repo-path · **IDEMPOTENT per (issue, sha) since #392 (2026-09-19):** before posting, the existing comments are read and searched for the body's OWN full-sha marker (`Implementation commit: <full-sha>`); a hit skips the post and exits **0** — the same code as a successful post, because "the mandatory note for this sha is on the issue" is the success condition both callers test. `--force` bypasses the check for a deliberate re-post. A **FAILED** comment read never skips: it fails OPEN to posting with a stderr WARNING, because the per-commit note is mandatory (owner 2026-08-28 22:54Z) and a duplicate is cosmetic where a missing note is a process breach; the read retries once first. The guard lives at the **posting authority**, not the caller — this tool has two (oc-ship-chain LEG2 and oc-commit), so a caller-side guard would cover one path and leave the other able to duplicate. LEG2 is the exposed one by construction: it runs ABOVE the ship lock (`oc-ship-chain:766` posts, `:792` acquires), so a chain re-run after a LEG3-LEG5 failure re-posts unguarded (live: #319 carried two identical comments for `6e26029d`, 07:01Z and 07:15Z). `OC_ISSUELOG_GH` overrides the gh binary (hermetic selftest hook). · **#395 (2026-09-19): the verdict is DURABLE.** The guard's four outcomes were indistinguishable in every record — skip, post and fail-open all exited 0 with `extra{}`. The tool now emits `idem=<posted\|skip\|fail-open\|forced>` on **stdout** and into the log row's **`extra`** via `oc_log_extra idem` (`tools/lib/oc-log.sh:114`), from one writer. Both surfaces are needed: `oc-ship-chain:778` runs this tool under `OC_TOOLS_NOLOG=1`, so **no tools.log row exists on the retry path that minted #392's duplicate** — there the chain's journal is the only record, and LEG2 now journals `LEG2 issue-log done (idem=<v>)`, with a missing token reading `unknown` (an older tool version) rather than silence. Selftest leg E asserts the `extra{}` half behaviourally (`OC_TOOLS_NOLOG=0` is load-bearing: the parent selftest exports it and the child would inherit it). |
| oc-issue-sweep | 0 | 2 | 0 no-candidates / 1 candidates / 3 api-fail · **M2-25 (2026-09-12, toolsmith self-found, Duty TM1):** the EXIT trap removes EVERY temp file the run creates — `$TMP.raw`/`$TMP.sorted`/`$TMP.final`, not just `$TMP`. The trap was `rm -f "$TMP"` while the run wrote three siblings, so each invocation leaked 3 files into `/tmp` (measured 1070 files, 0.1 MB, oldest 2026-09-10). The selftest now asserts an empty `TMPDIR` sandbox after a real run; the negative control neutering the trap back to `rm -f "$TMP"` fails exactly that assertion. |
| oc-job-verify | 0 | 1 | 0 VERIFIED / 2 IN-FLIGHT / 3 FAILED / 4 REF-MISMATCH / 5 NOT-FOUND · embed-sha compares exact OR first-12-hex prefix (old-embed truncation tolerance) |
| oc-ledger | 0 | 2 | 0 ok / 1 verdict (cadence FIRE / version mismatch / `roster` or `events` NO-MATCH) / 3 ledger / 4 write / 5 battery-gate / 6 version-sync-gate · `--verbs`: lists registered subcommands; unrecognized verbs emit vocabulary hints on stderr; rc 0 · `claims <N>`: open claim-refs by issue, empty output + rc 0 = clean/unclaimed; rc 2 usage, rc 3 ledger; **c6b1a539 residual (2026-09-12): 30 of 130 live claim rows were invisible and/or unclosable — two independent mechanisms, measured live (21 carried no `#N` in `what`, 20 the `(unattributed…` sentinel as `by`, overlap 11). (a) Refs match by TOKEN, never substring: `claims 76` no longer matches a claim on `#760` — the old `*"#${n}"*` glob was a false POSITIVE as well as a false CLEAN. Accepted ref forms: `#N`, `issue N`, `issue=N`, `issue#N`. (b) A claim whose `by` is the sentinel is closed by ANY later closer referencing the same #N — the actor test is DROPPED for it, because an unattributed claim has no actor to discriminate on; an ATTRIBUTED claim still requires its own actor, so this cannot over-close. (c) `_latest_claim_issue` (and therefore `claim-ref`, and `confirm`, which delegates there) uses the same token extractor: the old first-`#<digits>`-anywhere grep both missed the no-hash forms AND derived the WRONG issue from a passing mention (live n=1585: `issue 94` + a `#93` mention derived 93). Measured over 130 rows: 109 old-resolved — 108 same, 1 corrected, 0 lost; 6 newly resolve** · `sweep-closed-claims [--dry-run]`: inspects open claims against closed issues on GitHub/fork and reports or marks stale claims closed; **Defect #272 (2026-09-17, Toolsmith): replaced naive `re.search(r"#?(\d+)", what)` with `issue_ref_tokens(what)` to prevent false-negative token extraction on date prefixes (`2026-09-15`), repo owner handles (`leshchenko1979`), or hex UUID prefixes, and added multi-ref support (`#38, #80`) so claims referencing multiple issues track unclosed tokens until all constituent issues are closed; verified with 317 selftests** · `lessons [--kind lesson|incident|all] [--class text]`: lesson/incident rows by class, one n/kind/t/what line per hit, empty rc 0 = none; rc 2 usage, rc 3 ledger · `roster [--role <substr>] [--live] [--include-retired] [--json]`: role → FULL uuid resolution; TSV uuid/role/topic/feature/ack, `--live` keeps only live sessions (profile session DB, read-only, OC_LEDGER_DB override), `--json` array; **law 9 (2026-09-12, defect #6): rows whose `.state` == RETIRED are EXCLUDED by default — a retired lane must never be a dispatch target and a role-to-uuid resolver that returns one is the same defect from the resolution side (live repro `e756b84b` retired_compiler resolved while retired); `--include-retired` lists them for diagnostics only, and a retired-only role is rc 1 NO-MATCH whose stderr names retirement as the reason**; **rc 1 = NO-MATCH verdict, never a silent empty**; rc 2 usage, rc 3 ledger/DB unreadable · `retire <uuid> [--topic N] [--role R] [--why TEXT]`: removes ONE row from roster (drops signing) and records `roster-retire` event; `--why` optional (defaults to "(no reason given)"); **ROW-SCOPED**: `--topic`/`--role` exact selectors; multiple matches without selector refused rc 2; rc 0 retired / 2 usage-or-ambiguous / 3 ledger unreadable · `events|tail [--n N] [--kind K] [--actor U] [--since T] [--json] [--full]`: recent ledger rows, TSV n/t/kind/by/what newest LAST; `--actor` substring match over normalised actor (`.by // .actor // .role`), `--since` byte-lexicographic compare over normalised timestamp (`.t // .ts`), `--kind` exact match over normalised kind (`.kind // .type`), content field normalises (`.what // .text // .summary // .note`); `--full` lifts 160-char content clip (the truncation-nudge measures the normalised field); **schema-mismatch fix (lane 329bf3a3, 2026-09-13): rows carrying non-canonical synonym keys no longer render blank content or drop out of filters**; **rc 1 = NO-MATCH verdict**; rc 2 usage, rc 3 ledger unreadable · `stamp <kind> "<what>" [--by <label>] [--allow-unattributed] [--allow-no-ref]`: **#19 (2026-09-12, dispatched by Triage 530c29ec — found on Toolsmith's own law-12 row n=3794, and lane d5863180's n=3786 before it): an ANONYMOUS row is REFUSED rc 2.** `derive_by` correctly refuses to invent an actor and returns a sentinel; the verb used to write that sentinel into the ledger anyway, so warn-and-write made the defect the DEFAULT. Name the actor with `--by <label>` or `OC_ACTOR`, or declare the anonymous row deliberate with `--allow-unattributed` (the escape hatch a guard needs, cf. sync `--allow-sweep`). The refusal sits AFTER the ledger gate so a missing/corrupt ledger still reports rc 3, never a masked rc 2. `sync` is unaffected: it writes its own `by: "oc-ledger sync"` row directly. **M2-3 (2026-09-12, owner mechanical audit item M2-3):** `derive_by` RESOLVES a uuid FRAGMENT against the roster. The lookup was an EXACT compare, so 8 live rows carried a bare 8-char prefix (`462181e9`, `212b3c83`, `c6b1a539`, `d5863180`, `2fae1230`) whose full uuid IS rostered — that is an UNRESOLVABLE LOOKUP, not an unrostered lane, and each of the five resolves to exactly ONE roster uuid. A 4+ char hex/dash fragment matching exactly one worker now stamps as `<role> <full-uuid>`; 0 or >1 matches keep the sentinel, because an ambiguous prefix must never GUESS an actor. `OC_ACTOR` is lowercased first (uuids are lowercase). **No refusal was added, deliberately:** the queue's first cut (refuse the unrostered full uuid) was measured and rejected — 64 of the 86 full-uuid sentinel rows belong to uuids rostered TODAY (stamped inside the legitimate pre-`enroll` window), it turns two green assertions RED (`oc-ledger` selftest `derive-by-unrostered` + `tools/tests/run.sh` section 11, whose fixture ledger has `"workers":[]` and asserts the stamp WROTE), and `oc-ship-chain` passes an explicit `--by` fallback so a refusal there would trade the chain's whole ledger trail for a warning. An unresolvable actor now WARNS on stderr naming the remedy (`oc-ledger enroll <uuid> <role>`) while still stamping the sentinel; a meaningful LABEL (`hq/opencrabs-dev-factory`) is not a fragment and is written VERBATIM. Selftest 228 -> 239 (+11 assertions); negative control neutering the prefix predicate returns exactly those 2 red, no others. · `ack <uuid> <0.N.N> [--at <ts>]`: record a worker's skill-version adoption; rc 0 / 2 usage (uuid or version shape) / 3 ledger unreadable or unknown uuid (roster discovery first). **M2-4 (2026-09-12, owner mechanical audit item M2-4): `ack` is IDEMPOTENT per (uuid, version)**, mirroring `sync`'s dup guard. `oc-drift-check --ack` DELEGATES here (oc-drift-check:78), so one adoption that both drift-checked and acked wrote two byte-identical rows seconds apart (lane 1a63f103 n=3717/3718 two seconds apart; d18ce16a n=3713/3714; 462181e9 x3). A re-ack of a version already on record still REFRESHES `last_acked`/`updated_at` — so a lane re-acking after a failed git leg does not keep a stale `last_acked.at` — appends NO event, prints `(already on record — idempotent, no dup event)` and stays **rc 0**. rc 0 is load-bearing here: `oc-drift-check` runs `|| die 3`, so a non-zero on the dup path would break the delegated ack outright. The key is the EXACT composed string `worker <uuid> acked <v>`, never `contains` — a substring test would make 0.4.1 match 0.4.10 and swallow a real bump. A different uuid at the SAME version, or the same uuid at a NEW version, is new information and still appends. Selftest 239 -> 264 (+25 assertions); negative control neutering the guard returns exactly those 7 red, no others. **M2-23 (2026-09-12, toolsmith self-found, Duty TM1):** the `unrostered-actor` sentinel is IDEMPOTENT. A LABEL keeps the sentinel (`hq/opencrabs-dev-factory` -> `unrostered-actor hq/...`), but a value that ALREADY carries it is echoed VERBATIM with its own WARNING and never prefixed a second time. Origin: Toolsmith derived `OC_ACTOR` with `jq ... | sort -u | tail -1` over the `by` column, which returns the alphabetically-LAST distinct value rather than the LATEST, picked a 2026-09-09 sentinel row (n=2353), and `stamp` prefixed it again, writing `unrostered-actor unrostered-actor 2fae1230` (ledger n=3973; retracted n=3974, re-stamped n=3975). A doubly-prefixed actor is unattributable and breaks an exact-string actor grep, so the double prefix was precisely the thing the sentinel guard exists to prevent. Selftest 264 -> 267 (+3 assertions); negative control neutering the pattern returns exactly those 2 red (`-verbatim`, `-warns`), the `-rc` assertion staying green because it tests the exit code, not the guard. **M2-24 (2026-09-12, toolsmith self-found, Duty TM1 — live repro during the M2-23 stamp):** the value `derive_by` PRODUCES must be round-trippable. It emits the composed form `<role> <uuid>`, but every lookup accepted only a BARE uuid, so the composed form carried a space, hit the LABEL branch (`*[!0-9a-f-]*`), and fell through to the sentinel — the exact string the function itself writes could not be fed back. That is the natural caller pattern, not a slip: `oc-ledger roster --live --role R` prints a TSV whose first two columns ARE the uuid and the role. Live repro: n=3989 was written `unrostered-actor toolsmith 2fae1230-...` (retracted n=3990, re-stamped n=3991) — same family as n=3973. The branch resolves the TRAILING token and stamps the CANONICAL `<role> <uuid>` from the roster (the M2-3 normalisation: the roster's role, never the caller's guess); a two-word LABEL whose tail is not a uuid still takes the sentinel. It sits AFTER the M2-23 guard so a sentinel stays VERBATIM. Selftest 267 -> 271 (+4 assertions); negative control neutering the branch returns exactly those 2 red (`-canonical`, `-not-sentinel`), the `-rc` and `-label-sentinel` assertions staying green. · `sync` mechanics: battery-gated; commits both repos + tags; chains `oc-shadow-rotate` tail and mirror pushes (WARN-only); ack rows record skill-drift adoption; `--bundle` sweeps STATE receipts (tools.log/baseline.json/orders.json/journal); closed enum vocabulary (confirm, swap-result, notify-fanout, shipchain, roster-retire, claim, close, done, unclaim, reject). **D-2 (2026-09-12, HQ d72bd52d, verified live):** the claim-closure contract was a DEAD LETTER. `claims <N>` documents "a claim is OPEN unless a LATER event kind in (close|confirm|reject|done|unclaim) references the same #N", but `KINDS` carried NONE of close/done/unclaim/reject, so `stamp close|done|unclaim|reject` died rc 2 — 4 of the 5 documented closers were UNSTAMPABLE. Worse, the 5th could not close either: `cmd_confirm` appended only to `.workers[].events` (14 such rows live) while `cmd_claims` scans the TOP-LEVEL `.events` (27 `confirm` rows there, all from the `stamp confirm` verb) — an array the scanner never reads. Live impact: #150 and #191 both SHIPPED and both still read OPEN. Two fixes, one commit: the four kinds are admitted to `KINDS`, and `cmd_confirm` now appends the top-level row `cmd_claims` scans — carrying `by` COMPOSED as `<role> <uuid>`, because `cmd_claims` links a closure with `(.value.by // "") | contains($u)`. Selftest 271 -> 283 (+12 assertions); a negative control neutering the KINDS admission returns exactly the 4 closure-kind assertions red, and one dropping the top-level arm returns exactly the closure assertion red. **A gate that never closes gets ignored.** · **SIBLING SENTINEL — `unrostered-actor <uuid>` (lane a5b34466 report, corroborated by 6cd8175f; 2026-09-13):** the D-2 fix dropped the actor test for exactly ONE sentinel (`(unattributed`), leaving its SIBLING — what `derive_by` writes when `OC_ACTOR` is not in the roster — matched by nothing, so those rows could only be closed by hand-reproducing the historical `by` string, which writes a row attributed to ANOTHER actor (the false-attribution family). Both sentinels mean "no resolvable actor", so the arm now alternates the two sentinel prefixes (`"(unattributed"*` or `"unrostered-actor"*`), both dropping the actor test. Bounds unchanged: the closer must still be LATER and must reference the same #N by TOKEN, and #19 refuses NEW anonymous rows, so this reaches legacy data only. **Measured live (predicate: `.kind=="claim"` and `.by` startswith `unrostered-actor`): 25 rows over 18 distinct issues; 23 of those 25 ROWS were still OPEN at fix time, across 17 distinct still-open ISSUES** — a5b34466's #66 self-closed when they stamped their own `done #66` row carrying the IDENTICAL sentinel string, which is the general case: a `by` is written VERBATIM, so a sentinel `by` closes naturally only when the closer repeats that exact string. **Behavioral proof on the REAL ledger** (#127, a malformed-label row `unrostered-actor 95bec69b-editor-lane`): baseline 1 row OPEN under BOTH tools; after appending ONE natural closer (kind=`done`, rostered `by`, references #127) the fixed tool returns **0 rows** and the pre-fix tool still returns **1**. The PREFIX match is why the 6 malformed legacy labels (`… q1-tapredraw`, `… editor-141`, `… d5863180-editor`, `… 95bec69b-editor-lane`, `… editor 7e1ebbb6-…`, and `… 2fae1230`, which carries no #N ref and so sits outside the `claims <N>` surface entirely) are covered by the same arm rather than a second one. **SCOPE — this is NOT the difference between closable and unclosable** (6cd8175f's own retraction of "unclosable by construction"): an explicit `--by` naming the exact historical string ALWAYS worked, because `cmd_stamp` writes an explicit `--by` VERBATIM and `derive_by` runs only when `by` is empty. The residual is a PROCESS gap — no supported third-party closure path (H-5 assigns the close to the claiming lane, which is often retired), an undocumented hand-reproduction step, and a row attributed to another actor when you do it. A third-party closure verb (attribution-preserving) is the larger fix and is deliberately NOT this change. Selftest 295 -> 298 (+3 assertions: the sentinel closes, the malformed label closes, and an ATTRIBUTED claim still does NOT over-close); negative control reverting the alternation reddens exactly the 2 new closure assertions, the `-rc` one staying green because it tests the exit code, not the predicate. · **Defect #335 (2026-09-18, Toolsmith):** `claim --issue <N>` must WIN over a prose mention. The prefix guard was a SUBSTRING search (`case *"#$issue"*`) over the WHOLE prose, so a lane claiming #299 while narrating "fixed #294" had its prefix suppressed and the row anchored on the LEADING cluster (#294) — the claim landed on the WRONG issue, and `oc-commit` derived `Issue-Ref #294` from that row and posted its post-commit comment onto a FINISHED issue. The guard now asks the CONSUMER's own predicate (`_primary_issue_tokens`, the single owner `oc_claims.primary_issue_tokens` every reader uses): suppress the prefix ONLY when the prose already LEADS with a cluster naming $issue. `--issue #N` is normalised (`issue="${issue#\#}"`) so a stray '#' can no longer build `CLAIM ##N` and dodge the membership test. `claim-ref <uuid> [--open]` — NEW FLAG: `--open` answers only with a claim that is STILL OPEN, the sole ref a consumer may safely ACT on; plain form unchanged (latest claim row, open or closed). rc 0 / 2 (unknown flag, missing uuid) / 3 (no claim, no ref, or — with `--open` — the claim is CLOSED). Selftest 335 -> 353 (+18 assertions); negative control on the pre-fix tool reddens exactly 8 — `claim-issue-flag-wins-target` (`[299]` -> `[294]`, the reported bug reproduced), `-prose` (299 -> 294), `-hash-normalised`, and the 5 `claim-ref-open-*` cases — while both regression guards (`-flag-wins-rc`, `-leading-cluster-verbatim`) stay green on BOTH tools. · **#441 (2026-09-21, toolsmith lane): `claim-ref` now VALIDATES its argument domain.** It checked PRESENCE only, so any non-empty string was taken as the actor and `_latest_claim_issue`'s free `contains($u)` substring answered with a FOREIGN lane's claim at rc 0 — measured pre-fix on live state: `claim-ref 440` -> `402` rc 0 (440 sits inside a real uuid), `2fae` -> `436` rc 0 (a four-character fragment), `441` -> `393` rc 0 (the defect's OWN issue number), while `438` -> rc 3 `no claim event by 438` — the SAME class of typo failing in OPPOSITE directions, so the failure MODE was unlearnable. Accepted now: a canonical lowercase-hex uuid, or the M2-24 composed `<role> <uuid>` form `derive_by` emits (the TRAILING token is tested, so the round trip is exact); everything else is **rc 2** naming `claims <N>`, the verb that DOES take an issue number. The match itself is deliberately UNTOUCHED (a full 36-char uuid cannot substring-lie, and the composed form still needs the substring) — mutant B, `contains($u)` -> `== $u`, reddens 15 legs including `claim-ref-after-claim`/`claim-ref-value`, because `OC_ACTOR="$W2"` stores the COMPOSED `by`. Mutant A (guard dropped) reddens the 7 new domain legs, `claim-ref-non-uuid-rc2` among them reproducing the live lie (`want rc=2 got rc=0`, answering `4242`). The same commit DRYs the uuid predicate into ONE `_is_uuid` shared by `ack`/`enroll`/`promote`/`retire`/`claim-ref` (four byte-identical private copies removed; zero behaviour change, pinned by four new rc-2 legs). · **#503 (2026-09-22, toolsmith lane): `enroll` WARNS when a supplied `--topic` names no live `session_bindings.thread_id`, and the `roster-enroll` event now RECORDS the supplied topic.** `--topic` was previously taken verbatim and coerced with `tonumber`, so ANY numeric value enrolled rc 0 — a live row carried `topic_id 478` (an ISSUE number, never a forum topic) and its enroll event recorded no topic at all, so the bad value was untraceable after the fact. **rc is UNCHANGED on every path (warn-only)**: enrollment runs at lane bring-up and must not be blockable by an absent or busy db; the db is derived from the state dir (`dirname STATE_DIR/opencrabs.db`, overridable with `OC_LEDGER_DB`, read through `lib/oc_sqlite`), an absent db is SILENT rather than an error, and an omitted `--topic` is untouched. Selftest 404 -> 409 (+5 assertions); negative control on the pre-fix artifact reddens exactly `enroll-dead-topic-warns` and `enroll-event-records-topic`, while the live-topic and omitted-topic arms are asserted SILENT. |
| oc-lint-laws | 0 | 2 | 0 clean / 1 findings / 3 tools-dir-or-corpus-file-missing · **M2-18 law-vs-tool corpus lint (HQ mechanical audit, n=3845).** The law names verbs and flags; nothing checked they EXIST — so `oc-ship-chain --resume` and `oc-ledger stamp proposal` each reached a lane and were caught only when an editor RAN the dead verb. This is the check: every `oc-*` invocation in the law corpus is resolved against the tool it names. Three classes — PHANTOM-FLAG (the tool's source carries ZERO occurrences of the flag), PHANTOM-VERB (a subcommand-dispatching tool carries zero occurrences of the verb), UNKNOWN-TOOL (no such file in `tools/`). **An invocation is scoped to its MARKDOWN UNIT, not its line:** outside a fence only backtick spans are scanned, inside a fence the whole line is one unit, because a span is what a reader copies. Line-scoping was the first draft's defect — 40 then 13 findings on a CLEAN corpus, every one a flag belonging to a DIFFERENT command later on the same line (`cargo test --locked` after `oc-prchecks`, `--gated-run` after `oc-deploy`); a second window is the tool→flag GAP, capped at 3 tokens so a flag five words into prose (`standalone oc-wt stays the fallback only when --create is unavailable` — that `--create` is `oc-wt`'s) is never attributed. **CITATIONS:** a law that DOCUMENTS an absence ("there is no `--resume` flag") names a phantom ON PURPOSE, so a line carrying an absence marker downgrades the hit to a CITATION — counted, printable with `--show-citations`, EXIT-NEUTRAL; failing the incident table that records the family would make the lint unusable. The marker list is deliberately narrow and `Do not forget to run X` does NOT suppress (selftest-asserted). `--strict` narrows "known" to case-arm declarations (`^\s+--flag)`) to catch a flag documented in prose but never wired; the default zero-occurrence rule UNDER-reports rather than mis-reports, because a linter that cries wolf gets ignored. Corpus = the 11 normative markdown files; `CHANGELOG.md` and `war-stories.md` are EXCLUDED (they quote historical invocations that were correct when written). `OC_LINT_LAWS_TOOLS_DIR` is a TEST HOOK pointing the tool scan at a fixture `tools/` dir. `--selftest` = 15 offline fixture assertions, including the wide-gap regression guard. |
| oc-log-search | 0 | 2 | 0 matches-found / 1 zero-hits |
| oc-notify-fanout | 0 | 2 | **wave receipt attribution (#19, 2026-09-12):** the `notify-fanout` ledger row now passes an explicit `--by "${OC_ACTOR:-oc-notify-fanout}"`, because `oc-ledger stamp` refuses an anonymous row — a wave stamps under the caller's lane when known, else under the tool. 0 fanout-complete / 3 ledger-unavailable / 4 no-valid-targets / 6 dry-run-plan (informational) / 7 concurrent-refused (wave-id lock: holder pid+ts named in message; stale auto-cleared) · per-lane idempotency: `wave-<id>.sent` resume semantics — duplicates skip cleanly, never double-notify · forum-scope guard: targets must be bound to opencrabs-dev forum chat, override with `--forum-chat` / `OC_FANOUT_FORUM_CHAT` · **LLM ergonomics & flag aliases (2026-09-17, Triage quirk):** `--body` / `--body-file` accepted as aliases for `--text` / `--text-file`; unrecognized flags output the complete valid candidate flag list on stderr with rc=2 per fleet-directives LLM Ergonomics §2 · **law 9 target set (2026-09-12, defect #6)**: rows whose `.state` == RETIRED are removed from the target set BEFORE per-target validation (12 legacy rows predate v1.3 retire-removal; `e756b84b` was FIRST in two HQ waves, burning the slowest slot on a lane that can never act); the skip is journaled per wave (`SKIP <uuid> INACTIVE(state=RETIRED)`), the row is never sent · **law 10 partial-delivery verdict (2026-09-12, defect #5)**: every send ATTEMPT is recorded to `run/wave-<id>.targets` BEFORE the send (dedup'd, dry-runs excluded); `--verify <wave-id>` diffs manifest vs receipts — rc 0 COMPLETE / rc 8 PARTIAL naming each unreached lane on stderr (+ resume hint) / rc 3 no manifest — so a wave killed mid-loop (SIGTERM/cgroup teardown) can never masquerade as complete · in-wave send failures keep exit 4 but now print `PARTIAL DELIVERY: N of M attempted send(s) failed` with the resume/audit path · per-send bound: `OC_FANOUT_SEND_TIMEOUT` (an explicit value WINS; otherwise the cap is DERIVED once per wave from a one-shot probe of the CLI — see D-3 below) — `timeout` per send, so a hung lane becomes a FAIL receipt rc=124 and the wave continues; measured 2026-09-12: 20/47 sends were >30s from a real outage, 75.6s max was a live delivery · **D-3 derived cap (HQ d72bd52d, 2026-09-13)**: the hardcoded 90s default sat BELOW the CLI's floor and EVERY send failed rc=124 — a no-op `session notify` now costs ~120s on the ops DB (950MB + 75MB WAL, 1,159 live sessions; the cost is the CLI's own session path, NOT the DB — a direct sqlite count is 51ms), so the v0.4.163 wave landed 14/28 with every failure at ~90,1xx ms. The cap is now `max(OC_FANOUT_SEND_FLOOR, probe_ms * OC_FANOUT_PROBE_MULT / 1000)`, derived from a probe carrying the ALL-ZEROS uuid — rejected at CLI validation (rc 2), so it SENDS NOTHING while still paying the startup cost. The probe runs ONCE, BEFORE the target loop (inside the loop the derivation would depend on run state: a wave where every target dedupes to SKIP would derive nothing — the same argv-echo shape this fix removes), and only when the cap is unset and `dry=0`. The measured probe and the resulting cap are reported on stderr (`SEND-CAP probe <ms>ms -> cap <s>s`) so the number is auditable rather than asserted. Knobs: `OC_FANOUT_PROBE_CAP` (bound on the probe, default 300) · `OC_FANOUT_PROBE_MULT` (headroom, default 3) · `OC_FANOUT_SEND_FLOOR` (minimum cap, default 90) · **law 15 cgroup detach (2026-09-12, defect #15)**: a wave inherits the cgroup of whoever launched it, so a launcher hosted by the unit a swap is about to bounce (`opencrabs-ops.service`) gets its wave SIGKILLed mid-flight at the swap's `systemctl --user restart` — survivors got their briefs, the rest never heard, NOTHING reported the truncation (HQ d72bd52d: `wave-2b2a99d25bd2` last wrote `.sent` at 09:38:18Z after exactly 5 lanes, no live process by 09:45, killed by the 09:39:02Z swap; prior waves died at 211s and 530s). The wave now re-execs under a transient scope OUTSIDE the unit (`systemd-run --user --scope --collect`) BEFORE the wave lock is taken, so a re-entry can never deadlock on a lock this invocation already holds (same pre-lock ordering as oc-deploy). Overrides: `OC_FANOUT_UNIT` (default `opencrabs-ops.service`) · `OC_FANOUT_IN_UNIT_CGROUP` (auto\|1\|0 test hook) · `OC_FANOUT_DETACH_CMD` (MUST block and pass the child's rc through — `setsid -w`, never bare `setsid`) · `OC_FANOUT_DETACHED` (loop guard). Detach is a no-op outside the cgroup; `--verify` is read-only and never detaches. · **law 5 quiet default (2026-09-12, HQ defect d72bd52d)**: `--mode now|turn-end|quiet` is passed straight through to the CLI and **DEFAULTS TO QUIET** — `hq.md §Duty 3` (owner law 2026-09-07) mandates `delivery=quiet` for ANY version bump, so a tool whose default was `now` could not deliver the law it exists to serve. In quiet mode there is NO interrupt-retry: a deferred verdict (rc 0 + notification id) IS the expected result, and re-issuing would convert a deferred send into the immediate wake the flag prevents — the retry fires only for the non-quiet modes. An invalid `--mode` dies rc 2 BEFORE any send. `--mode` is emitted BEFORE `--text` on purpose: the argv tail stays `... --text <brief> <uuid>`, the shape the selftest's `sent_brief()` parser keys on. · **law 16 self-identity derived, never hardcoded (defect D-1, HQ 2026-09-12)**: `self_uuid` — the lane a wave EXCLUDES as its own invoker — resolves from `--self`, else `OC_FANOUT_SELF`, else the invoking session's `OC_ACTOR` (TRAILING token, so a composed `"<role> <uuid>"` resolves too); an unresolved self is a **HARD ERROR** (rc 2, no wave). The production default used to be a hardcoded lane uuid (`2fae1230-…`, a selftest fixture value promoted by `7c7844cd` at v0.4.123): it excluded the Toolsmith lane from EVERY wave while leaving `--include-self` dead for every other invoker, so HQ self-notified its own brief — 7 waves, 53 uuids delivered, Toolsmith in 0. The battery could not catch it because every fixture run passes `OC_FANOUT_SELF` explicitly, so the DEFAULT path was never exercised (assertions added with this fix). A wrong self is INVISIBLE — it silently drops a real target — which is why the tool refuses to guess one. **D-5b (HQ d72bd52d, 2026-09-13):** the auto-appended RELOAD line used to pass `${version}` as argv — the LIVE version the wave itself announces — so every lane received a check whose argv == live BY CONSTRUCTION and could only answer NO-DRIFT (lane 212b3c83 printed NO-DRIFT 8x and cited it as compliance evidence). The line now prescribes the OMIT-ARG form, `oc-drift-check <uuid> --ack`, which reads the lane's recorded `last_acked` instead — a pure function of state on disk. Selftest 66 -> 68 (+2 assertions: the omit-arg form SHIPS, and no version is handed to the checker as argv); a negative control restoring `${version}` reddens exactly those 2, and one dropping `--ack` reddens exactly 1. · **law 17 dormancy/activity filter (owner order 2026-09-13):** eliminates waking idle workers across fanout waves. Core roles (`toolsmith`, `triage`, `hq`) ALWAYS receive fanout broadcasts regardless of idle duration. Worker lanes whose SQLite session recency `updated_at` is older than 12h AND that hold no active worktree or ledger claim are pruned cleanly with receipt `SKIP <uuid> <role> DORMANT(idle_hours=XX.X)` (counted in skipped total, never sent). Workers with activity within 12h or with active worktrees/claims are retained. Selftest + 3 negative controls: 74 assertions. |
| oc-order-validate | 0 | 1 | 0 VALID / 2 UNMERGED / 3 UNSIGNED-unknown / 4 UNKNOWN-REF |
| oc-ping-proof | 0 | 2 | 0 WOKEN / 1 SILENT / 3 UNREACHABLE / 4 parse-fail |
| oc-pr-atomicity | 0 | 1 | 0 ATOMIC / 2 NON-ATOMIC / 4 PR-not-found |
| oc-pr-fault-scope | 0 | 2 | 0 IN-SCOPE / 1 BASE-FAULT / 3 gh-fail |
| oc-prchecks | 0 | 2 | 0 GREEN / 3 RED / 4 dispatch-api-lock / 5 in-flight-timeout / 6 CANCELLED-superseded / 7 carrier-head-unresolvable / 8 AMBIGUOUS-same-ref-witness-unverifiable (fail-closed, dispatch refused, #115B) / 4 also = ADOPTION IDENTITY MISMATCH (adopted run is not workflow_dispatch on carrier — n=1721 B; headSha pin impossible by design, n=1730; mismatch rc-4 emits OC_PR_SUPERSEDED token = NON-retryable do-not-resume, C-A1 v0.4.104) · `wait <ref> [--budget N] [--poll S]` subcommand (v0.4.145, lens C-1): single-invocation blocking gate wait loop (default budget 2700s, poll 30s) eliminating ad-hoc sleep loops; rc 0 GREEN / rc 3 RED / rc 5 timeout / rc 2 usage · `--fast` passes `fast=true` to workflow dispatch (runs cargo fmt + clippy; skips cargo test) · `no_cancel` INPUT-CONTRACT PROBE (2026-09-17, Triage QUIRK 23:03Z): for a full-40-hex INPUT ref (immutable cutover / swap-mode gate) the dispatch carries `-f no_cancel=true` so pr-checks.yml's `cancel-in-progress: ${{ !inputs.no_cancel }}` cannot SIGTERM an in-flight run on the same sha; branch refs omit it (a moved PR-lane head genuinely invalidates the old verdict, so supersede stays correct). The flag is sent ONLY when the carrier yml — fetched once at step 2 with `Accept: application/vnd.github.raw` and reused for the existence gate — actually declares the input; a tool flag is not a workflow input, and GitHub 422s an undeclared one (`Unexpected inputs provided`), which killed EVERY full-sha dispatch rc 4 while the tool's own selftest stayed green on a string it had itself written. The probe makes the tool correct against BOTH yml versions, so tool and carrier can land in either order. Race guard: if the dispatch still 422s with the flag (yml reverted between probe and dispatch), it auto-retries WITHOUT the flag rather than failing the gate over an optimisation. Journal carries `yml_no_cancel_input` + `no_cancel_sent`; dispatch failure now surfaces gh's own 422 body on stderr instead of a bare rc 4. · creates in-flight run checkpoint `$LOCK_DIR/runs/in-flight/<session_id>.json` upon run adoption, preserved across signal kills and cleaned up on normal exit |
| oc-rebase-safety | 0 | 2 | overlap: 0 zero-overlap (gate-skip permitted per re-gate split law n=2259) / 1 overlap-found (gate-required) / 3 git-fail. audit: 0 clean / 1 losses (DROPPED or CHANGED rows on stdout) / 3 git-fail. Read-only plumbing; exit 1 is a VERDICT (house rule, oc-upstream-delta class). **Output shape (#22, 2026-09-12 — two lanes misled in one hour):** the conservative path (no `--tree`, or a single-parent tip) cannot derive the branch-side set, so it treats the FULL main-shift set as overlapping — that list is therefore **NOT an intersection**, and it used to print bare under a heading that read as an overlap verdict. A real overlap of ZERO was read as a file collision by two lanes (6630dc9a flagged it; 462181e9 nearly filed a false collision). Now: the conservative path heads its list `MAIN-SHIFT (not an overlap — merge-changed set unknown for a single-parent HEAD)` and the verdict line says outright that the list above is NOT an intersection; the real-intersection path heads its list `OVERLAP (real intersection of main-shift and branch-side)` and prints ONLY the intersected files. The verdict logic is unchanged — only the label was the defect. Nothing parses this stdout (the one consumer, oc-deploy, calls `audit`). Selftest asserts both shapes and the cross-checks (real is not labelled MAIN-SHIFT, conservative is not labelled real). |
| oc-review-persist | 0 | 2 / 3 / 4 | 0 persisted (re-read verified) / 2 usage (bad lens/empty input) / 3 re-read sha256 mismatch (storage NOT trusted) / 4 write-fail (mkdir/write/index-append); check-cycle: 0 census complete / 2 usage / 3 catalog UNDERIVABLE (refuses to call a cycle clean off an unreadable catalog) / 5 lens(es) missing -- unreceipted (canonical name, no index line) and UNPERSISTED (report present under a non-canonical name) fail only under --strict-receipt |
| oc-roster | 0 | 2 | 0 ok / 3 unreadable source (session DB, ledger, or `oc-wt`) · DERIVED in-progress roster (2026-09-11, plan step 1 fork-rebase gate): joins ledger `events[kind=claim]` (INTENT) + `oc-wt list` dirty (EVIDENCE) + session-DB liveness (LIVENESS) + forum bindings (SCOPE) into one classified table. Verbs `live [--detail]` (liveness only — set-equal to the `oc-notify-fanout` v0.4.129 query), `forum [--detail]`, `claims`, `work`, `classify [--json]` (TSV uuid/role/signals/class; classes ACTIVE/IDLE/ORPHAN/UNKNOWN — UNKNOWN is ESCALATED to the owner, never guessed). A claim author the session DB has never seen is a PHANTOM: reported on stderr, EXCLUDED from every class (a hand-assembled frankenstein uuid can never enter a freeze list — n=2221 class). Stores nothing; `--selftest` runs 44 offline fixture checks |
| oc-seal-state | 0 | 1 (noop) / 2 (unknown flag, F-L1) | 0 OK / 1 bare-invocation-noop / 2 unknown-flag-usage OR CONTRIBUTOR-SCAN-FAIL / 3 WRITE-FAIL-INVALID (usage surfaces = 2; 3 is always a write fault, lens A-6 v0.4.96) |
| oc-shadow-rotate | 0 | 2 | 0 ok-noop / 2 io-fail (usage merged into 2, C-#3) |
| oc-ship-audit | 0 | 2 | 0 all-SWAPPED / 1 ORPHANED |
| oc-ship-chain | 0 | 9 | 0 SWAPPED (chain complete: gate→issue-log→ff-merge→ship→swap, one chain-id) / 3 precondition not met — pre-flight (dirty or missing fork checkout, unpushed remote branch, or LEG1 gate returning oc-prchecks rc 2 usage/sha-shape/unknown-ref; lane fix, not infra) / 4 GATE-RED or CARRIER-RED (structured verdict; lane fix — CARRIER-RED is oc-deploy ship rc 6, a carrier run that reached a terminal-RED conclusion) / 5 NON-FF (lane rebases via oc-rebase-safety then re-runs) / 5 TIP-MOVED (the branch tip no longer equals `--sha`: a rebase + force-push happened while the gate ran, so LEG3 must NOT land an ungated tip — re-gate and relaunch) / 5 REBASE-GATE (ship rc 5 carrying the machine-readable `OC_DEPLOY_GATE=rebase-conflict|rebase-audit-failed` token on stderr: oc-deploy's own in-tool auto-rebase refused the push — the same lane fix as NON-FF, NOT a timeout) / 6 dispatch/poll infra (prchecks rc 4/7/8; ship rc other than 0/5/6) / 7 GATE STILL IN FLIGHT (prchecks rc 5 after gate budget + resume-polls; run id printed, re-run with `--gated-run <id>`) / 8 SHIP-WAIT-TIMEOUT (ship rc 5 with NO rebase-gate token: the carrier build did not reach a terminal state inside the LEG4 budget — the run is still BUILDING, do NOT re-dispatch it; wait for it, then resume with `oc-deploy poll --sha <sha> --execute --wait <sec>` or re-run the chain with a larger `--wait`) / 9 SHIP-LOCK-TIMEOUT (defect #391, 2026-09-19: another chain held the landing lock for the whole queue budget. A QUEUE condition, NOT infra and NOT a broken gate — do NOT re-dispatch the gate, wait for the holder or raise the budget with `SHIP_LOCK_TIMEOUT` / `OC_SHIPCHAIN_LOCK_WAIT`. The lock is acquired at LEG3 and released only from the exit trap, so it is held across LEG3+LEG4+LEG5; the default budget is therefore LEG4's `--wait` + 600s. The old hardcoded 600s was structurally shorter than the ~3300s hold, so every queued chain expired by construction — measured over tools.log shipchain rows: 56 contended, 36 acquired-after-wait, 10 timeouts; and the old rc 6 death read as INFRA, sending lanes to re-dispatch the gate and burn another full cycle). Runs `--fast` pre-merge gate by default (`fast=true`, ~2.2 min); `--full` or `--no-fast` opts into full ~30 min gate suite. **LEG1 gate rc 5 is non-terminal**: resume-polls (default 2, `OC_SHIPCHAIN_GATE_RESUMES`) before exiting rc 7. `--gate-wait <sec>` (default 2700s; `OC_SHIPCHAIN_GATE_WAIT`) sets LEG1 budget; `--wait` sets the LEG4 ship/swap budget (default 2700s; `OC_SHIPCHAIN_SHIP_WAIT`). **Defect #336 (2026-09-18, Toolsmith):** the LEG4 budget defaulted to 900 s while a carrier build on this box takes 947-1344 s (median 1136 s) — predicate: `gh run list --repo leshchenko1979/opencrabs --workflow quick-build-linux.yml --limit 40`, completed runs with conclusion=success, duration `updatedAt - createdAt` (queue + build); the last 9 such runs on 2026-09-18. The old default therefore expired on every one of the 10 most recent healthy builds and every one surfaced as the generic INFRA branch (rc 6) — a slow build and a broken carrier were the same exit code. Build time stepped up rather than drifting: the 27 before them ran 394-651 s (6.6-10.8 min) apart from one 963 s outlier, so the 900 s budget used to hold and stopped holding. The default is now 2700 s (22.6 min of headroom over the worst observed, 1344 s), and the LEG4 rc mapping is contract-faithful: `oc-deploy ship --execute --wait` exits 0/2/5/6 from the ship verb and NEVER 3 (every `die 3` in oc-deploy carries a `fanout:` prefix), so the old `3) CARRIER-RED` arm was dead code whose label lied while a real terminal-RED (rc 6) fell through to INFRA. rc 5 is now split by cause — rebase-gate token → rc 5 (lane fix), no token → rc 8 (wait-timeout, resume by waiting). Scope ends at SWAPPED (smoke and owner approval stay outside). No session notifies emitted. **Journaling**: stamps `kind=shipchain` under `CHAIN-<shortsha>-<UTCstamp>` per leg; journal write failure logs on stderr and is never fatal. **Pre-flight**: verifies `$BRANCH` on fork remote (`refs/heads/$BRANCH`) before dispatch; missing branch returns rc 3 precondition error. **Actor defaulting (v0.4.152 §Attribution & Goal Hygiene A1, 2026-09-12)**: an explicit `OC_ACTOR` wins; otherwise the chain derives it from the `Session-Id` trailer on the `--sha` commit and EXPORTS it, so its own journal rows and every child tool's rows are attributed instead of landing `(unattributed — pass --by or export OC_ACTOR)`. Nothing is invented — but since `oc-ledger stamp` REFUSES an anonymous row (#19, 2026-09-12) the chain's journal call passes an explicit `--by "${OC_ACTOR:-oc-ship-chain}"` fallback: the honest actor when no lane can be derived (precedent `oc-deploy` stamps `--by "oc-deploy recover-receipt"`), so a chain keeps its ledger trail instead of trading it for a loud journal failure. **LEG1 journal prints the resolved argv (2026-09-12, defect #9)**: the gate-start journal line is built from the invoked `PRCHECKS_CALL` array — the old line restated `${FAST_GATE:+ --fast}` from the flag variable, and since `FAST_GATE` is always "1"/"0" the journal claimed `--fast` even on `--full` runs. Selftest asserts argv truth in both modes (`--fast` present by default, absent on `--full`) + the sha named. **LEG1 docs-only gate skip (#20, owner ruling 2026-09-12 11:04Z — “We don’t need the pure docs commits to pass through ci on our side”)**: a FRESH dispatch whose EVERY changed path (`git diff --name-only <sha>^..<sha>`) is markdown, and none of which is compiled into the binary, skips LEG1 entirely — journal `GATE-SKIPPED-DOCS sha=<sha> reason=every-changed-path-is-markdown-and-none-is-compiled-into-the-binary paths=<csv>` plus the same text on stderr, so a skip can never be mistaken for a passed gate. The predicate is deliberately NOT an extension check: 21 distinct `.md` paths are `include_str!`/`include_bytes!`-compiled into the binary, so editing one changes compiled output and MUST still gate. That set is DERIVED from the source tree on every chain (`include_str!` resolves relative to the CONTAINING `.rs`, not the crate root, so literal+containing-file are resolved as a pair) and is never hardcoded — a rotted skip-list would let a RED binary through a skipped gate. `--gated-run` / `--gated-sha` are explicit “verify THIS run” requests and are honoured regardless of the sha’s contents; `OC_SHIPCHAIN_NO_DOCS_SKIP=1` forces the gate even for a docs-only sha. Selftest section 20 covers the skip (journaled, no gate dispatched, visible on stderr), the compiled-md negative case (README.md still gates), and the override. **Cgroup detach / restart survival (LAW 17, defect #21, 2026-09-12):** a chain launched from a session is a child of the daemon and therefore sits INSIDE the unit cgroup a swap's `systemctl --user restart` tears down — it is SIGTERM'd mid-leg and simply STOPS, with no ledger row and no failure report (two chains died this way at the 11:36:46Z swap: one `gate infra rc=143`, one correctly QUEUED on the ship lock and killed for waiting, so serialisation gave it a false sense of safety). The chain now re-execs outside that cgroup BEFORE `acquire_ship_lock` via a transient scope (`systemd-run --user --scope --collect`); pre-lock ordering is load-bearing because the chain holds its lock across LEG3/LEG4 and a queued chain can wait minutes — a post-lock re-entry would self-deadlock. The detach command MUST BLOCK and pass the child's rc through (`setsid -w`, never bare `setsid`) or a NON-FF refusal reads as success. **LAW 17 RESIDUAL — stdio is a SECOND teardown (2026-09-12, editor 462181e9 report, field-proven):** the cgroup is not the only thing the launching tool call owns. A chain started from a CAPTURED tool call inherits that call's stdout/stderr PIPES, so the re-exec'd scope survived the cgroup teardown and then died EPIPE on its FIRST stderr write — `CHAIN-6f62b2c1-20260912T115518Z` ended 26 minutes in with a 0-byte log, no LEG3 row and no `rc` field, its first stderr write being `acquire_ship_lock`'s `printf ... >&2`; the scope's journal showed no kill line, so it simply STOPPED. The re-exec now binds stdio to a FILE (`</dev/null >>"${OC_SHIPCHAIN_LOG:-/tmp/oc-ship-chain-detached-$$.log}" 2>&1`) because a caller-side redirect only covers callers who remember; the selftest asserts the binding via a fixture marker that can reach a file only when the redirect exists. **Hermetic selftest (same change):** the detach hook defaulted to `auto`, which reads the HOST's /proc/self/cgroup, so seven assertions passed or failed depending on WHERE the selftest ran — inside the unit cgroup the detach fired in tests that have nothing to do with detaching, and they passed only because the re-exec inherited the captured pipe. The stdio redirect removed that accident and they went red. The selftest now exports `OC_SHIPCHAIN_SELFTEST=1`, under which `auto` resolves to 0 (outside the unit); detach tests set `OC_SHIPCHAIN_IN_UNIT_CGROUP=1` explicitly. Selftest 88/0. **LEG3 TIP PIN (defect fix 2026-09-12, lane c6b1a539):** LEG3 merged `$BRANCH` BY NAME with no `tip == $SHA` assertion, and a lane rebase + force-push mid-gate is a NORMAL operation here — so LEG3 could fast-forward fork main onto a tip NO GATE VALIDATED while LEG4 shipped the ORIGINAL `$SHA`, leaving main and the shipped artifact divergent while the ledger still read `DONE SWAPPED sha=$SHA`. Near-miss: chain 3811585 pinned c5fc4203, the lane force-pushed 234c0d51 mid-gate, and FORK_REPO already carried a local branch at 234c0d51. LEG3 now resolves the tip ONCE, refuses rc 5 with a TIP-MOVED journal line, and merges `$TIP` — there is no second resolution for the ref to move under. **LEG5 CONFIRMS THE MARKER, NOT A STDOUT TOKEN (defect fix 2026-09-12, editor 462181e9, receipted live):** LEG5 scanned the poll's stdout for `*SWAPPED*` — a token `oc-deploy` has NEVER emitted (a case-sensitive sweep of the whole tool returns ZERO; the uppercase hits are variable names like SWAP_TARGET). `poll --execute` hands off to a transient Phase B unit and exits 0 PRE-bounce BY DESIGN, so its stdout cannot carry a completion marker: EVERY successful chain exited rc 6 'swap not confirmed' on a box that was already swapped, and the natural next move for a lane that trusts the exit code is to RE-RUN the chain — a double-swap on an already-swapped host. The selftest could not see it because its `fake-deploy` stub DID print the token: the fixture was more permissive than the real tool. Both stubs are now contract-faithful (ship records what it shipped, poll hands off with NO completion token, status reports the marker) and LEG5 reads `deployed.sha` through `oc-deploy status --json`. **Harness guards (same change):** `set -e` plus a bare `OUT="$(...)"` aborted the ENTIRE selftest on the first failing chain (rc 6, zero output, no assertion printed) — a harness that cannot report a failure cannot be trusted green; the two unguarded assignments are now `RC=0; ... || RC=$?`. Negative control: restoring the dead token predicate turns 26 assertions RED, every one of them a chain that expected rc 0 and got rc 6. Overrides: `OC_SHIPCHAIN_UNIT` (default `opencrabs-ops.service`) · `OC_SHIPCHAIN_IN_UNIT_CGROUP` (auto|1|0 — test hook) · `OC_SHIPCHAIN_DETACH_CMD` · `OC_SHIPCHAIN_DETACHED` (loop guard). The journal-failure diagnostic now BRANCHES on the cause: rc 143/137 names the restart/cgroup teardown and says re-run the chain, while a genuine kind rejection keeps the KINDS hint — the old message printed the KINDS hypothesis for ANY non-zero rc, sending the operator to repair a component that was not broken. Selftest section 21 covers detach-fires-inside, loop-guard, no-detach-outside, survival of a process-group kill, and the in-selftest negative control (detach off → TRUNCATED). · **`--eval-docs-skip [<sha>]` (J-7 mechanization)**: evaluates whether a commit qualifies for the Pure-Docs LEG1 gate skip per fleet-directives.md Pure-docs LEG1 skip law (v0.4.161); standalone CLI evaluator (rc 0 docs-only / gate skip permitted, prints matching paths on stdout; rc 1 gate required / contains code or compiled-in markdown; rc 2 usage / missing sha; rc 3 bad commit ref or git error) |
| oc-skew-scan | 0 | 2 | 0 clean / 1 skew / 3 parse-fail |
| oc-start | 0 | 2 | 0 Initialized / 2 Usage error / 3 Claim refused (already active) / 4 Branch exists / 5 Git or worktree failure · Unified claim, branch, and worktree initializer (Cycle c14). Runs `oc-ledger claim --issue <N>`, creates feature branch from base (default origin/main), and mounts clean worktree via `oc-wt add` in `$HOME/opencrabs-wt/<task>` (override via `OC_WT_BASE`). The STARTED line REPORTS the path resolved by `lib/oc-wt-resolve.sh` — the same predicate `oc-wt` uses (#429: `list` strips the `oc-wt-` prefix, so a task's tree commonly lives at `$WT_BASE/oc-wt-<task>`, not at `$WT_BASE/<task>`). Exits print exact resume commands. |
| oc-smoke | 0 | 2 | 0 Smoke PASS / 1 Smoke FAIL / 2 Usage error / 3 Precondition-or-infra failure · Unified 4-leg smoke verification & verdict row generator (Cycle c14). Wraps `oc-smoke-evidence` live binary identity check, optional behavioral `--probe <cmd>`, canonical `$STATE_DIR/smoke-verdicts.log` tab-separated row generation with newline-safe append, and automatic `oc-ledger stamp done` claim closure upon PASS (suppressible via `--no-ledger`) · **#432 (2026-09-20, toolsmith)**: the `target=` DEFAULT is the DEPLOYMENT UNIT `live-ops`, not the smoke's own subject. It was seeded `Issue #<n>`, so every run that omitted `--target` — the common path — wrote the one value the v0.4.218 ledger-hygiene ruling says the key must NOT carry: 65 of the 178 non-conforming rows measured in #432 are this tool's own default rather than lane prose. An explicit `--target` still wins. · **#436 + #306 (2026-09-21, toolsmith)**: the row's OPENING CLAUSE names its own verdict — it was the literal `4-leg smoke pass:` on EVERY row, so all 6 FAIL rows then in the canonical log (predicate: evidence field starting `4-leg smoke`; instant 2026-09-21T00:00Z) asserted a pass in their first five words, i.e. the durable record contradicted field 2 exactly on the rows a reader consults when something went wrong. On PASS the phrase is unchanged byte-for-byte (58 of 68 PASS rows carry it; no migration, no greppable marker spent). The same clause asserted `lineage=true` for a leg this tool CANNOT compute — `grep -c 'is-ancestor\|merge-base' tools/oc-smoke` → 0 — so `lineage=` now reports what the CALLER checked via the new `--lineage verified|failed|unverified`, defaulting to `unverified` (a leg not checked must not read as a checked one), and a caller-reported `failed` forces the row to FAIL, as an identity mismatch or a failed probe already does. The new token is validated: anything outside the three values is rc 2. |
| oc-smoke-evidence | 0 | 2 | 0 IDENTITY-MATCH / 1 MISMATCH / 3 unit-or-proc-fail · **`--append-log <path>` (2026-09-12, defect #13, lane c10cd97b)**: the SANCTIONED append path for state smoke-verdicts.log — appends the evidence block newline-safely (a log left unterminated by a previous writer is terminated first, rows can never glue); appends on rc 0/1 (verdict rows are evidence either way), writes nothing on rc 2/3; plain shell `>>` is what glued c10cd97b's rows — do not use it · **M2-2 (2026-09-12, toolsmith)**: the path ARGUMENT is now optional and a WRONG path is unrepresentable — a bare `--append-log` targets the canonical ABSOLUTE `$STATE_DIR/smoke-verdicts.log`, a RELATIVE argument resolves against `$STATE_DIR` (it used to land in the CALLER'S CWD — a decoy that looks right and holds none of the evidence; 2 rows lost), an absolute argument is honoured, and any target that is not the canonical log emits a loud `NOT the canonical` NOTE on stderr · **D-4 (2026-09-13, toolsmith, HQ d72bd52d)**: `--late-entry --sha <sha>` emits a HISTORICAL VERDICT ROW — the tool's identity block reads the RUNNING daemon's MainPID/artifact sha and so structurally CANNOT describe a past sha, leaving the sanctioned writer unable to record one (0 tool-written identity blocks vs ~14 lane-authored verdict rows in the canonical log). The row is built from ARGV: `ts \t PASS-LATE-ENTRY \t [run=] \t sha= \t [actor=] \t [issue=] \t target= \t [evidence=""]`; optionals are OMITTED when empty, never emitted as empty values (`run=` reads as a value, not absence); `--target` defaults to `live-ops`. It is a RECEIPT, NOT live-identity evidence: a distinct verdict token plus a stderr NOTE mean it can never be mistaken for one. `--sha` is REQUIRED (rc 2 without it) and `--markers`/`--negative-control` are REFUSED with `--late-entry` (rc 2) — they probe the running exe and are meaningless for a historical sha. rc 0 writes the row; rc 2 on misuse writes NOTHING · **#477 (2026-09-21, toolsmith)**: `--repair-row --match <text> --replace <text> [--log [<path>]]` — EQUAL-LENGTH in-place repair of a malformed smoke-verdicts.log row, i.e. the implementation of `upstream-merge-runbook.md §Ledger hygiene`, which ORDERS in-place repair and FORBIDS rename/truncate because the log is appended to CONCURRENTLY: a rename replaces the inode and orphans the `O_APPEND` fd a peer's `>>` holds, silently dropping that peer's rows (before this verb existed the order named no tool, and a lane hand-rolled the forbidden `os.replace`). `--match` must be UNIQUE and BYTE-equal in length to `--replace`; the target is re-read immediately before the write, spliced IN PLACE on the SAME INODE (`dd conv=notrunc`), synced, and the invariant is then PROVEN rather than assumed — inode unchanged, file did not shrink, region reads back as `--replace`. The length guard is a BYTE guard (`wc -c`), never `${#}`, which counts CHARACTERS under a UTF-8 locale and would happily accept a 2B→4B grow. `--log` carries the M2-2 discipline: bare/flag-like = the canonical `$STATE_DIR/smoke-verdicts.log`, relative resolves against `$STATE_DIR` (never the caller's CWD), absolute is honoured verbatim, and a divergent target announces itself on stderr. rc 0 repaired / 1 REFUSED (no unique match, a length-changing pair, or the target bytes moved under a concurrent writer) / 2 misuse (missing `--match`/`--replace`, a multi-line argument, or combination with `--markers`/`--negative-control`/`--park-owner-eye`/`--late-entry`) / 3 IO error or a failed post-write invariant. Argument SHAPE settles before any filesystem probe, so a malformed invocation is rc 2 whatever the state of the target; and a REFUSAL WRITES NOTHING — pinned by sha256 in the selftest, because a refusal that silently writes is the very defect class this verb exists to prevent. |
| oc-tg-audit | 0 | 2 | 0 clean / 1 violation / 3 log-missing |
| oc-upstream-delta | 0 | 2 | 0 clean / 1 delta (verdict) / 3 fetch-git-fail · `--json` outputs JSON object with base, ahead, behind, absorbed, and absorbed_candidates list |
| oc-waiter | 0 | 1 (retired) | RETIRED in v0.4.135: replaced by native detached bash execution (background: true); --help returns 0, subcommands return 1 |
| oc-watcher-audit | 0 | 2 | `[--since <ts>] [--json] [--dir <dir>] [--notify-orphans] [--dry-run]` · 0 clean / 1 violations-found (alarm) / 3 dir-missing · scans `tmp/detached/*.json` for `UNTHROTTLED_WATCH` (no `--interval`), `OFF_SPEC_INTERVAL` (interval other than 30/60 — the fleet law), `NOHUP_SPAWN`, `HANDROLLED_SLEEP_LOOP` · `--notify-orphans` scans interrupted detached tasks and in-flight checkpoints, resolves workflow run conclusion via `gh run view`, wakes dormant lanes with `session_notify`, and tracks notified tasks in `$LOCK_DIR/orphans_notified.json` to guarantee idempotent single-notification across repeated sweep cycles · `--since` is a rolling window: tasks spawned earlier are skipped, timestamps are parsed as datetimes (fractional seconds and `+00:00` offsets included), and a task with an unparseable timestamp is INCLUDED (age unprovable — never silently dropped) · `--json` emits a JSON **ARRAY** of violation objects carrying `state` + `live`, never an object · `--selftest` = 26 offline fixture assertions |
| oc-wt | 0 | 2 | 0 ok / 3 path-exists-dirty / 4 index-failed / 5 repo-branch-missing / 6 behind-base · Worktree manager. `add` creates at `$HOME/opencrabs-wt/<task>` (configurable via `OC_WT_BASE`); every verb RESOLVES a task through `lib/oc-wt-resolve.sh` — ONE predicate shared with `oc-start` (#429: the resolution is PARTITIONED — the canonical base by a 3-name ladder `$WT_BASE/<task>`, `$WT_BASE/oc-wt-<task>`, legacy `$HOME/oc-wt-<task>`, and every OTHER base by registry over the rows `list` reads). Column 1 of `list` is therefore exactly the task name `remove` accepts. |

## Carrier CI lane map — one `workflow_call` body behind two thin shells (v0.4.233, 2026-09-21)

The carrier branch `ci/quick-build-linux` carries **three** workflow files where it used to carry two. Both lane entry points are now thin callers of one shared body:

| File | Role | Dispatched as |
|---|---|---|
| `ci-lane-impl.yml` | the shared body — `on: workflow_call`; jobs `order-gates` + `build` (mode `build`) and `pr-gates` (mode `gate`) | **never** — no `workflow_dispatch` trigger, not dispatchable |
| `quick-build-linux.yml` | thin shell, `name: Quick Linux Build`; caller job `ship` → `mode: build` | `gh workflow run quick-build-linux.yml --ref ci/quick-build-linux` |
| `pr-checks.yml` | thin shell, `name: PR-lane gates`; caller job `gate` → `mode: gate` | `gh workflow run pr-checks.yml --ref ci/quick-build-linux` |

**No tool changes.** Both filenames are unchanged, so `oc-deploy ship` and `oc-prchecks` keep dispatching the same two files by name — the indirection is invisible to them. `--ref ci/quick-build-linux` remains MANDATORY (neither shell is on `main`).

**Job names are now PREFIXED, and the prefix is a SHIP-LANE hazard, not cosmetics.** Under `workflow_call` GitHub renders every called job as `<caller> / <called>`, so the live names read `ship / ORDER gates (<sha>)`, `ship / Linux amd64 (<sha>, <features>)`, `gate / PR-lane gates (<sha>)`. `run_built_sha` (`oc-deploy:1874-1882`) returns the sha from the **FIRST** `.jobs[].name` that decodes to a 40-hex, so a caller name carrying a run sha would SHADOW the real build job and the ship lane would build the wrong tree. Both caller jobs are therefore pinned to a short token (`name: ship` / `name: gate`), deliberately NOT the old `ORDER gates (...)` / `PR-lane gates (...)` expressions. The decode itself is prefix-agnostic — `oc_decode_job_embed` (`lib/oc-embed.sh:13-14`) anchors on the parenthetical — proven live 2026-09-21 on runs `35612234852` / `35612238741`: both resolve to the dispatched sha, and the skipped sibling jobs carry no 40-hex.

**`main` is untouched.** `?ref=main` returns the four main-resident files (`auto-assign.yml`, `ci.yml`, `prerelease.yml`, `release.yml`) and **neither lane file**; `?ref=ci/quick-build-linux` returns those four plus the three carrier files — `ci-lane-impl.yml` included, because a `workflow_call` target must exist on the same ref.

## Remote topology — `origin` is the single canonical remote (updated 2026-09-15)

`origin` is the sole push and fetch remote for the skill repo (`git@github.com:leshchenko1979/opencrabs-skill.git`, SSH transport).
The redundant `mirror2` alias (which pointed to the exact same repository) has been retired.

## Unified tools log (moved from SKILL.md v0.4.131)

Every tool in `tools/` sources `tools/lib/oc-log.sh` and appends ONE JSONL line
on exit — the fleet-analysis aggregate (per-tool journals remain the per-run
record).

- **Path:** `/root/.opencrabs/profiles/ops/opencrabs-dev/tools.log` (override with `OC_TOOLS_LOG`).
- **Schema:** `{"ts":"…Z","tool":"oc-…","args":"…","exit":N,"secs":N.N,"extra":{}}` — tools add fields via `oc_log_extra key value`.
- **Suppression:** the invocation IS a selftest — the `--selftest` FLAG **or the bare `selftest` SUBCOMMAND** (one shared `oc_is_selftest()` predicate; first-token match for the bare form, so a VALUE that merely reads `selftest`, e.g. `--dir selftest`, never silences a real run) — or `OC_TOOLS_NOLOG=1` (the battery exports it — synthetic runs never pollute the log). Missing `jq` → no write; logging NEVER changes the host tool's exit code. **M2-21 (2026-09-12, toolsmith):** the predicate used to match the flag token ONLY, but `oc-deploy:2976` dispatches `--selftest|selftest) selftest "$@" ;;` as ONE case arm — so a bare-subcommand selftest never exported `OC_TOOLS_NOLOG` and every fixture child it spawned was logged to the PRODUCTION log: **93 rows per run**, six phantom shas across two weeks (08-31 → 09-12) that `oc-ship-audit` read back as ORPHANED dispatches (rc 1, gating `oc-ledger commit-pending`). `oc-deploy`'s `selftest()` now also exports `OC_TOOLS_NOLOG=1` as its first statement so hermeticity does not depend on argv parsing; the predicate is the load-bearing layer (0 rows alone — the export alone still leaks the parent's own row).

Recipes (verified live):

```bash
# NOTE (2026-09-11, toolsmith): tools.log is JSONL, but it carries 2 NON-JSON
# lines (383, 443) — prose appended by hand on 2026-08-31; 443 is an
# evidentiary actor-correction, so it is NOT deleted. A plain `jq -r` over the
# whole file ABORTS there ("Invalid numeric literal at line 383"), which means
# two of the recipes below were labelled "verified live" while they could not
# run at all. They now read raw and skip unparseable lines (`-Rr 'fromjson?'`);
# malformed lines are dropped SILENTLY, so if a count looks short, that is why.
# failing invocations (note: rc≠0 is often a VERDICT, not a crash —
# oc-skew-scan 1 = skew found, oc-ping-proof 1 = SILENT; filter .tool first)
jq -Rr 'fromjson? | select(.exit!=0) | [.ts,.tool,.exit,.args] | @tsv' tools.log
# usage per tool
jq -Rr 'fromjson? | .tool' tools.log | sort | uniq -c | sort -rn
# newest line
tail -1 tools.log | jq -c .
```
