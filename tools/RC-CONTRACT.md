# tools/ RC contract (C-#3, v0.4.78)

Fleet-wide conventions, then the per-tool register. The battery
(`tools/tests/run.sh`, section "rc contract --help=0 fleet-wide") asserts the
`--help` row live: every `oc-*` tool must answer `--help` with rc **0**
(output content is not asserted — lens A L3, v0.4.79). Usage errors print WHY (C-#1) — a bare
code with no diagnostic is a bug.

Fleet conventions:

- `--help` / `-h` → **0** (usage text on stdout).
- Usage/argument error → **2** on every tool EXCEPT the legacy registers
  noted below (usage≠2: `oc-deploy` 1, `oc-artifact-verify` 1,
  `oc-job-verify` 1, `oc-order-validate` 1, `oc-index-worktree` 5,
  `oc-pr-atomicity` 1, `oc-seal-state` 1/2/3, `oc-ship-chain` 3/7,
  `oc-waiter` 1 (retired) — long-documented, selftest-asserted vocabularies;
  changing them would break every lane keyed on the old codes).
- Verdict codes are per-tool and documented here + in each tool header.
- `oc-prchecks` rc=2 carries a diagnostic + a same-args retry backoff
  (2s..10s inside 120s).

| Tool | help | usage | Verdict codes |
|------|------|-------|---------------|
| oc-artifact-verify | 0 | 1 | 0 PASS / 1 invocation / 2 NOT-ELF-MISSING / 3 MARKER-MISSING / 4 SHA-PROVENANCE-MISMATCH / 5 VERSION-MISMATCH · embed-source sha compares exact OR first-12-hex prefix (old-embed truncation tolerance) |
| oc-attrib | 0 | 2 | 0 ok / 3 git-fail / 4 empty-range / 5 markers-missing |
| oc-branch-sweep | 0 | 2 | 0 nothing-deleted / 1 deletions / 3 git-fail |
| oc-carrier-features | 0 | 2 | 0 set / 3 yml-unfetchable / 4 no-features-input |
| oc-commit | 0 | 2 | 0 committed / 3 gate-fail / 4 git-fail / 5 comment-fail |
| oc-deploy | 0 | 1 | 0 ok-noop / 2 rebase-gate-push-verify-rollback AND usage/validation diags (flag-validation `die 2`, e.g. poll `--wait` non-integer, `--notify-session` empty; ship rc-2 gate deaths carry machine-readable `OC_DEPLOY_GATE=<cause>` tokens: missing-sha / wait-plan-mode / notify-poll-only / rebase-needed / feature-mismatch / fetch-fail) AND ship FEATURE-DEFAULT explicit-mismatch (`--features` != deployed.meta.json, refused pre-dispatch; default path auto-adopts deployed set) / 3 retired / 4 stage-gate-launch + ship features-compat gate (silent feature-drop refused pre-dispatch; `--allow-features-drop` override) / 5 poll-wait-timeout + ship `--wait` timeout (ship --execute --wait N bounded-polls dispatched run: GREEN rc 0, timeout rc 5, terminal-RED rc 6; plan-mode `--wait` refuses rc 2) / 9 kill-file · `status [--json]`: inspects running binary vs on-disk target sha, uptime, and last-swap metadata; rc 0 running matches disk / rc 1 mismatch-or-unswapped / rc 2 usage · `contributors` verb RETIRED (rc 1 + loud pointer to `oc-attrib --contributors`) · **Carrier Build Coalescence & Ancestry Matching:** In GitHub Actions, carrier workflow `ci/quick-build-linux` uses `concurrency: group: quick-build-linux` (1 active, 1 pending; 3rd push cancels/replaces pending 2nd). `oc-deploy ship/poll` uses **ancestry-aware matching** (`git merge-base --is-ancestor "$SHA" "$CAND_SHA"`): if a carrier run finishes building a descendant commit that includes `$SHA`, the run is accepted as a valid build for `$SHA`. `swap-execute` acquires host-wide `flock -x $STATE_DIR/host-swap.lock` around `swap_execute()` and enforces **rebase-aware lineage monotonicity** to prevent stale binary overwrites. **Lineage guard (v0.4.148 ancestry-only; REBASE-AWARE v0.4.151 — the v0.4.148 form refused EVERY post-rewrite swap, fleet-blocking, reported 2026-09-12):** three-tier verdict over `OC_DEPLOY_REPO` — (1) **monotonic** `git merge-base --is-ancestor "$PREV_SHA" "$SHA"` rc 0 → accept, silent, NO journal row (fast path, unchanged from v0.4.148); (2) **rewritten** PREV orphaned by a fork-main rewrite, but a commit in `merge-base(PREV,SHA)..SHA` carries the SAME `git patch-id --stable` (identical change, new sha) → accept, journal `reason:"rewrite-equivalent-swap"` + `rewritten_twin:<sha>` + stderr note; (3) **absent** both objects exist and PREV's change is genuinely NOT in the incoming lineage → `die 2`, journal `reason:"non-monotonic-swap"`, machine-readable stderr token `OC_DEPLOY_SWAP_GUARD=stale-build` (so a genuine stale/delayed-build overwrite stays distinguishable from an orphaned-by-rewrite refusal). Verdict **unknown** (either object not in the repo) → guard SKIPPED, preserving the pre-v0.4.151 "if both exist" behaviour. Patch-id work runs ONLY on the tier-1 failure path, so the happy path still costs one `--is-ancestor`. `--allow-rewritten-lineage "<justification>" [--actor A]` is the audited escape hatch for a genuinely-absent change: justification MANDATORY (bare flag → `die 2` + `OC_DEPLOY_GATE=override-needs-justification`), acceptance journaled `reason:"rewritten-lineage-override"` with `justification` + `actor` (default `$OC_DEPLOY_ACTOR`/`id -un`). Read-only diagnosis verb `lineage-check [--prev P] [--sha S] [--repo R] [--json]` → rc 0 monotonic|rewritten, rc 2 absent|unknown, printing verdict + twin + next step. **Post-rewrite lane runbook:** a rebase orphans the deployed sha but no longer blocks swaps — re-run the same `oc-ship-chain` leg and it is accepted as `rewrite-equivalent-swap`; NEVER hand-edit `deployed.sha` to re-point around a refusal;  **Restart survival / cgroup safety (v0.4.153):** a DIRECT `swap-execute` inherits the launcher's cgroup; when that launcher is a session hosted by the unit being bounced (`opencrabs-ops.service`), step 8's `systemctl --user restart` SIGKILLs the swap AT the restart line, so steps 9-12 (post-verify, sidecar-sync, receipt, fanout) never run and - because the swap-result stamp is deliberately LAST (F-M1: receipt evidence must precede the stamp) - NO ledger row is written at all: the host is swapped and the ledger is SILENT (journal `swap-acb8e205-...` stops at seq 7 marker-write, mtime == swapped_at second, 2026-09-12). swap-execute now re-execs under a transient scope outside the bounce cgroup BEFORE any work (pre-lock); the detach command MUST BLOCK and pass the child's rc through (`systemd-run --user --scope`, `setsid -w`) - a non-blocking launcher returns 0 immediately and a guard refusal would read as success. Read-only repair verb `recover-receipt [--journal J] [--run R] [--sha S] [--dry-run] [--historical] [--json]`: stamps the missing `swap-result` row ONLY when the journal shows install exit=0 AND marker-write exit=0, carries NO receipt step, the sha resolves, and no existing row already quotes sha+run (idempotent - a re-run is a no-op); **rc 0 = handled** (recovered, or correctly nothing to do) / **rc 2 = refused** (the journal does not describe a recoverable real swap); `--historical` records a swap later SUPERSEDED instead of refusing; fan-out is NOT replayed (use `oc-deploy fanout --run <id>`). Selftest section 22 covers parent-kill survival, the negative control, and recovery idempotency. |
| oc-drift-check | 0 | 2 | 0 no-drift / 1 DRIFT / 3 ledger-skilldir-fail · skill-dir resolution v0.4.130 ruling n=2369: `--skill-dir` flag > `OC_SKILL_DIR` env > CANONICAL profile copy (`~/.opencrabs/profiles/ops/skills/opencrabs-dev`) — the invoking script's location is NEVER self-resolved (worktree copies produced contradicting same-day verdicts, 2026-09-10) |
| oc-harvest-census | 0 | 2 | 0 ELIGIBLE / 1 REFUSED (already MERGED or IN_FLIGHT upstream) / 3 API-or-git-fail |
| oc-harvest-dispatch | 0 | 2 | 0 APPROVED / 1 REJECTED (already merged, empty diff, superseded) / 2 usage / 3 git-or-api-fail / 4 CAPACITY_EXHAUSTED |
| oc-harvest-sweep | 0 | 2 | 0 clean / 1 findings / 3 git-fail |
| oc-health | 0 | 2 | 0 clean / 1 findings (warn-class present) / 3 measurement-failure (a check could not be measured — never silently treated as clean) · OWNER-ORDERED 8-class rotating sweep (2026-09-11): base invariants (disk headroom ≥85%, live deadlock locks, dead mirror reaping) + 1 rotating class per hourly run (filesystem, runtime_procs, persistence, git_vcs, schedulers, fleet_lanes, logs, issue_portfolio) · `--class <name>` audits a specific class, `--all` audits all 8 classes in a single full pass, `--rotate` advances `health-rotation.json`, `--status` displays current rotation state · `--reap` applies ONLY the provably-dead SAFE class (stale locks, backups beyond keep older than 24h, stale tmp `/tmp/oc-*`, leaked `mktemp` WORKDIRs `/tmp/tmp.*` matching the oc-deploy signature); worktrees are NEVER auto-removed (`oc-wt remove` has a dirty-tree gate a sweep must not bypass) · `--json` is the whole stdout contract (no chatter on stdout) · class & check catalog: `tools/HEALTH-CLASSES.md` and `tools/HEALTH-CHECKS.md` · overrides for hermetic testing: `OC_HEALTH_STATE|FORK|DB|TMP_GLOB|TMP2_GLOB|SKILL_DIR|WT_PATTERNS|SKIP_VERSION|LOG|ROTATION_FILE|DETACHED_DIR|WATCHER_WINDOW_H` · `--selftest` = 38 offline fixture assertions |
| oc-index-worktree | 0 | 5 | 0 OK / 4 index-failed |
| oc-issue-log | 0 | 2 | 0 posted / 3 gh-fail |
| oc-issue-sweep | 0 | 2 | 0 no-candidates / 1 candidates / 3 api-fail · **M2-25 (2026-09-12, toolsmith self-found, Duty TM1):** the EXIT trap removes EVERY temp file the run creates — `$TMP.raw`/`$TMP.sorted`/`$TMP.final`, not just `$TMP`. The trap was `rm -f "$TMP"` while the run wrote three siblings, so each invocation leaked 3 files into `/tmp` (measured 1070 files, 0.1 MB, oldest 2026-09-10). The selftest now asserts an empty `TMPDIR` sandbox after a real run; the negative control neutering the trap back to `rm -f "$TMP"` fails exactly that assertion. |
| oc-job-verify | 0 | 1 | 0 VERIFIED / 2 IN-FLIGHT / 3 FAILED / 4 REF-MISMATCH / 5 NOT-FOUND · embed-sha compares exact OR first-12-hex prefix (old-embed truncation tolerance) |
| oc-ledger | 0 | 2 | 0 ok / 1 verdict (cadence FIRE / version mismatch / `roster` or `events` NO-MATCH) / 3 ledger / 4 write / 5 battery-gate / 6 version-sync-gate · `--verbs`: lists registered subcommands; unrecognized verbs emit vocabulary hints on stderr; rc 0 · `claims <N>`: open claim-refs by issue, empty output + rc 0 = clean/unclaimed; rc 2 usage, rc 3 ledger · `lessons [--kind lesson|incident|all] [--class text]`: lesson/incident rows by class, one n/kind/t/what line per hit, empty rc 0 = none; rc 2 usage, rc 3 ledger · `roster [--role <substr>] [--live] [--include-retired] [--json]`: role → FULL uuid resolution; TSV uuid/role/topic/feature/ack, `--live` keeps only live sessions (profile session DB, read-only, OC_LEDGER_DB override), `--json` array; **law 9 (2026-09-12, defect #6): rows whose `.state` == RETIRED are EXCLUDED by default — a retired lane must never be a dispatch target and a role-to-uuid resolver that returns one is the same defect from the resolution side (live repro `e756b84b` retired_compiler resolved while retired); `--include-retired` lists them for diagnostics only, and a retired-only role is rc 1 NO-MATCH whose stderr names retirement as the reason**; **rc 1 = NO-MATCH verdict, never a silent empty**; rc 2 usage, rc 3 ledger/DB unreadable · `retire <uuid> [--topic N] [--role R] [--why TEXT]`: removes ONE row from roster (drops signing) and records `roster-retire` event; `--why` optional (defaults to "(no reason given)"); **ROW-SCOPED**: `--topic`/`--role` exact selectors; multiple matches without selector refused rc 2; rc 0 retired / 2 usage-or-ambiguous / 3 ledger unreadable · `events|tail [--n N] [--kind K] [--actor U] [--since T] [--json] [--full]`: recent ledger rows, TSV n/t/kind/by/what newest LAST; `--actor` substring match on `.by`, `--since` byte-lexicographic compare on `t`, `--full` lifts 160-char `what` clip; **rc 1 = NO-MATCH verdict**; rc 2 usage, rc 3 ledger unreadable · `stamp <kind> "<what>" [--by <label>] [--allow-unattributed]`: **#19 (2026-09-12, dispatched by Triage 530c29ec — found on Toolsmith's own law-12 row n=3794, and lane d5863180's n=3786 before it): an ANONYMOUS row is REFUSED rc 2.** `derive_by` correctly refuses to invent an actor and returns a sentinel; the verb used to write that sentinel into the ledger anyway, so warn-and-write made the defect the DEFAULT. Name the actor with `--by <label>` or `OC_ACTOR`, or declare the anonymous row deliberate with `--allow-unattributed` (the escape hatch a guard needs, cf. sync `--allow-sweep`). The refusal sits AFTER the ledger gate so a missing/corrupt ledger still reports rc 3, never a masked rc 2. `sync` is unaffected: it writes its own `by: "oc-ledger sync"` row directly. **M2-3 (2026-09-12, owner mechanical audit item M2-3):** `derive_by` RESOLVES a uuid FRAGMENT against the roster. The lookup was an EXACT compare, so 8 live rows carried a bare 8-char prefix (`462181e9`, `212b3c83`, `c6b1a539`, `d5863180`, `2fae1230`) whose full uuid IS rostered — that is an UNRESOLVABLE LOOKUP, not an unrostered lane, and each of the five resolves to exactly ONE roster uuid. A 4+ char hex/dash fragment matching exactly one worker now stamps as `<role> <full-uuid>`; 0 or >1 matches keep the sentinel, because an ambiguous prefix must never GUESS an actor. `OC_ACTOR` is lowercased first (uuids are lowercase). **No refusal was added, deliberately:** the queue's first cut (refuse the unrostered full uuid) was measured and rejected — 64 of the 86 full-uuid sentinel rows belong to uuids rostered TODAY (stamped inside the legitimate pre-`enroll` window), it turns two green assertions RED (`oc-ledger` selftest `derive-by-unrostered` + `tools/tests/run.sh` section 11, whose fixture ledger has `"workers":[]` and asserts the stamp WROTE), and `oc-ship-chain` passes an explicit `--by` fallback so a refusal there would trade the chain's whole ledger trail for a warning. An unresolvable actor now WARNS on stderr naming the remedy (`oc-ledger enroll <uuid> <role>`) while still stamping the sentinel; a meaningful LABEL (`hq/opencrabs-dev-factory`) is not a fragment and is written VERBATIM. Selftest 228 -> 239 (+11 assertions); negative control neutering the prefix predicate returns exactly those 2 red, no others. · `ack <uuid> <0.N.N> [--at <ts>]`: record a worker's skill-version adoption; rc 0 / 2 usage (uuid or version shape) / 3 ledger unreadable or unknown uuid (roster discovery first). **M2-4 (2026-09-12, owner mechanical audit item M2-4): `ack` is IDEMPOTENT per (uuid, version)**, mirroring `sync`'s dup guard. `oc-drift-check --ack` DELEGATES here (oc-drift-check:78), so one adoption that both drift-checked and acked wrote two byte-identical rows seconds apart (lane 1a63f103 n=3717/3718 two seconds apart; d18ce16a n=3713/3714; 462181e9 x3). A re-ack of a version already on record still REFRESHES `last_acked`/`updated_at` — so a lane re-acking after a failed git leg does not keep a stale `last_acked.at` — appends NO event, prints `(already on record — idempotent, no dup event)` and stays **rc 0**. rc 0 is load-bearing here: `oc-drift-check` runs `|| die 3`, so a non-zero on the dup path would break the delegated ack outright. The key is the EXACT composed string `worker <uuid> acked <v>`, never `contains` — a substring test would make 0.4.1 match 0.4.10 and swallow a real bump. A different uuid at the SAME version, or the same uuid at a NEW version, is new information and still appends. Selftest 239 -> 264 (+25 assertions); negative control neutering the guard returns exactly those 7 red, no others. **M2-23 (2026-09-12, toolsmith self-found, Duty TM1):** the `unrostered-actor` sentinel is IDEMPOTENT. A LABEL keeps the sentinel (`hq/opencrabs-dev-factory` -> `unrostered-actor hq/...`), but a value that ALREADY carries it is echoed VERBATIM with its own WARNING and never prefixed a second time. Origin: Toolsmith derived `OC_ACTOR` with `jq ... | sort -u | tail -1` over the `by` column, which returns the alphabetically-LAST distinct value rather than the LATEST, picked a 2026-09-09 sentinel row (n=2353), and `stamp` prefixed it again, writing `unrostered-actor unrostered-actor 2fae1230` (ledger n=3973; retracted n=3974, re-stamped n=3975). A doubly-prefixed actor is unattributable and breaks an exact-string actor grep, so the double prefix was precisely the thing the sentinel guard exists to prevent. Selftest 264 -> 267 (+3 assertions); negative control neutering the pattern returns exactly those 2 red (`-verbatim`, `-warns`), the `-rc` assertion staying green because it tests the exit code, not the guard. **M2-24 (2026-09-12, toolsmith self-found, Duty TM1 — live repro during the M2-23 stamp):** the value `derive_by` PRODUCES must be round-trippable. It emits the composed form `<role> <uuid>`, but every lookup accepted only a BARE uuid, so the composed form carried a space, hit the LABEL branch (`*[!0-9a-f-]*`), and fell through to the sentinel — the exact string the function itself writes could not be fed back. That is the natural caller pattern, not a slip: `oc-ledger roster --live --role R` prints a TSV whose first two columns ARE the uuid and the role. Live repro: n=3989 was written `unrostered-actor toolsmith 2fae1230-...` (retracted n=3990, re-stamped n=3991) — same family as n=3973. The branch resolves the TRAILING token and stamps the CANONICAL `<role> <uuid>` from the roster (the M2-3 normalisation: the roster's role, never the caller's guess); a two-word LABEL whose tail is not a uuid still takes the sentinel. It sits AFTER the M2-23 guard so a sentinel stays VERBATIM. Selftest 267 -> 271 (+4 assertions); negative control neutering the branch returns exactly those 2 red (`-canonical`, `-not-sentinel`), the `-rc` and `-label-sentinel` assertions staying green. · `sync` mechanics: battery-gated; commits both repos + tags; chains `oc-shadow-rotate` tail and mirror pushes (WARN-only); ack rows record skill-drift adoption; `--bundle` sweeps STATE receipts (tools.log/baseline.json/orders.json/journal); closed enum vocabulary (confirm, swap-result, notify-fanout, shipchain, roster-retire, claim). |
| oc-lint-laws | 0 | 2 | 0 clean / 1 findings / 3 tools-dir-or-corpus-file-missing · **M2-18 law-vs-tool corpus lint (HQ mechanical audit, n=3845).** The law names verbs and flags; nothing checked they EXIST — so `oc-ship-chain --resume` and `oc-ledger stamp proposal` each reached a lane and were caught only when an editor RAN the dead verb. This is the check: every `oc-*` invocation in the law corpus is resolved against the tool it names. Three classes — PHANTOM-FLAG (the tool's source carries ZERO occurrences of the flag), PHANTOM-VERB (a subcommand-dispatching tool carries zero occurrences of the verb), UNKNOWN-TOOL (no such file in `tools/`). **An invocation is scoped to its MARKDOWN UNIT, not its line:** outside a fence only backtick spans are scanned, inside a fence the whole line is one unit, because a span is what a reader copies. Line-scoping was the first draft's defect — 40 then 13 findings on a CLEAN corpus, every one a flag belonging to a DIFFERENT command later on the same line (`cargo test --locked` after `oc-prchecks`, `--gated-run` after `oc-deploy`); a second window is the tool→flag GAP, capped at 3 tokens so a flag five words into prose (`standalone oc-index-worktree stays the fallback only when --create is unavailable` — that `--create` is `oc-wt`'s) is never attributed. **CITATIONS:** a law that DOCUMENTS an absence ("there is no `--resume` flag") names a phantom ON PURPOSE, so a line carrying an absence marker downgrades the hit to a CITATION — counted, printable with `--show-citations`, EXIT-NEUTRAL; failing the incident table that records the family would make the lint unusable. The marker list is deliberately narrow and `Do not forget to run X` does NOT suppress (selftest-asserted). `--strict` narrows "known" to case-arm declarations (`^\s+--flag)`) to catch a flag documented in prose but never wired; the default zero-occurrence rule UNDER-reports rather than mis-reports, because a linter that cries wolf gets ignored. Corpus = the 11 normative markdown files; `CHANGELOG.md` and `war-stories.md` are EXCLUDED (they quote historical invocations that were correct when written). `OC_LINT_LAWS_TOOLS_DIR` is a TEST HOOK pointing the tool scan at a fixture `tools/` dir. `--selftest` = 15 offline fixture assertions, including the wide-gap regression guard. |
| oc-notify-fanout | 0 | 2 | **wave receipt attribution (#19, 2026-09-12):** the `notify-fanout` ledger row now passes an explicit `--by "${OC_ACTOR:-oc-notify-fanout}"`, because `oc-ledger stamp` refuses an anonymous row — a wave stamps under the caller's lane when known, else under the tool. 0 fanout-complete / 3 ledger-unavailable / 4 no-valid-targets / 6 dry-run-plan (informational) / 7 concurrent-refused (wave-id lock: holder pid+ts named in message; stale auto-cleared) · per-lane idempotency: `wave-<id>.sent` resume semantics — duplicates skip cleanly, never double-notify · forum-scope guard: targets must be bound to opencrabs-dev forum chat, override with `--forum-chat` / `OC_FANOUT_FORUM_CHAT` · **law 9 target set (2026-09-12, defect #6)**: rows whose `.state` == RETIRED are removed from the target set BEFORE per-target validation (12 legacy rows predate v1.3 retire-removal; `e756b84b` was FIRST in two HQ waves, burning the slowest slot on a lane that can never act); the skip is journaled per wave (`SKIP <uuid> INACTIVE(state=RETIRED)`), the row is never sent · **law 10 partial-delivery verdict (2026-09-12, defect #5)**: every send ATTEMPT is recorded to `run/wave-<id>.targets` BEFORE the send (dedup'd, dry-runs excluded); `--verify <wave-id>` diffs manifest vs receipts — rc 0 COMPLETE / rc 8 PARTIAL naming each unreached lane on stderr (+ resume hint) / rc 3 no manifest — so a wave killed mid-loop (SIGTERM/cgroup teardown) can never masquerade as complete · in-wave send failures keep exit 4 but now print `PARTIAL DELIVERY: N of M attempted send(s) failed` with the resume/audit path · per-send bound: `OC_FANOUT_SEND_TIMEOUT` (default 90s, `timeout` per send — a hung lane becomes a FAIL receipt rc=124 and the wave continues; measured 2026-09-12: 20/47 sends were >30s from a real outage, 75.6s max was a live delivery) · **law 15 cgroup detach (2026-09-12, defect #15)**: a wave inherits the cgroup of whoever launched it, so a launcher hosted by the unit a swap is about to bounce (`opencrabs-ops.service`) gets its wave SIGKILLed mid-flight at the swap's `systemctl --user restart` — survivors got their briefs, the rest never heard, NOTHING reported the truncation (HQ d72bd52d: `wave-2b2a99d25bd2` last wrote `.sent` at 09:38:18Z after exactly 5 lanes, no live process by 09:45, killed by the 09:39:02Z swap; prior waves died at 211s and 530s). The wave now re-execs under a transient scope OUTSIDE the unit (`systemd-run --user --scope --collect`) BEFORE the wave lock is taken, so a re-entry can never deadlock on a lock this invocation already holds (same pre-lock ordering as oc-deploy). Overrides: `OC_FANOUT_UNIT` (default `opencrabs-ops.service`) · `OC_FANOUT_IN_UNIT_CGROUP` (auto\|1\|0 test hook) · `OC_FANOUT_DETACH_CMD` (MUST block and pass the child's rc through — `setsid -w`, never bare `setsid`) · `OC_FANOUT_DETACHED` (loop guard). Detach is a no-op outside the cgroup; `--verify` is read-only and never detaches. · **law 5 quiet default (2026-09-12, HQ defect d72bd52d)**: `--mode now|turn-end|quiet` is passed straight through to the CLI and **DEFAULTS TO QUIET** — `hq.md:95` (owner law 2026-09-07) mandates `delivery=quiet` for ANY version bump, so a tool whose default was `now` could not deliver the law it exists to serve. In quiet mode there is NO interrupt-retry: a deferred verdict (rc 0 + notification id) IS the expected result, and re-issuing would convert a deferred send into the immediate wake the flag prevents — the retry fires only for the non-quiet modes. An invalid `--mode` dies rc 2 BEFORE any send. `--mode` is emitted BEFORE `--text` on purpose: the argv tail stays `... --text <brief> <uuid>`, the shape the selftest's `sent_brief()` parser keys on. |
| oc-order-validate | 0 | 1 | 0 VALID / 2 UNMERGED / 3 UNSIGNED-unknown / 4 UNKNOWN-REF |
| oc-ping-proof | 0 | 2 | 0 WOKEN / 1 SILENT / 3 UNREACHABLE / 4 parse-fail |
| oc-pr-atomicity | 0 | 1 | 0 ATOMIC / 2 NON-ATOMIC / 4 PR-not-found |
| oc-pr-fault-scope | 0 | 2 | 0 IN-SCOPE / 1 BASE-FAULT / 3 gh-fail |
| oc-prchecks | 0 | 2 | 0 GREEN / 3 RED / 4 dispatch-api-lock / 5 in-flight-timeout / 6 CANCELLED-superseded / 7 carrier-head-unresolvable / 8 AMBIGUOUS-same-ref-witness-unverifiable (fail-closed, dispatch refused, #115B) / 4 also = ADOPTION IDENTITY MISMATCH (adopted run is not workflow_dispatch on carrier — n=1721 B; headSha pin impossible by design, n=1730; mismatch rc-4 emits OC_PR_SUPERSEDED token = NON-retryable do-not-resume, C-A1 v0.4.104) · `wait <ref> [--budget N] [--poll S]` subcommand (v0.4.145, lens C-1): single-invocation blocking gate wait loop (default budget 2700s, poll 30s) eliminating ad-hoc sleep loops; rc 0 GREEN / rc 3 RED / rc 5 timeout / rc 2 usage · `--fast` passes `fast=true` to workflow dispatch (runs cargo fmt + clippy; skips cargo test) |
| oc-review-persist | 0 | 2 / 3 / 4 | 0 persisted (re-read verified) / 2 usage (bad lens/empty input) / 3 re-read sha256 mismatch (storage NOT trusted) / 4 write-fail (mkdir/write/index-append); check-cycle: 0 census complete / 2 usage / 3 catalog UNDERIVABLE (refuses to call a cycle clean off an unreadable catalog) / 5 lens(es) missing -- unreceipted (canonical name, no index line) and UNPERSISTED (report present under a non-canonical name) fail only under --strict-receipt |
| oc-roster | 0 | 2 | 0 ok / 3 unreadable source (session DB, ledger, or `oc-wt`) · DERIVED in-progress roster (2026-09-11, plan step 1 fork-rebase gate): joins ledger `events[kind=claim]` (INTENT) + `oc-wt list` dirty (EVIDENCE) + session-DB liveness (LIVENESS) + forum bindings (SCOPE) into one classified table. Verbs `live [--detail]` (liveness only — set-equal to the `oc-notify-fanout` v0.4.129 query), `forum [--detail]`, `claims`, `work`, `classify [--json]` (TSV uuid/role/signals/class; classes ACTIVE/IDLE/ORPHAN/UNKNOWN — UNKNOWN is ESCALATED to the owner, never guessed). A claim author the session DB has never seen is a PHANTOM: reported on stderr, EXCLUDED from every class (a hand-assembled frankenstein uuid can never enter a freeze list — n=2221 class). Stores nothing; `--selftest` runs 31 offline fixture checks |
| oc-roster-selftest | 0 | 1 | 0 PASS / 1 FAIL / 2 usage · hermetic fixture-based runner for oc-roster validation |
| oc-seal-state | 0 | 1 (noop) / 2 (unknown flag, F-L1) | 0 OK / 1 bare-invocation-noop / 2 unknown-flag-usage OR CONTRIBUTOR-SCAN-FAIL / 3 WRITE-FAIL-INVALID (usage surfaces = 2; 3 is always a write fault, lens A-6 v0.4.96) |
| oc-shadow-rotate | 0 | 2 | 0 ok-noop / 2 io-fail (usage merged into 2, C-#3) |
| oc-ship-audit | 0 | 2 | 0 all-SWAPPED / 1 ORPHANED |
| oc-log-search | 0 | 2 | 0 matches-found / 1 zero-hits |
| oc-skew-scan | 0 | 2 | 0 clean / 1 skew / 3 parse-fail |
| oc-smoke-evidence | 0 | 2 | 0 IDENTITY-MATCH / 1 MISMATCH / 3 unit-or-proc-fail · **`--append-log <path>` (2026-09-12, defect #13, lane c10cd97b)**: the SANCTIONED append path for state smoke-verdicts.log — appends the evidence block newline-safely (a log left unterminated by a previous writer is terminated first, rows can never glue); appends on rc 0/1 (verdict rows are evidence either way), writes nothing on rc 2/3; plain shell `>>` is what glued c10cd97b's rows — do not use it · **M2-2 (2026-09-12, toolsmith)**: the path ARGUMENT is now optional and a WRONG path is unrepresentable — a bare `--append-log` targets the canonical ABSOLUTE `$STATE_DIR/smoke-verdicts.log`, a RELATIVE argument resolves against `$STATE_DIR` (it used to land in the CALLER'S CWD — a decoy that looks right and holds none of the evidence; 2 rows lost), an absolute argument is honoured, and any target that is not the canonical log emits a loud `NOT the canonical` NOTE on stderr |
| oc-tg-audit | 0 | 2 | 0 clean / 1 violation / 3 log-missing |
| oc-upstream-delta | 0 | 2 | 0 clean / 1 delta (verdict) / 3 fetch-git-fail · `--json` outputs JSON object with base, ahead, behind, absorbed, and absorbed_candidates list |
| oc-watcher-audit | 0 | 2 | `[--since <ts>] [--json] [--dir <dir>]` · 0 clean / 1 violations-found (alarm) / 3 dir-missing · scans `tmp/detached/*.json` for `UNTHROTTLED_WATCH` (no `--interval`), `OFF_SPEC_INTERVAL` (interval other than 30/60 — the fleet law), `NOHUP_SPAWN`, `HANDROLLED_SLEEP_LOOP` · `--since` is a rolling window: tasks spawned earlier are skipped, timestamps are parsed as datetimes (fractional seconds and `+00:00` offsets included), and a task with an unparseable timestamp is INCLUDED (age unprovable — never silently dropped) · `--json` emits a JSON **ARRAY** of violation objects carrying `state` + `live`, never an object · `--selftest` = 20 offline fixture assertions |
| oc-waiter | 0 | 1 (retired) | RETIRED in v0.4.135: replaced by native detached bash execution (background: true); --help returns 0, subcommands return 1 |
| oc-wt | 0 | 2 | 0 ok / 3 path-exists-dirty / 4 index-failed / 5 repo-branch-missing / 6 behind-base |
| oc-rebase-safety | 0 | 2 | overlap: 0 zero-overlap (gate-skip permitted per re-gate split law n=2259) / 1 overlap-found (gate-required) / 3 git-fail. audit: 0 clean / 1 losses (DROPPED or CHANGED rows on stdout) / 3 git-fail. Read-only plumbing; exit 1 is a VERDICT (house rule, oc-upstream-delta class). **Output shape (#22, 2026-09-12 — two lanes misled in one hour):** the conservative path (no `--tree`, or a single-parent tip) cannot derive the branch-side set, so it treats the FULL main-shift set as overlapping — that list is therefore **NOT an intersection**, and it used to print bare under a heading that read as an overlap verdict. A real overlap of ZERO was read as a file collision by two lanes (6630dc9a flagged it; 462181e9 nearly filed a false collision). Now: the conservative path heads its list `MAIN-SHIFT (not an overlap — merge-changed set unknown for a single-parent HEAD)` and the verdict line says outright that the list above is NOT an intersection; the real-intersection path heads its list `OVERLAP (real intersection of main-shift and branch-side)` and prints ONLY the intersected files. The verdict logic is unchanged — only the label was the defect. Nothing parses this stdout (the one consumer, oc-deploy, calls `audit`). Selftest asserts both shapes and the cross-checks (real is not labelled MAIN-SHIFT, conservative is not labelled real). |
| oc-ship-chain | 0 | 7 | 0 SWAPPED (chain complete: gate→issue-log→ff-merge→ship→swap, one chain-id) / 3 precondition not met — pre-flight (dirty or missing fork checkout, unpushed remote branch, or LEG1 gate returning oc-prchecks rc 2 usage/sha-shape/unknown-ref; lane fix, not infra) / 4 GATE-RED or CARRIER-RED (structured verdict; lane fix) / 5 NON-FF (lane rebases via oc-rebase-safety then re-runs) / 5 TIP-MOVED (the branch tip no longer equals `--sha`: a rebase + force-push happened while the gate ran, so LEG3 must NOT land an ungated tip — re-gate and relaunch) / 6 dispatch/poll infra (prchecks rc 4/7/8) / 7 GATE STILL IN FLIGHT (prchecks rc 5 after gate budget + resume-polls; run id printed, re-run with `--gated-run <id>`). Runs `--fast` pre-merge gate by default (`fast=true`, ~2.2 min); `--full` or `--no-fast` opts into full ~30 min gate suite. **LEG1 gate rc 5 is non-terminal**: resume-polls (default 2, `OC_SHIPCHAIN_GATE_RESUMES`) before exiting rc 7. `--gate-wait <sec>` (default 2700s; `OC_SHIPCHAIN_GATE_WAIT`) sets LEG1 budget; `--wait` sets LEG4 ship/swap budget. Scope ends at SWAPPED (smoke and owner approval stay outside). No session notifies emitted. **Journaling**: stamps `kind=shipchain` under `CHAIN-<shortsha>-<UTCstamp>` per leg; journal write failure logs on stderr and is never fatal. **Pre-flight**: verifies `$BRANCH` on fork remote (`refs/heads/$BRANCH`) before dispatch; missing branch returns rc 3 precondition error. **Actor defaulting (v0.4.152 §Attribution & Goal Hygiene A1, 2026-09-12)**: an explicit `OC_ACTOR` wins; otherwise the chain derives it from the `Session-Id` trailer on the `--sha` commit and EXPORTS it, so its own journal rows and every child tool's rows are attributed instead of landing `(unattributed — pass --by or export OC_ACTOR)`. Nothing is invented — but since `oc-ledger stamp` REFUSES an anonymous row (#19, 2026-09-12) the chain's journal call passes an explicit `--by "${OC_ACTOR:-oc-ship-chain}"` fallback: the honest actor when no lane can be derived (precedent `oc-deploy` stamps `--by "oc-deploy recover-receipt"`), so a chain keeps its ledger trail instead of trading it for a loud journal failure. **LEG1 journal prints the resolved argv (2026-09-12, defect #9)**: the gate-start journal line is built from the invoked `PRCHECKS_CALL` array — the old line restated `${FAST_GATE:+ --fast}` from the flag variable, and since `FAST_GATE` is always "1"/"0" the journal claimed `--fast` even on `--full` runs. Selftest asserts argv truth in both modes (`--fast` present by default, absent on `--full`) + the sha named. **LEG1 docs-only gate skip (#20, owner ruling 2026-09-12 11:04Z — “We don’t need the pure docs commits to pass through ci on our side”)**: a FRESH dispatch whose EVERY changed path (`git diff --name-only <sha>^..<sha>`) is markdown, and none of which is compiled into the binary, skips LEG1 entirely — journal `GATE-SKIPPED-DOCS sha=<sha> reason=every-changed-path-is-markdown-and-none-is-compiled-into-the-binary paths=<csv>` plus the same text on stderr, so a skip can never be mistaken for a passed gate. The predicate is deliberately NOT an extension check: 21 distinct `.md` paths are `include_str!`/`include_bytes!`-compiled into the binary, so editing one changes compiled output and MUST still gate. That set is DERIVED from the source tree on every chain (`include_str!` resolves relative to the CONTAINING `.rs`, not the crate root, so literal+containing-file are resolved as a pair) and is never hardcoded — a rotted skip-list would let a RED binary through a skipped gate. `--gated-run` / `--gated-sha` are explicit “verify THIS run” requests and are honoured regardless of the sha’s contents; `OC_SHIPCHAIN_NO_DOCS_SKIP=1` forces the gate even for a docs-only sha. Selftest section 20 covers the skip (journaled, no gate dispatched, visible on stderr), the compiled-md negative case (README.md still gates), and the override. **Cgroup detach / restart survival (LAW 17, defect #21, 2026-09-12):** a chain launched from a session is a child of the daemon and therefore sits INSIDE the unit cgroup a swap's `systemctl --user restart` tears down — it is SIGTERM'd mid-leg and simply STOPS, with no ledger row and no failure report (two chains died this way at the 11:36:46Z swap: one `gate infra rc=143`, one correctly QUEUED on the ship lock and killed for waiting, so serialisation gave it a false sense of safety). The chain now re-execs outside that cgroup BEFORE `acquire_ship_lock` via a transient scope (`systemd-run --user --scope --collect`); pre-lock ordering is load-bearing because the chain holds its lock across LEG3/LEG4 and a queued chain can wait minutes — a post-lock re-entry would self-deadlock. The detach command MUST BLOCK and pass the child's rc through (`setsid -w`, never bare `setsid`) or a NON-FF refusal reads as success. **LAW 17 RESIDUAL — stdio is a SECOND teardown (2026-09-12, editor 462181e9 report, field-proven):** the cgroup is not the only thing the launching tool call owns. A chain started from a CAPTURED tool call inherits that call's stdout/stderr PIPES, so the re-exec'd scope survived the cgroup teardown and then died EPIPE on its FIRST stderr write — `CHAIN-6f62b2c1-20260912T115518Z` ended 26 minutes in with a 0-byte log, no LEG3 row and no `rc` field, its first stderr write being `acquire_ship_lock`'s `printf ... >&2`; the scope's journal showed no kill line, so it simply STOPPED. The re-exec now binds stdio to a FILE (`</dev/null >>"${OC_SHIPCHAIN_LOG:-/tmp/oc-ship-chain-detached-$$.log}" 2>&1`) because a caller-side redirect only covers callers who remember; the selftest asserts the binding via a fixture marker that can reach a file only when the redirect exists. **Hermetic selftest (same change):** the detach hook defaulted to `auto`, which reads the HOST's /proc/self/cgroup, so seven assertions passed or failed depending on WHERE the selftest ran — inside the unit cgroup the detach fired in tests that have nothing to do with detaching, and they passed only because the re-exec inherited the captured pipe. The stdio redirect removed that accident and they went red. The selftest now exports `OC_SHIPCHAIN_SELFTEST=1`, under which `auto` resolves to 0 (outside the unit); detach tests set `OC_SHIPCHAIN_IN_UNIT_CGROUP=1` explicitly. Selftest 88/0. **LEG3 TIP PIN (defect fix 2026-09-12, lane c6b1a539):** LEG3 merged `$BRANCH` BY NAME with no `tip == $SHA` assertion, and a lane rebase + force-push mid-gate is a NORMAL operation here — so LEG3 could fast-forward fork main onto a tip NO GATE VALIDATED while LEG4 shipped the ORIGINAL `$SHA`, leaving main and the shipped artifact divergent while the ledger still read `DONE SWAPPED sha=$SHA`. Near-miss: chain 3811585 pinned c5fc4203, the lane force-pushed 234c0d51 mid-gate, and FORK_REPO already carried a local branch at 234c0d51. LEG3 now resolves the tip ONCE, refuses rc 5 with a TIP-MOVED journal line, and merges `$TIP` — there is no second resolution for the ref to move under. **LEG5 CONFIRMS THE MARKER, NOT A STDOUT TOKEN (defect fix 2026-09-12, editor 462181e9, receipted live):** LEG5 scanned the poll's stdout for `*SWAPPED*` — a token `oc-deploy` has NEVER emitted (a case-sensitive sweep of the whole tool returns ZERO; the uppercase hits are variable names like SWAP_TARGET). `poll --execute` hands off to a transient Phase B unit and exits 0 PRE-bounce BY DESIGN, so its stdout cannot carry a completion marker: EVERY successful chain exited rc 6 'swap not confirmed' on a box that was already swapped, and the natural next move for a lane that trusts the exit code is to RE-RUN the chain — a double-swap on an already-swapped host. The selftest could not see it because its `fake-deploy` stub DID print the token: the fixture was more permissive than the real tool. Both stubs are now contract-faithful (ship records what it shipped, poll hands off with NO completion token, status reports the marker) and LEG5 reads `deployed.sha` through `oc-deploy status --json`. **Harness guards (same change):** `set -e` plus a bare `OUT="$(...)"` aborted the ENTIRE selftest on the first failing chain (rc 6, zero output, no assertion printed) — a harness that cannot report a failure cannot be trusted green; the two unguarded assignments are now `RC=0; ... || RC=$?`. Negative control: restoring the dead token predicate turns 26 assertions RED, every one of them a chain that expected rc 0 and got rc 6. Overrides: `OC_SHIPCHAIN_UNIT` (default `opencrabs-ops.service`) · `OC_SHIPCHAIN_IN_UNIT_CGROUP` (auto|1|0 — test hook) · `OC_SHIPCHAIN_DETACH_CMD` · `OC_SHIPCHAIN_DETACHED` (loop guard). The journal-failure diagnostic now BRANCHES on the cause: rc 143/137 names the restart/cgroup teardown and says re-run the chain, while a genuine kind rejection keeps the KINDS hint — the old message printed the KINDS hypothesis for ANY non-zero rc, sending the operator to repair a component that was not broken. Selftest section 21 covers detach-fires-inside, loop-guard, no-detach-outside, survival of a process-group kill, and the in-selftest negative control (detach off → TRUNCATED). |

## Remote topology — `mirror2` and `origin` are ONE repository (verified 2026-09-12)

The "push `mirror2` first, then `origin`" ritual buys **no redundancy**. In the skill
repo both remotes point at the SAME GitHub repository; only the transport differs:

| Remote | URL | Transport |
|---|---|---|
| `mirror2` | `git@github.com:leshchenko1979/opencrabs-skill.git` | SSH |
| `origin` | `https://github.com/leshchenko1979/opencrabs-skill.git` | HTTPS |

Evidence (same-turn receipts, 2026-09-12): `git remote -v` shows the two URLs above;
after `git push mirror2 main` succeeded, `git push origin main` reported
**"Everything up-to-date"**; and `git ls-remote mirror2 HEAD` / `git ls-remote origin HEAD`
return the same sha. A push to either updates both.

**Consequence:** do NOT count the two remotes as a second copy, a backup, or an
independent mirror when reasoning about durability or about "did the push land twice".
The push order still stands as process (it is the fleet ritual), but the honest reading
is ONE remote reached two ways. A genuine second copy needs a DIFFERENT host or
repository. Reported by HQ `d72bd52d` 2026-09-12 and verified first-hand here before
this note was written (a finding is not a fact until the surface is read).

## Unified tools log (moved from SKILL.md v0.4.131)

Every tool in `tools/` sources `tools/lib/oc-log.sh` and appends ONE JSONL line
on exit — the fleet-analysis aggregate (per-tool journals remain the per-run
record).

- **Path:** `/root/.opencrabs/profiles/ops/opencrabs-dev/tools.log` (override with `OC_TOOLS_LOG`).
- **Schema:** `{"ts":"…Z","tool":"oc-…","args":"…","exit":N,"secs":N.N,"extra":{}}` — tools add fields via `oc_log_extra key value`.
- **Suppression:** the invocation IS a selftest — the `--selftest` FLAG **or the bare `selftest` SUBCOMMAND** (one shared `oc_is_selftest()` predicate; first-token match for the bare form, so a VALUE that merely reads `selftest`, e.g. `--dir selftest`, never silences a real run) — or `OC_TOOLS_NOLOG=1` (the battery exports it — synthetic runs never pollute the log). Missing `jq` → no write; logging NEVER changes the host tool's exit code. **M2-21 (2026-09-12, toolsmith):** the predicate used to match the flag token ONLY, but `oc-deploy:2976` dispatches `--selftest|selftest) selftest "$@" ;;` as ONE case arm — so a bare-subcommand selftest never exported `OC_TOOLS_NOLOG` and every fixture child it spawned was logged to the PRODUCTION log: **93 rows per run**, six phantom shas across two weeks (08-31 → 09-12) that `oc-ship-audit` read back as ORPHANED dispatches (rc 1, gating `oc-ledger commit-pending`). `oc-deploy`'s `selftest()` now also exports `OC_TOOLS_NOLOG=1` as its first statement so hermeticity does not depend on argv parsing; the predicate is the load-bearing layer (0 rows alone — the export alone still leaks the parent's own row).

Recipes (verified live):

```bash
# NOTE (2026-09-11, toolsmith): tools.log is JSONL, but it carries 2 NON-JSON
# lines (383, 443) — prose appended by hand on 2026-08-31; 443 is an
# evidentiary actor-correction, so it is NOT deleted. A plain `jq -r` over the
# whole file ABORTS there ("Invalid numeric literal at line 383"), which means
# two of the recipes below were labelled "verified live" while they could not
# run at all. They now read raw and skip unparseable lines (`-Rr 'fromjson?'`);
# malformed lines are dropped SILENTLY, so if a count looks short, that is why.
# failing invocations (note: rc≠0 is often a VERDICT, not a crash —
# oc-skew-scan 1 = skew found, oc-ping-proof 1 = SILENT; filter .tool first)
jq -Rr 'fromjson? | select(.exit!=0) | [.ts,.tool,.exit,.args] | @tsv' tools.log
# usage per tool
jq -Rr 'fromjson? | .tool' tools.log | sort | uniq -c | sort -rn
# newest line
tail -1 tools.log | jq -c .
```