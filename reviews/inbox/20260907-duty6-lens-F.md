# Duty-6 Lens F (TOOL CODE) — 2026-09-07, reviewer 0d83b563 (read-only, all quotes re-verified verbatim)
HEADLINE: n=1878 guard VERIFIED PRESENT in oc-deploy source — ship :1703-1708 + swap-execute :1828-1830 (cat-file -e + rc1 phantom refusal), selftest 4a uses real fabricated sha 4c0115ba; suspected stale-$out selftest bug RETRACTED (hashline artifact).
PRIORITY FINDING: guard had no green battery behind it at review time (battery-last.json stale 7h). HQ disposition: battery re-run 13:13:50Z PASS 144/0 — intermediate 143/1 FAIL was HQ artifact (inline 120s runs truncated battery mid-section). RESOLVED.
HIGH/MEDIUM findings (12+): oc-deploy ship w/o --sha silently adopts CWD HEAD, no fork-main containment check, no selftest (fix: require --sha or containment-check); swap journal name lacks $$ suffix (swap-$1-$(date+%s).jsonl — same-second collision truncates first run's crash journal; fanout has $$); poll validates --wait AFTER real RED-scan fanout side effects (:1749 vs :1766 — move check up); rc-2 collision: --notify-session empty → rc2 usage err colliding with documented rc2=rebase-gate verdict; fanout/contributors sub-registers absent from RC-CONTRACT; Session-Id extraction FIRST-vs-LAST divergence oc-order-validate:95 (head -1) vs oc-attrib:131/:115 (tail -1) — quoted trailer validates differently than attributes (fix: git interpret-trailers helper); 12-hex prefix tolerance on HARD identity gates oc-job-verify + oc-artifact-verify (truncation-collision path; fix: exact equality); oc-waiter first-poll GREEN unseeded = RUN_ID empty → NOTIFY-FAILED exit4, lane never woken (selftest never exercises first-poll-GREEN; fix: parse RUN line from rc-0 too) [HIGH]; oc-waiter JSON injection via --label/--ref raw printf (fix: jq -nc --arg); oc-prchecks C-F2 witness matcher substring collision (ref main len4>3 matches feature/fix-main → false witness skips adopting_pin; fix: extra.sha/ref equality); oc-seal-state unknown-flag rc2 collides w/ documented rc2 + RC-CONTRACT says usage=1 (contract divergence); oc-toolaccum single rogue log line → die 3 whole tool (fix: pre-filter like oc-ship-audit); oc-branch-sweep --keep fail-open (invalid regex → branch treated unprotected → branch -D; fix: validate regex up front); oc-harvest-sweep selftest farce (tests inline extraction no longer used, delegates to oc-attrib).
LOW: oc-order-validate --tip unvalidated+undocumented, RC-CONTRACT row missing exit 4; oc-review-persist row missing 3/4; oc-ledger dead cmd_check_version SKILL pair, ack ignores unknown flags; oc-harvest-sweep two-dot endpoint diff; misc per-file LOWs (temp leak oc-issue-sweep, case-sensitive Closes|Fixes oc-pr-atomicity, etc).
CLEAN: oc-ledger (claim-ref bare-digits cross-check resolved, lock+n continuity 10-way race test), oc-attrib (aside F7), oc-ship-audit, oc-shadow-rotate, oc-upstream-delta, oc-smoke-evidence SIGPIPE fix, oc-prchecks register complete + best-tested.
TOP-3: battery re-run (RESOLVED green) · oc-waiter first-poll-GREEN hole · Session-Id interpret-trailers unification.
