# Skill review — RUN 2 — Lens A (redundancy + ontology)
Agent ecd4557d · post-v0.4.42 · delivered 2026-08-29 · persisted by supervisor d72bd52d

1. SKILL.md §frontmatter provenance (v0.4.42 entry) · quote: "RULING (event 1323): option (b) — the HOSTING session (the turn that observes the tool's stamp on disk) owns the commit. Hosting session bundles the tool stamp WITH its own adjacent stamp in ONE commit IF both are pending at commit time (commit message names ALL stamps); no mid-run commits, no per-stamp hunting." · dimension: redundancy · problem: The new v0.4.41/42 git-commit discipline is now stated twice: this provenance entry re-encodes the full operative ruling already canonical in supervisor.md Duty 3 ("are committed by the HOSTING session — the turn that observes the stamp on disk — bundling the tool stamp with its own adjacent stamp in one commit if both are pending (message names all stamps)"), with different wording and NO pointer to Duty 3; the v0.4.41 entry similarly restates the discipline ("one commit per bump … one commit per ledger stamp inside the same flock as the write") though it at least points to "supervisor.md bump section". editor.md and tools/ correctly carry no copy. Two operative copies = drift hazard if the ruling is amended again. · fix: condense both provenance entries to changelog + pointer ("discipline + BEND canonical in supervisor.md Duty 3"); keep the operative text only there. · severity: minor

2. editor.md §scope · quote: "The Editor NEVER dispatches BUILD runs (`quick-build-linux.yml`), NEVER watches build runs, and NEVER touches binaries — all automation territory via `oc-deploy` (NO dispatch exceptions: BUILD TRIGGERS = exactly TWO with no exceptions — SKILL.md §Hard rules, A3 ruling 2026-08-29; the PR-head gate is the step-2c pr-checks dispatch, a lint/test gate, not a build trigger)." · dimension: redundancy · problem: The A3 BUILD-TRIGGERS ruling is stated FOUR times across two files: here, SKILL.md §STEP ZERO ("BUILD TRIGGERS = exactly TWO with NO exceptions (§Hard rules, A3 ruling 2026-08-29); the Phase-7 PR-head gate is the step-2c pr-checks dispatch"), SKILL.md §Hard rules (canonical), and editor.md §Phase 7 step 2b ("BUILD TRIGGERS = exactly TWO, no exceptions"). Four copies of one owner ruling, four slightly different wordings — any future amendment must touch all four. · fix: keep the ruling ONLY in SKILL.md §Hard rules; replace the other three sites with one-line pointers; keep step 2b's RETIRED notice but delete the rule restatement. · severity: major

3. editor.md §Phase 1 step 3 · quote: "**NO CLAIMING ON THE FORK** (owner directive 2026-08-27 17:07Z): no \"Tackling this\" comments, no self-assignment, no labels/reactions on fork issues — the owner's notification surface stays clean." · dimension: redundancy · problem: Restates SKILL.md §Hard rules ISSUE ROUTING row ("Fork issues NEVER claimed on GitHub: no tackling comments, assignment, labels/reactions by any lane — claiming = `Issue-Ref` trailer + workers-ledger row; uniqueness sweeps stay read-only search"); step 2 likewise restates the NEW-issues-on-fork row ("NEVER file a new issue on `adolfousier/opencrabs`"). Same owner directives carried twice with diverging detail (the ledger-half of the rule is missing here). · fix: shrink Phase 1 steps 2–3 to procedure + pointer ("per SKILL.md §Hard rules ISSUE ROUTING"). · severity: minor

4. editor.md §Phase 7 Rules · quote: "The PR body MUST reference THE issue as a FULL FORK URL at the END of the description (`Original issue: https://github.com/leshchenko1979/opencrabs/issues/N` — EXACTLY one, atomicity rule). `Closes #N` is FORBIDDEN on upstream PR bodies" · dimension: redundancy · problem: The PR-body rule is stated three times: SKILL.md ISSUE ROUTING row ("body = detailed description ending `Original issue: <full fork URL>`; NEVER `Closes #N`"), this Rules bullet, and the step-4 `gh pr create` comment — all three carry the same content. · fix: keep the instruction once (step-4 comment, where the command lives); the Rules bullet and SKILL.md row become pointers. · severity: minor

5. editor.md §Phase 7 step 0 · quote: "Proceed ONLY on Alexey's approval — his reply or a positive reaction to the request counts; silence does NOT." · dimension: redundancy · problem: Restates SKILL.md ISSUE ROUTING row ("APPROVAL = Alexey's reply or a positive Telegram reaction to the explicit request in the forum topic; silence is NOT consent"); the same gate is also re-asserted in editor.md scope ("smoke evidence owner-approved — Phase 7 gate") and Phase 7 trigger. · fix: keep the approval definition ONLY in SKILL.md §Hard rules; Phase 7 step 0 keeps the procedure (post evidence, wait) + pointer. · severity: minor

6. editor.md §Telegram surface law · quote: "Reads: `tg_get_messages` in your own topic only; no `tg_search_global`, no cross-chat/list probing. Reactions allowed (owner consent signal)." · dimension: redundancy · problem: Near-verbatim restatement of canonical SKILL.md §Telegram surface law ("`tg_get_messages` limited to the own topic … no `tg_search_global` / cross-chat reads … Reactions are allowed (owner consent signal)"); the section's first bullet likewise restates the never-invoke-send/edit rule while itself pointing at the canonical section. v0.4.35 deduped this law once; these remnants re-accumulate it. · fix: cut the editor section to editor-DELTA duties only (IDEA box push, session_notify targeting); drop the restated send-ban/read-limit/reaction lines. · severity: minor

7. compiler.md §ORDER intake step 1 · quote: "The tool checks merge-base containment vs `origin/main` + Session-Id trailer scan; exit 0 = LGTM with evidence JSON, 2 = UNMERGED, 4 = UNSIGNED." · dimension: redundancy · problem: Duplicated exit-code register that has DRIFTED from both the tool source and the canonical SKILL.md register (corrected v0.4.40): actual codes are 0 VALID / 1 invocation / 2 UNMERGED / 3 UNSIGNED / 4 UNKNOWN-REF. compiler.md maps UNSIGNED to 4 and omits 1/3 — wrong even for a re-enable. · fix: replace the inline codes with the register pointer ("exit codes per SKILL.md §Canonical tooling") or correct to 0/1/2/3/4. · severity: major

8. compiler.md §Step 5 item 4 · quote: "Writes/rewrites `baseline.json` + appends `orders.json` (if the ledger is non-empty) in ONE atomic call, exit-coded (0 = sealed, 4 = invariant broke mid-write, 5 = missing input)." · dimension: redundancy · problem: Duplicated oc-seal-state exit-code register, fully stale: the tool (and SKILL.md register) define 0 ok / 1 invocation / 2 scan-fail / 3 write-fail — codes 4 and 5 do not exist and 2/3 are misdescribed. · fix: delete the inline code list; point at SKILL.md §Canonical tooling. · severity: major

9. tools/oc-deploy §header comment (Ledger resolution) · quote: "The old $STATE_DIR default kept a second ledger in the skill dir — live swap stamps + fanout reads used it while supervisor stamps went to the canonical file: two-file drift, baseline/orders backup paths pointing at an empty dir, cycle counter reset 25->1 (incident 2026-08-29)." · dimension: duplicated-war-story · problem: The 2026-08-29 two-file ledger drift incident is narrated in THREE places with near-identical clauses: this tool header, SKILL.md provenance v0.4.38 ("two-file drift, baseline/orders backup paths at an empty dir, cycle counter reset 25->1"), and the canonical home SKILL.md §Shared war stories ("Two-file ledger drift" row). · fix: tool header keeps the one-line WHY + pointer ("incident 2026-08-29 — SKILL.md §Shared war stories"); provenance keeps its changelog summary; delete the duplicated detail clauses. · severity: minor

10. editor.md §Phase 6 GATE comment · quote: "# GATE (decision 2026-08-27, rewired 2026-08-28 per owner \"1 ok\"): the # all-tests evidence is the GREEN `pr-checks.yml` CI run on the branch — it # runs `cargo test --locked --all-features` upstream of any build." · dimension: ontology-violation · problem: "all-tests evidence" invents a test-kind label outside the four canonical ontology terms (SMOKE TEST / CODE TESTS / FEATURE-PRESENCE CHECK / EXECUTION SANITY SIGNAL); the pr-checks fmt+clippy+test run IS the CODE TESTS kind, and the ontology rule says never name a kind by any other coinage. · fix: reword to "the CODE TESTS evidence is the GREEN `pr-checks.yml` CI run". · severity: minor

11. supervisor.md §Duty 7 item 3 · quote: "KERNEL-SEMANTIC → batched to the owner with a verdict table; ships ONLY on his word (diagram gate applies)." · dimension: undefined-coinage · problem: "diagram gate" is defined nowhere in the skill package (single occurrence on disk; the ledger only ever names it as "design-approval diagram gate" inside one event body, also undefined). A supervisor executing Duty 7 cannot tell what this gate is or how to check it. · fix: either define the gate inline (what artifact/word constitutes it) or delete the parenthetical — "ships ONLY on his word" is already the complete gate. · severity: minor

12. SKILL.md §Telegram surface law · quote: "Violation pattern for the supervisor: a TOOL_ACCUM row showing an editor lane calling a telegram send/edit tool → session_notify the rule; second offense → review toggled." · dimension: undefined-coinage · problem: "TOOL_ACCUM row" is never defined in the skill package and is not a ledger row at all — it is a DEBUG log marker of the opencrabs binary (src/brain/provider/custom_openai_compatible.rs `[TOOL_ACCUM]` lines); workers-ledger.json contains zero TOOL_ACCUM entries. The supervisor is told to enforce off a record type with no stated location, shape, or grep recipe (the `grep -v TOOL_ACCUM` self-contamination lesson lives only in ops MEMORY.md, outside this surface). "review toggled" is likewise undefined. · fix: define the evidence source (ops profile log path + grep pattern) or strike the enforcement clause. · severity: major

13. SKILL.md §Hard rules (BUILD TRIGGERS bullet) · quote: "the Phase-7 step-2b quick-build PR-head dispatch is RETIRED (superseded by the step-2c pr-checks gate; carrier Gate 3 containment rejects any sha outside fork `main` anyway)." · dimension: undefined-coinage · problem: "carrier Gate 3" is jargon with no definition anywhere in the package: no carrier-gate enumeration exists; the containment check is oc-order-validate's gate 3 of the four ORDER gates (tool header: "3. CONTAINMENT — sha is an ancestor of the fork main tip"), an ORDER gate in the ship path, not a carrier-yml gate — the carrier builds whatever ref it is passed. · fix: reword to "ORDER gate 3 (CONTAINMENT, oc-order-validate) rejects any sha outside fork main". · severity: minor

14. editor.md §Phase 7 step 2b · quote: "# 2b. RETIRED (A3 owner ruling 2026-08-29): the direct quick-build PR-head #     dispatch is GONE — BUILD TRIGGERS = exactly TWO, no exceptions; carrier #     Gate 3 (containment in fork main) rejects any PR-head sha." · dimension: undefined-coinage · problem: Same undefined "carrier Gate 3" coinage as SKILL.md §Hard rules — containment is an oc-order-validate ORDER gate, not a carrier gate, and no gate numbering is defined in any skill file. · fix: reword to "ORDER gate 3 (CONTAINMENT) rejects any PR-head sha". · severity: minor

15. compiler.md §Step 2 ROLE_EXCEPTION founding-case note · quote: "*(Founding case: governor.rs E0308 compiled by dark author 61161247, 2026-08-26 07:20 UTC — full story in SKILL.md §Shared war stories.)*" · dimension: undefined-coinage · problem: "dark author" is invented jargon never defined anywhere; the canonical war story (SKILL.md) says the author "slept through the owner's night window" — "dark" is an unexplained synonym. · fix: replace with the canonical wording ("unreachable author — asleep through the owner's night window"). · severity: minor

16. compiler.md §scope · quote: "TWO sanctioned touches, both bounded in their own steps: the Step 6 integration sweep and Step 7 rebase-port fixups (SKILL.md §Role router)." · dimension: stale-ref · problem: SKILL.md has NO section named "Role router" (grep confirms zero matches); "role router" appears only as prose in SKILL.md's intro line. The sanctioned-touches text actually lives under "Roles DO NOT intersect" in §STEP ZERO — establish the role. · fix: re-anchor to "(SKILL.md §STEP ZERO, Roles DO NOT intersect)". · severity: minor

17. s2-swap-journal-spec.md §Journal location · quote: "`STATE_DIR/oc-deploy/journal/swap-<short-sha>.jsonl` — one file per swap attempt, append-only, every line fsync'd before the next step starts." · dimension: stale-ref · problem: The spec's filename pattern does not match the implementation or disk: oc-deploy writes `JF="$JOURNAL_DIR/swap-$1-$(date +%s).jsonl"` and every journal on disk carries the epoch suffix (e.g. `swap-06fe2e0a-1787941895.jsonl`). The spec (cited by oc-deploy as its wiring authority) is stale. · fix: update the pattern to `swap-<short-sha>-<epoch>.jsonl`. · severity: minor

18. s2-swap-journal-spec.md §Journal location · quote: "STATE_DIR = the state dir oc-deploy already owns for `deployed.sha`." · dimension: undefined-coinage · problem: Terminology collision hardened by v0.4.41: oc-deploy's STATE_DIR defaults to the SKILL dir (`STATE_DIR="${OC_DEPLOY_STATE_DIR:-/root/.opencrabs/profiles/ops/skills/opencrabs-dev}"` — where deployed.sha/journal actually live), but since v0.4.41 "state dir" is a reserved term for the canonical state dir `/root/.opencrabs/profiles/ops/opencrabs-dev` (a SEPARATE git repo). SKILL.md §Session-notify loop also uses `STATE_DIR/...` without ever defining it, so a reader can land in the wrong directory/repo. · fix: rename the prose variable (e.g. "SKILL_DIR / OC_DEPLOY_STATE_DIR, default the skill dir") in both files, or add a one-line definition at first use in SKILL.md. · severity: minor

Archived-role live-use scan: No findings — every Compiler mention on the surface is RETIRED/archived/pre-S3 annotated; nothing treats compiler.md as a live role.

Tally: 18 findings — severity: major 4 (2, 7, 8, 12), minor 14. (Tally assembled by supervisor from per-finding severities; agent delivery carried no summary line.)
