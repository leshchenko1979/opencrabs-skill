# Duty-6 review run 2 — Lens C (CLI automation) — 2026-08-29

Skill: opencrabs-dev v0.4.42 · Reviewer: sub-agent 45bc3e4b (read-only) · Persisted by supervisor session d72bd52d (topic 30220)
Lens: recurring multi-step manual rituals deterministic enough for one CLI command.
Special-attention brief: is the v0.4.41/42 stamp+double-commit bump ritual still manual, and what is the delta vs the pending oc-ledger design (/root/oc-work/oc-ledger-design-20260829.md)?

## Findings (7 total: 1 critical, 2 major, 4 minor)

1. supervisor.md §Duty 3 — Push updates to idle workers · quote: "stamp ONE ledger event (version published, no per-worker notify rows) AND commit BOTH git repos — skill-dir repo: one commit per bump; state-dir repo: one commit per ledger stamp, inside the same flock as the write (git-history regime, v0.4.41)" · dimension: manual-ritual · problem: the NEW v0.4.41/42 version-bump ritual is still pure prose: SKILL.md frontmatter hand-edit (Duty 1: "bump the version in `SKILL.md` frontmatter") + battery run (SKILL.md §Canonical tooling: "Must stay green before any version bump") + hand-jq ledger stamp under flock + git commit in the skill repo + git commit in the state repo + tag — 5+ hand-executed steps per bump, with the v0.4.42 BEND ("are committed by the HOSTING session — the turn that observes the stamp on disk — bundling the tool stamp with its own adjacent stamp in one commit if both are pending") also resolved by eyeballing git status. Field-drift in this exact ritual already burned once (0.4.37 vs 0.4.39). · fix: `oc-ledger sync --version <v> --why "<provenance>"` owning end-to-end: battery-receipt gate → flock(3 version fields + skill-bump event, tmp→mv, jq -e validate) → state-repo `git add workers-ledger.json && git commit` inside the same flock → skill-repo commit + tag `v<v>`. OVERLAP: this is exactly the oc-ledger design (/root/oc-work/oc-ledger-design-20260829.md, `sync` subcommand + Addendum §2–3 git legs) — no code exists. DELTA: (a) zero implementation; (b) the design's battery gate reads a `battery-last.json` receipt that tools/tests/run.sh does not write (verified: no such file, no writer in run.sh) — the receipt write is a prerequisite; (c) the design has no mechanic for the v0.4.42 hosting-session bundle of TOOL-written stamps — add `oc-ledger commit-pending [--bundle]` (commits all pending state-repo stamps, message names every n) as the interim verb until stamp self-commit ships and supersedes the bend · severity: critical

2. supervisor.md §Duty 7 — Idea box · quote: "INBOX = the ledger: on receipt the Supervisor stamps an `idea` event into `workers-ledger.json` (sender session, ts, text) — durable, jq-filterable, cannot die in a session log." · dimension: manual-ritual · problem: every ruling (Duty 5: "each logged as an event entry in `workers-ledger.json` (`rulings`)"), idea arrival, and idea-verdict is a hand-jq append to the same live file the 2026-08-29 deletion incident ruled tool-write-only — flock + tmp→replace + n-computation re-derived by hand on every stamp, plus the state-repo git commit per stamp since v0.4.41. · fix: `oc-ledger stamp <kind> "<what>" [--by <label>]` (kind vocabulary: ruling/idea/idea-verdict/incident/note; n = max+1 inside lock; git-commit leg inside the same flock). OVERLAP: identical to the design doc's `stamp` subcommand, no code; DELTA: none beyond "not implemented" — the design's stamp git leg also supersedes the Duty-3 hosting-session bend for tool stamps once live · severity: major

3. supervisor.md §Duty 2 — Worker registry · quote: "Auto-discovery (decision 5): on every roster sweep, an unknown active session becomes a provisional registry row, confirmed by its first signed commit (Session-Id trailer = identity proof)." · dimension: manual-ritual · problem: creating a workers[] row (uuid/role/topic/feature/confirmed/last_notified/last_acked) and flipping provisional→confirmed is hand-jq row surgery on the live ledger, repeated on every roster sweep that surfaces a new session and at every editor claim (editor.md Phase 1: "your feature row in `workers-ledger.json` (first ledger timestamp wins...)"). · fix: `oc-ledger enroll <uuid> --role <r> --topic <id> --feature <slug> [--provisional]` (full-uuid shape check, duplicate-uuid refusal, atomic write under flock + state-repo commit; `--confirm <uuid>` flips the flag on first signed commit). OVERLAP: partial — the design doc covers ack updates but explicitly NOT row creation ("ack is never a row creator"); DELTA: enroll/confirm subcommands are missing from the design and must be added · severity: minor

4. supervisor.md §Duty 2 — Worker registry · quote: "**Ack contract (decision 3):** a worker acks with a one-line `ACK <version>` at its next turn boundary after notification." · dimension: manual-ritual · problem: turning each arriving ACK into a `last_acked = {version, at}` row update is hand-jq on the live ledger, once per worker per published version — the write that oc-skew-scan and oc-ping-proof both read back. · fix: `oc-ledger ack <uuid> <version> [--at <ts>]` (uuid/version shape validation, unknown-uuid refusal, atomic write + ack event + state-repo commit). OVERLAP: fully covered by the design doc's `ack` subcommand; DELTA: none beyond "not implemented" · severity: minor

5. editor.md §Phase 5 — lint gate before every commit · quote: "Iterate on code locally, push the branch, re-dispatch pr-checks, read the run log — that loop replaces every local lint run." · dimension: manual-ritual · problem: every editor fix-round re-runs a 4-step flag-sensitive ritual by hand: `gh workflow run pr-checks.yml --repo leshchenko1979/opencrabs --ref ci/quick-build-linux -f ref=<branch>` (the yml lives ONLY on the carrier branch — wrong ref = silent no-dispatch) → hunt the run id via `gh run list` (workflow_run returns no id) → watch to terminal → open the run and read the fmt STEP outcome separately because "soft-fail hides failures from the run" (Phase 7 rules). Recurs on every push of every editor lane. · fix: `oc-prchecks <branch-or-sha> [--wait N]` — dispatches pr-checks.yml on the carrier ref with `ref=<arg>`, resolves the triggered run id, polls to terminal, prints run URL + per-step conclusions with the fmt step explicit; exit 0 GREEN / 2 usage / 3 RED / 4 dispatch-or-api-fail / 5 still-in-flight (no --wait). Not covered by any existing tool or by the oc-ledger design · severity: major

6. SKILL.md §Upstream relations · quote: "**Watch = supervisor duty (`oc-deploy` watch), every build cycle** (archived anchor: Step 1 pre-flight): fetch `adolfousier`, compare upstream tip vs fork merge-base." · dimension: manual-ritual · problem: the upstream-watch computation (fetch adolfousier → merge-base → rev-list count → which fork-only commits are patch-id absorbed candidates) is re-derived by hand each watch cycle, and `oc-deploy watch` only covers the stray-commit tripwire on fork main, not the upstream delta; the archived compiler.md Step 7 Watch block shows the same three-command sequence. The PROPOSE/WAIT judgment stays human — only the delta arithmetic should move. · fix: `oc-upstream-delta [--repo ~/opencrabs]` — fetches both remotes, prints ahead/behind counts vs merge-base, lists delta commits, and flags fork-only commits with a patch-id twin upstream as ABSORBED-candidates (TSV); exit 0 clean / 1 delta-present / 3 fetch-or-git-fail. Not covered by any existing tool or the design doc · severity: minor

7. editor.md §Phase 2 — Worktree per task · quote: "git -C ~/opencrabs worktree add ~/oc-wt-<task> <branch>" · dimension: manual-ritual · problem: every task runs the same two-command creation pair — worktree add, then `oc-index-worktree` on the new path ("then index the tree so codegraph works inside it — worktrees inherit NO per-tree index"), plus prune-before-add hygiene — and the same two-command teardown ("git -C ~/oc-wt-<task> status --porcelain   # must be empty — all committed & pushed" then worktree remove), re-run on every fix-round RECREATE. Skipping the index step silently breaks codegraph in the new tree. · fix: `oc-wt add <task> <branch> [--base origin/main]` / `oc-wt remove <task>` — add: worktree prune → add from freshly-fetched origin/main → chain oc-index-worktree; remove: refuse on non-empty porcelain → remove → prune; exit 0 ok / 2 usage / 3 dirty-tree / 4 index-failed. Not covered by any existing tool (oc-index-worktree only does the indexing leg) · severity: minor

## Tally

Seven findings (1 critical, 2 major, 4 minor). Finding 1 is the special-attention answer: the v0.4.41/42 bump ritual (ledger stamp under flock + double git commit + tag) remains manual prose in supervisor.md §Duty 3, and the single command that should own it end-to-end is `oc-ledger sync --version <v> --why "<provenance>"`, which overlaps the existing design at /root/oc-work/oc-ledger-design-20260829.md with three named deltas: no code exists, the battery-receipt prerequisite (`battery-last.json` in tools/tests/run.sh) is absent from disk, and the v0.4.42 hosting-session commit-bundle bend has no mechanic in the design (proposed: `oc-ledger commit-pending [--bundle]` as the interim verb).
