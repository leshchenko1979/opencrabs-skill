REVIEWER A — REDUNDANCY + ONTOLOGY · opencrabs-dev v0.4.39**

---

**1.** editor.md §Phase 6 · L269–273: `E0425-first, brace-depth counting, match-arm narrowing — live in SKILL.md / §Red-run triage heuristics, shared core, one location): / - Red run returned → start a fix round (Phase 6c): NEW worktree every time → / fix on the SAME branch → commit → push → report the NEW sha. Re-ship via / oc-deploy ship --execute...` · dimension: redundancy/structural rot · problem: orphaned fragment stranded mid-Phase-6 after the S3 ship-path rewrite — a pointer sentence with a dangling closing paren and no bullet-list owner, followed by a fix-round sequence that Phase 6c (L295+) restates in full. It reads as the un-deleted tail of a moved section; a reader hits "one location" immediately followed by a second inline copy of that procedure · fix: delete L269–273; the one-line triage pointer already exists inside Phase 6c where it belongs · **MED**

**2.** editor.md §Phase 6b · L310–311: `CODE TESTS (fmt/clippy/cargo test) are a different kind, CI-only — / Phase 7 step 2b.` (same error in SKILL.md §Test ontology table, L202: `pre-flight gate before PRs (Phase 7 step 2b)`) · dimension: stale ref · problem: fmt/clippy/cargo test live in step **2c** (pr-checks triad); step 2b is the PR-head compile gate (quick-build dispatch). The skill set itself proves it: SKILL.md L67 cites "(editor.md Phase 7 2c)" for pr-checks and editor.md L39 says "(fmt/clippy/test, Phase 7 step 2c)" — these two pointers predate the v0.4.28 insertion of 2c and were never re-anchored · fix: L311 and SKILL.md L202 → "Phase 7 step 2c" · **MED**

**3.** SKILL.md §Role router · L108–111: `The Editor NEVER installs/swaps binaries, NEVER restarts daemons, NEVER dispatches / BUILD runs — shipping goes through oc-deploy ship (S3; the one sanctioned / exception is the editor's own Phase 7 step 2b/2c CI-gate dispatch).` vs editor.md §Scope · L15: `sanctioned exception: the Phase 7 step 2b pre-flight gate).` vs SKILL.md §Hard rules · L458: `BUILD TRIGGERS = exactly TWO (v0.4.3, S3-rewired 2026-08-28): an editor's` · dimension: redundancy (inconsistent canonical copies of a dispatch-authority rule) · problem: three statements, three scopes — router exempts 2b**+2c**, editor.md exempts 2b **only** ("SOLE"), and the BUILD-TRIGGERS hard rule admits exactly two triggers with no exception, leaving no room for editor.md L422's direct `gh workflow run quick-build-linux.yml` at all. This is the exact rule class that produced the logged rogue-dispatch offense (2026-08-26/27); an editor reading editor.md alone and a supervisor reading SKILL.md alone will rule differently on whether 2c is legal · fix: one canonical statement in the SKILL.md hard rule listing the sanctioned exception set explicitly (2b compile gate + 2c pr-checks, direct dispatch, exempt from the two-trigger count); editor.md's scope sentence points to it instead of re-scoping · **HIGH**

**4.** editor.md §Phase 0 · L47: `git -C ~/opencrabs fetch adolfousier main && git -C ~/opencrabs merge --ff-only adolfousier/main` · dimension: stale ref (superseded mechanism) · problem: this is the retired merge-sync operation as a standing editor step. SKILL.md L320: `Sync model = REBASE-PORT (merge-sync retired 2026-08-26)` — fork main intentionally diverges from upstream, so this either fails loudly (non-ff) or, worse, succeeds and points the shared checkout at upstream `main`, a ref nothing downstream uses (worktrees cut from `origin/main` per Phase 2; shared checkout explicitly "NEVER evidence", L51) · fix: Phase 0 reduces to fetching and verifying `origin/main` freshness; the adolfousier merge step is deleted or marked superseded-by-rebase-port · **MED**

**5.** supervisor.md §PROCESS-TOOL OWNERSHIP · L23–24: `oc-deploy --selftest` / `(50 checks, must stay green before any version bump), the append-only journal,` · dimension: stale ref (hardcoded number) · problem: the count rotted — provenance v0.4.37 and v0.4.38 (SKILL.md L25–26) record `selftest 128 cases`. A supervisor quoting "50 checks" in a version-gate ruling cites a wrong invariant · fix: drop the number: "`oc-deploy --selftest` must stay green before any version bump" — counts belong in provenance lines, which are dated · **MED**

**6.** supervisor.md §Duty 2 · L46–47: `(NOT next to this skill: a skill-dir duplicate drifted and was deleted 2026-08-29;` and §Duty 6 Reviewer D · L173–174: `born from the / 2026-08-29 live-ledger deletion incident` (plus the full telling in SKILL.md L26 provenance: `two-file drift, baseline/orders backup paths at an empty dir, cycle counter reset 25->1; incident 2026-08-29`) · dimension: duplicated war story · problem: the same incident retold three times in two files with different detail levels and no canonical home · fix: add the incident to SKILL.md §Shared war stories (the canonical index); Duty 2 keeps the operational pointer (canonical path + v0.4.38), Reviewer D keeps a one-line pointer to that row · **MED**

**7.** SKILL.md §Canonical tooling rows L56, L57, L59 + §Session-notify loop L136–147 + editor.md §Phase 6b L303–304 and §Phase 7b L506 · verbatim: SKILL.md L56 `(post-swap fan-out targeting — [#24](...) LIVE via \`oc-deploy fanout\` since v0.4.37; Duty reviews)`; L141 `attribution range. LIVE since v0.4.37: \`oc-deploy fanout --run <id>\` (issue` · dimension: redundancy (v0.4.37 addition) · problem: "mechanical fan-out, issue #24, LIVE since v0.4.37" appears five-plus times; the session-notify bullet re-describes mechanics the fanout tool row already fully owns (`dead uuid = journal \`skip\` + note; auto-fired at the \`swap_execute\` tail, idempotent via \`fanout.state\`` ≈ tool row's `Idempotent via fanout.state ... Auto-wired: GREEN fires at swap_execute tail`) · fix: fanout tool-table row = single canonical mechanics+status block; the loop bullet keeps one sentence + "see tool table"; editor.md mentions keep "mechanical fan-out" without re-dating/re-linking · **MED**

**8.** editor.md §Telegram surface law · L73: `- You NEVER use telegram send/edit tools (\`telegram_send\`, \`tg_send_message\`, / \`tg_edit_message\`, \`telegram_edit\`) — for ANY destination, including your own` vs SKILL.md §Telegram surface law (canonical, marked at editor.md L71) · dimension: redundancy · problem: the duty block re-copies the tool enumeration verbatim, the reads rules (`no \`tg_search_global\``, own-topic `tg_get_messages`) and the reactions clause — four specifics that must now be edited in two files on every law change (the list is already stale-shaped: any new send tool silently escapes both enumerations) · fix: editor.md says "never invoke any telegram send/edit tool — canonical enumeration in SKILL.md"; duties keep only editor-specific consequences · **LOW**

**9.** editor.md §Phase 6 GATE comment · L191: `# test runs on this box (box law, AGENTS.md: cargo forbidden in ANY form).` · dimension: stale ref (wrong canonical pointer) · problem: inside the skill set the box law's full text is editor.md §Box law (SKILL.md L268: `Full text: editor.md §Box law`); AGENTS.md is the workspace-level pointer doc, not the skill's law · fix: cite "editor.md §Box law" (or "§Box law, top of this file") · **LOW**

**10.** SKILL.md §Shared environment facts · L257–268: `- This box has **no sanctioned Rust toolchain** — CI is the compiler (ruling / 2026-06-16, hardened owner-GO 2026-08-28). Never install cargo/rustc here — / and never INVOKE them: ... Sanctioned local tools: \`/usr/local/bin/rustfmt\` wrapper (fmt only) — modum / RETIRED from the process 2026-08-28 (owner order); lint evidence = GREEN / pr-checks.yml run.` · dimension: redundancy · problem: despite ending with `Full text: editor.md §Box law`, the bullet carries ~90% of the law (PATH forms, disabled-tree path, rustfmt/modum status, CI-dispatch rule) — two near-complete copies of a hard rule in one file set; v0.4.34 already had to sync both · fix: reduce the bullet to the 2-line invariant + pointer; details live once in editor.md §Box law · **LOW**

**11.** supervisor.md §Duty 3 · L90–93: `> Delivery discipline for any supervised ping (see the v0.4.19 gate above): / live roster check in the SAME turn; silent target -> one retry -> record the / delivery OUTCOME as a ledger/registry event note; \`target_session\` parses as a / FULL UUID - partial ids rejected at parse time.` vs SKILL.md L161–163: `DELIVERY ≠ QUEUE ACCEPTANCE (2026-08-26, Alexey): a ping counts as delivered / ONLY with post-ping proof — same-turn live roster check (\`session_search\`), / target PINGED-WOKEN ... or PINGED-SILENT.` · dimension: redundancy · problem: wake-proof rule stated twice; only the ledger-event-note clause is supervisor-specific · fix: keep one line "delivery discipline per SKILL.md §session_notify mechanics" + the ledger-note addition · **LOW**

**12.** supervisor.md §Duty 7 · L201: `\`IDEA: ADD|CHANGE <rule/tool> in <file+section> BECAUSE <gap actually hit>\`` vs editor.md §Telegram surface law · L77: `format \`IDEA: ADD|CHANGE <rule/tool> in <file+section> BECAUSE <gap\`` · dimension: redundancy (v0.4.39 addition) · problem: the exact format string is duplicated across files the same day it was born (and re-stated a third time in the L27 provenance); template drift between the two worker-facing copies is only a matter of edits · fix: canonical template in supervisor.md Duty 7 (which already says "Format = Duty 4's strict format with an `IDEA:` prefix" — Duty 7 need not re-quote either); editor.md keeps "Duty 7 format, `IDEA:` prefix" · **LOW**

**13.** SKILL.md §Canonical tooling · L65: `Archived at \`tools/archive/oc-consent-check\`, audit history only` · dimension: stale refs (artifact/doc mismatch) · problem: the archive copy exists (tools/archive/oc-consent-check confirmed), but a second copy `tools/oc-consent-check.pre-S2` (Aug 27) still sits in tools/ root, alongside `oc-deploy.pre-S2`, six oc-deploy `.bak` files, and `s2-swap-journal-spec.md` — that spec is referenced by zero skill files (grep: no hits) · fix: route to Reviewer D with this reference list as evidence: the stray `.pre-S2` and the unreferenced spec are DELETE-SAFE/ARCHIVE candidates; the `.bak` series needs the readers/writers check first — but the skill text should not claim "archived" while a sibling copy lives in the live tool dir · **LOW**

**14.** SKILL.md §Red-run triage heuristics · L239–240: `a RED report vs an ACK calling that run "in_progress/AIRBORNE")` · dimension: ontology (undefined coinage) · problem: AIRBORNE is ORDER-era intake vocabulary that survives nowhere else in the live files (single occurrence, never defined here) — a new reader cannot decode it · fix: "in_progress/queued" · **LOW**

**15.** SKILL.md §Canonical tooling · L39: `## Canonical tooling (v0.4.12, PROCESS-TOOL ownership)` (section claims: `this section is the register + test source of truth`) · dimension: redundancy/register gap · problem: the register has no row for the primary CLI's own modes — `oc-deploy ship/poll/swap-execute/watch` appear only as prose in editor.md §Phase 6 and SKILL.md hard rules, so the "source of truth" register omits the one command every build flows through · fix: add one `./tools/oc-deploy <mode>` row summarizing modes + exit codes, pointing to editor.md for the ship path · **LOW**

---

**Dimension sweeps with nothing further to flag:** the four ontology kinds themselves are used consistently across all three live files — SMOKE TEST / CODE TESTS / FEATURE-PRESENCE CHECK / EXECUTION SANITY SIGNAL never conflate, including in the new v0.4.37 tool text (oc-deploy L738 notify correctly demands a behavioral smoke-test, no kind-claim shortcuts found). Compiler.md contains no new findings — every stale anchor in it is already self-marked RETIRED/archived and its war stories are pointers to SKILL.md, as designed.

**Count: 15 findings — 1 HIGH · 7 MED · 7 LOW.**


