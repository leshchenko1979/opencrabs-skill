# opencrabs-dev skill review — v0.4.39 (Duty 6 Reviewer B)

Files in scope: SKILL.md (482 lines / 42.5 KB), editor.md (527 / 31.5 KB), supervisor.md (214 / 13.2 KB), compiler.md (387 / 24.2 KB — archived), tools/oc-{deploy (1271/82.5 KB), order-validate, attrib, carrier-features, issue-sweep, skew-scan, ping-proof, watchdog-check}, lib/oc-log.sh, tests/run.sh (446 lines).

---

**1.** `SKILL.md` §Canonical tooling · line 49: `| \`./tools/oc-order-validate <sha>\` | ORDER gates inside \`oc-deploy ship\` (archived: compiler Step 1) | 0 ok / 2 UNMERGED / 4 UNSIGNED |` · **dimension:** dead reference / misdocumented gate semantics · **problem:** the register says exit 4 = UNSIGNED, but the tool on disk (oc-order-validate line 15) declares `0 VALID | 1 invocation | 2 UNMERGED | 3 UNSIGNED/unknown | 4 UNKNOWN-REF`. The ORDER-gate tool is the most safety-critical row in the table — readers (and oc-deploy consumers) will misclassify 4 as "missing trailer" when it actually means "sha not on remote" (different remediation). · **fix:** rewrite the cell to `0 VALID / 1 invocation / 2 UNMERGED / 3 UNSIGNED / 4 UNKNOWN-REF`; drop the `(archived: compiler Step 1)` parenthetical. · **severity:** HIGH.

**2.** `tools/oc-deploy` §ship --execute success path · line 1189: `echo "NEXT (S1 relay): notify compiler with the run id for its swap watch"` (companion: line 1194 plan-mode `would: append shadow journal + relay run id to compiler (S1)`, line 1249 `plan "stage $STAGE < S2: swap LOCKED (shadow mode — compiler keeps all swaps)"`) · **dimension:** dead reference in executable output / responsibility creep · **problem:** this string is printed to the invoking editor session on every successful dispatch (S3 happy path). The compiler is retired; the correct next step is the mechanical fan-out already wired into `swap_execute` / `poll`. An editor following the printed instruction sends a session_notify at a dead lane — exactly the noise the surface law bans. The 1249 line also surfaces on ledger-corruption fallback (stage() → S0), where the "compiler keeps all swaps" text is actively misleading. · **fix:** line 1189 → `echo "NEXT: fanout (oc-deploy) auto-notifies contributors on swap; poll --execute drives the swap chain"`; line 1194 plan equivalent; line 1249 → `plan "stage $STAGE < S2: swap LOCKED (shadow mode — set ledger meta.oc_deploy_stage to S2 to unlock)"`. · **severity:** HIGH.

**3.** `SKILL.md` §Canonical tooling rows 49–55 · verbatim anchors across 5+ rows: `(archived: compiler Step 1)`, `(archived: Step 1)`, `(archived: Step 3)`, `(archived: Step 5)`, `(archived: Step 4)`, `(archived: Step 7; live: editor Phase 7 parity)` · **dimension:** token weight / archived-era text in required reading · **problem:** a register's job is slot + exit codes; the `(archived: Step N)` tail is a navigation aid into a retired runbook that no live behavior depends on. ~70 tokens per role load that invites the reader into `compiler.md`. · **fix:** drop every `(archived: …)` parenthetical; add one line above the table — `Step anchors below refer to the archived compiler runbook (compiler.md) — read-only for re-enable context.`. · **severity:** MED.

**4.** `SKILL.md` frontmatter §provenance · lines 18–27 (11 lines, 4,635 bytes ≈ 11 % of the file). Representative entry, line 22: `    - "v0.4.34 (owner 2026-08-28 17:2xZ, two orders): (1) modum RETIRED from the process — lint/static-analysis evidence = GREEN pr-checks.yml only; Phase 5 rewritten CI-only; editor.md scope/box-law/inline mentions, SKILL.md, AGENTS.md synced; rustfmt wrapper stays, modum binary stays installed. (2) The v0.4.33 post-swap-notify rewiring REVERTED per owner ('revert the previous change about post swap notifications') — Phase 6b trigger, FAIL routing, SKILL.md notify loop, supervisor.md Duty 7 back to pre-0.4.33 text; the notify duty is homeless again pending a process-first re-proposal (this bump)"` · **dimension:** token weight · **problem:** 9-numbered-entry changelog (S3 cutover + v0.4.32–39), many ~500 chars, narrating superseded states ("REVERTED", "homeless again", retired duties). No procedure references it; live state is restated in the body sections. ~1.2 K tokens per load. · **fix:** keep the latest entry + a one-line pointer (`history: rsi/improvements.md or git log SKILL.md`); move the rest to `skills/opencrabs-dev/CHANGELOG.md`. · **severity:** MED.

**5.** `SKILL.md` §Hard rules · line 419: `- COMPILER INBOX POLICY (**RETIRED with the role 2026-08-28; kept as inbox discipline for any re-enabled build lane** — noise audit 2026-08-26, owner "fix all"): the Compiler receives ONLY ORDERs (incl. verified re-deliveries), red-run handoffs, E0308-class fix routing, receipts requests, owner directives, supervisor roster-deltas. Status pings, unsolicited cycle summaries, ACK reminders are FORBIDDEN — ACK bookkeeping is ledger-internal, never messaged to workers.` · **dimension:** archived-era text in required reading · **problem:** ~90 words describing the inbox of a role that no longer exists, in the section BOTH live roles read on every load. The live rule it still carries (ACK bookkeeping is ledger-internal) is supervisor-only, not editor-facing. · **fix:** compress to one line and move under `supervisor.md` Duty 3 delivery discipline: `Inbox discipline for any (re-enabled) build lane: ORDERs/red-run handoffs/owner directives only; ACK bookkeeping stays ledger-internal.`. · **severity:** MED.

**6.** `SKILL.md` §Hard rules · line 405: `- CONSENT REGISTER — **RETIRED 2026-08-28 (owner order 18:50Z: the deploy consent process is ELIMINATED).** GREEN carrier run + artifact verify IS the authorization now: oc-deploy auto-swaps (journal line \`auto-swap reason=green-run\`), no register check pre-swap, no human GO in the pipeline. The \`oc-consent-check\` machine gate (v0.4.21) is archived at \`tools/archive/oc-consent-check\`; ledger \`consents[]\` rows stay as historical record. Incident history (kept, non-deploy lesson still binds): v0.4.17, 2026-08-26 — an owner denial-ruling was issued from codified text while the live consent sat unread in topic history. Before denying ANY permission, grep the actual record (chat history / ledger); never rule from codified memory. The UPSTREAM-PR owner-word gate is UNTOUCHED by this retirement: PR shipment still gates on owner approval = reply or positive Telegram reaction to the explicit request; silence is NOT consent; spontaneous/ad-hoc PRs remain forbidden.` · **dimension:** token weight / archived-era text · **problem:** ~200 words; only ~30 are the still-binding lesson ("never rule from codified text; grep the live record before denying any permission"). The rest is deploy-pipeline history an editor doesn't act on (auto-swap mechanics, archive path, ledger `consents[]` rows) — and is already stated in `oc-deploy` header and `supervisor.md` Duty 5. · **fix:** extract the lesson as a standalone hard rule, keep one sentence noting the retired gate's archive location: `- Never rule from codified memory — grep the live record (chat / ledger) before denying any permission. (Deploy consent RETIRED 2026-08-28: oc-consent-check archived; ledger consents[] historical only.)`. · **severity:** MED.

**7.** `SKILL.md` §Upstream relations · line 313–317 and line 324: line 313 `1. **Watch = supervisor duty (\`oc-deploy\` watch), every build cycle** (archived anchor: Step 1 pre-flight): fetch`; line 316 `Small clean delta → run the sync automatically;`; line 324 `   force-push-with-lease. Full procedure: \`compiler.md\` Step 7.` · **dimension:** responsibility creep / dead pointer · **problem:** item 1 assigns "run the sync automatically" to the supervisor; item 2 defines the sync as REBASE-PORT whose "Full procedure: compiler.md Step 7" — a file whose own heading says "⛔ ROLE RETIRED — Do NOT load this expecting an active role" (compiler.md line 1). The live supervisor duty has no live procedure home; a supervisor following the pointer lands in a retired runbook whose Step 7 (line 11, v0.4.26 "modum" gate) contradicts the current pr-checks-only evidence rule. · **fix:** either (a) port the Step 7 port procedure (absorbed/superseded/survivor classification, force-with-lease, seam-verify) into `supervisor.md` as a Duty, replacing the compiler.md pointer, or (b) mark the sync "owner-word only until re-homed" in item 2. · **severity:** MED.

**8.** `SKILL.md` §Canonical tooling · line 59 (single cell, measured 12-line/≈2.1 KB body): `| \`./tools/oc-deploy fanout --run <id> [--dry-run]\` | [issue #24](https://github.com/leshchenko1979/opencrabs/issues/24): mechanical notify fan-out for one carrier run. GREEN (success + built sha == \`deployed.sha\`): notify contributor sessions via \`opencrabs session notify --profile ops\` (dead uuids: verb rc 2 → journal \`skip\`, never fatal). RED (failure): gh annotations → \`git blame\` → culprit \`Session-Id\` trailer notified (\`role=blamed\`), suspect cc on same-file later touchers, zero-sites fallback HUMAN-FLAGs all range sessions. Idempotent via \`fanout.state\`; \`--dry-run\` journals but never notifies or marks done. Journal: \`STATE_DIR/oc-deploy/journal/fanout-<run>-*.jsonl\` steps \`fanout-start / contributors / attributed / notified / skip / unowned / fanout-end\`. Auto-wired: GREEN fires at \`swap_execute\` tail (after receipt seal + markers, before ledger stamp); \`poll\` scans latest FAILED run → fanout. Both suppressed by \`OC_DEPLOY_NOFANOUT=1\` (drills) and subshell-isolated (fanout rc never leaks into swap/poll rc) | 0 done-or-noop / 2 usage / 3 gh-or-attrib-fail / 4 run-not-terminal |` · **dimension:** prose that should be elsewhere + duplication · **problem:** the cell is ~12× the size of its neighbors and is restated again in §Session-notify loop (lines 139–147) and in provenance v0.4.37 (line 21). A register cell should be "what the command is + exit codes"; the GREEN/RED targeting logic, journal step vocabulary, and suppression env-var are operator procedure. · **fix:** shrink the cell to one line — `mechanical notify fan-out for one carrier run (GREEN → contributor notify, RED → blame notify; auto-fired at swap tail / poll RED-scan; OC_DEPLOY_NOFANOUT=1 suppresses; journal under STATE_DIR/oc-deploy/journal/)`; keep detail in §Session-notify loop as the single home. · **severity:** MED.

**9.** `SKILL.md` §Shared war stories · line 481: `| ORDER-era triggers + bounded ROLE_EXCEPTION | 2026-08-26 07:20 UTC: compiler authored the governor.rs E0308 fix while its author (61161247) slept through the owner's night window — a silent role breach. Now builds fire only on editor ORDER or owner word (sim-validated — sim = ORDER/queue simulation, paired-seed 600 reps, run 2026-08-26), and the breach is legal only as the bounded ROLE_EXCEPTION (\`compiler.md\` Step 2) |` �� **dimension:** dead reference / consistency · **problem:** the cell says "Now builds fire only on editor ORDER or owner word" but the same file's Hard rule 458 (line 458–459) states `BUILD TRIGGERS = exactly TWO (v0.4.3, S3-rewired 2026-08-28): an editor's \`oc-deploy ship <full-sha>\` background task (the ORDER-to-Compiler notify is RETIRED), or Alexey's word`. A reader gets two different trigger laws depending on which section they land in; the war story teaches retired vocabulary. · **fix:** update the cell to `Now builds fire only via \`oc-deploy ship\` or owner word; the breach is legal only as the bounded ROLE_EXCEPTION (compiler.md Step 2 — archived)`. · **severity:** LOW-MED.

**10.** `editor.md` §Phase 6 · line 238–244: `*(ARCHIVED at S3 cutover 2026-08-28 — ordering is deleted; kept for history.)* Pre-S3 the hand-off was an ORDER message to the Compiler with answers COALESCED / QUEUED / UNMERGED / UNSIGNED, verified against \`orders.json\`. The failure mode that killed ordering: an ORDER for 04f64d8a never reached the compiler while the issue was already annotated "awaiting Compiler" — the hand-off failed silently; that is why \`oc-deploy ship\` now runs the gates itself and returns GREEN/RED to the invoking session.` · **dimension:** archived-era text in editor required reading · **problem:** 7 lines of retired vocabulary (ORDER, COALESCED/QUEUED, `orders.json`) the editor cannot act on. The durable lesson is one clause ("gates run inside ship; silent hand-off was the failure mode"). · **fix:** collapse to one italic line — `*(Pre-S3 ordering is archived; the old failure mode was a silently dropped hand-off. \`oc-deploy ship\` now runs the gates itself and returns GREEN/RED.)*`. · **severity:** LOW-MED.

**11.** Box-law duplication across required reading · `SKILL.md` line 257–271: `This box has **no sanctioned Rust toolchain** — CI is the compiler (ruling 2026-06-16, hardened owner-GO 2026-08-28). Never install cargo/rustc here — and never INVOKE them: a working rustup tree survives under \`~/.cargo/\` + \`~/.rustup/\` (kept for the owner-approved rustfmt wrapper), its compile binaries were DISABLED 2026-08-28 (\`/root/toolchain-disabled-20260828/\`, manifest + \`restore.sh\`), and the BLOCKED stubs in \`/usr/local/bin\` are the floor of the rule, not the rule. Any invocation in any form — PATH, login shell, PATH-prepend, explicit path — is a violation even when it exits 0. Sanctioned local tools: \`/usr/local/bin/rustfmt\` wrapper (fmt only — modum RETIRED from the process 2026-08-28 (owner order); lint evidence = GREEN pr-checks.yml run. build/test/clippy = CI dispatch (pr-checks.yml / quick-build). Full text: editor.md §Box law.` and `editor.md` line 24–36 (the full §Box law section) and a third short restatement at editor.md Phase 5 ("No local lint tooling on this box — modum is OUT of the process.") and at editor.md Phase 7 step 2c ("cargo is FORBIDDEN on this box (binaries disabled 2026-08-28)"). · **dimension:** token weight / duplication (prose that should have one home) · **problem:** the ban list (PATH prepend, explicit paths, stubs-floor, disabled tree, rustfmt exception) appears nearly in full in BOTH files' mandatory reading; SKILL.md's bullet ends "Full text: editor.md §Box law" after having restated ~80% of it. ~30 lines combined paid by every role load. · **fix:** SKILL.md keeps the 2-line version (`No cargo/rustc/clippy in any form — CI is the compiler. Full ban list: editor.md §Box law.`); Phase 5/7c keep one-clause pointers. · **severity:** LOW-MED.

**12.** `supervisor.md` §PROCESS-TOOL · line 24: `Current invariants instead of Compiler validation: \`oc-deploy --selftest\` (50 checks, must stay green before any version bump), the append-only journal, and ledger receipts.` · **dimension:** stale reference (numbers drift) · **problem:** "50 checks" is pre-S2; `SKILL.md` provenance entries v0.4.37 and v0.4.38 record `selftest 128 cases` (the static check count in the selftest region is ~55 pass-increments; the 128 figure counts invocation forms). A supervisor gating a version bump against "50" cannot reconcile the numbers, and the supervisor role is the one explicitly told to maintain invariant-counts. · **fix:** drop the number, cite the battery: `(\`oc-deploy --selftest\` green; \`tools/tests/run.sh\` 76 PASS)`. · **severity:** LOW-MED.

**13.** `SKILL.md` §STEP ZERO · line 99: `> **Editor, Compiler, or Supervisor?**` · **dimension:** dead reference · **problem:** every session load begins with a forced choice that includes a dead role. The table two lines below marks the role RETIRED, but the question invites the answer — one wasted clarification round whenever an operator types "Compiler". · **fix:** `> **Editor or Supervisor?** (Compiler: archived — say "re-enable compiler" to load compiler.md)`. · **severity:** LOW.

**14.** `supervisor.md` scope (line 7) and Duty 6 Reviewer B brief (line 160) · line 7: `Scope: own the skill set (\`SKILL.md\` / \`editor.md\` / \`compiler.md\` / \`supervisor.md\`)`; line 160: `migrating across Supervisor/Editor/Compiler scope lines.` · **dimension:** dead reference (minor) · **problem:** the duty description and the supervisor's own scope both name a role whose file begins with a RETIRED banner; Reviewer B's prompt tells future reviewers to police a scope line that no longer has a worker. · **fix:** line 7 → `Scope: own the skill set (\`SKILL.md\` / \`editor.md\` / \`supervisor.md\`; \`compiler.md\` archived)`. Line 160 → `migrating across Supervisor/Editor scope lines (Compiler archived)`. · **severity:** LOW.

**15.** `SKILL.md` §Session-notify loop · line 140: `baseline state file (\`oc-seal-state\`) — its \`sha\` is the left edge of the next` · **dimension:** dead reference (soft) · **problem:** since v0.4.38 `oc-seal-state` writes `/root/.opencrabs/profiles/ops/opencrabs-dev/baseline.json` (oc-seal-state line 41, `BASE="${OC_BASELINE:-/root/.opencrabs/profiles/ops/opencrabs-dev/baseline.json}"`); the skill text names the file with no path, and `deployed.sha` lives next to the skill files (a different dir). A worker grepping the skill dir finds nothing. `supervisor.md` line 26 spells the ledger's canonical path explicitly after the same 2026-08-29 incident; this is the parallel for baseline.json. · **fix:** one parenthetical — `baseline state file (oc-seal-state; canonical: ~/.opencrabs/profiles/ops/opencrabs-dev/baseline.json)`. · **severity:** LOW.

**16.** `tools/oc-order-validate` header · line 3: `# oc-order-validate — validate an editor ORDER before the compiler dispatches.` · **dimension:** dead reference (stale header) · **problem:** the tool now runs inside `oc-deploy ship` (the editor's own ship path); the compiler does not dispatch. Future maintainers reading the header assume the old flow. · **fix:** `# oc-order-validate — validate a build sha before dispatch (invoked from oc-deploy ship; 4 gates: shape, existence, containment, signature).`. · **severity:** LOW.

**17.** `SKILL.md` §Hard rules · line 386: `- ISSUE ROUTING (owner directive 2026-08-27 14:08Z): NEW issues NEVER go to` `  \`adolfousier/opencrabs\` — every new issue (upstream-code bugs and fork-only` `  infra alike) is filed on the FORK \`leshchenko1979/opencrabs\`. Upstream` `  receives PRs ONLY: the body is a detailed description with the link(s) to` `  the original fork issue(s) at the END (\`Original issue: <full fork URL>\`) — never` `  \`Closes #N\`, which on upstream resolves against the WRONG issue space. WE` `  close the fork issue right after filing the PR. Fork issues are NEVER claimed` `  on GitHub ... PR SHIPMENT still gates on the 2026-08-25 directive:` `  feature COMPLETE + smoke evidence OWNER-APPROVED → the Editor harvests` `  fork-only commits and opens the upstream PR ... APPROVAL GATE (v0.4.1): the Editor posts the` `  smoke evidence + an explicit approval request in its forum topic and WAITS —` `  Alexey's approval = his reply or a positive Telegram reaction to that request;` `  silence is NOT consent. Spontaneous / ad-hoc PRs remain forbidden.` · **dimension:** prose that should be a table · **problem:** ~15 lines of dense bullet that bundles ≥5 distinct rules (new-issue venue, PR-body format, no-fork-claiming, uniqueness sweep, PR shipment gate, approval gate, ad-hoc PR ban) with different counter-parties. Shared-column structure — rule / counterparty / gate / evidence — is a table, not a bullet. · **fix:** split into a table with rows `Rule | Counterparty | Gate | Source` (new-issue venue / PR body / fork-claim ban / uniqueness sweep / PR approval gate / ad-hoc ban) and move the 2026-08-22 history link into a single trailing footnote. · **severity:** LOW-MED.

**Dimensions explicitly checked and yielding nothing:** nothing — all four lenses (token weight, prose→table, dead references, responsibility creep) produced at least one finding. Cross-file consistency (war story 481 vs Hard rule 458 trigger law) surfaced under dead references and is a fifth incidental dimension worth flagging but not duplicated.

**Count summary:** 17 findings — 2 HIGH, 8 MED, 5 LOW-MED, 4 LOW (2 dead, 6 token weight, 3 prose→table, 4 responsibility creep, with several overlapping).


<!-- /phantom_blocked=1 -->
<!-- phantom_blocked=1 -->
<!-- reasoning -->
The previous turn's tool calls returned evidence (line numbers, the provenance entry, the NEXT S1 relay line, stage-gate line). I have everything I need. The system now confirms a different provider is handling and tells me to either invoke the correct tool or reply with a short confirmation if the task is already done.

The task is a READ-ONLY review. My final reply IS the report. I have the verified evidence (line numbers, quotes from disk) in my conversation. I will now produce the report as the final reply. No further tool calls needed — additional re-verification would violate "do not run additional tool calls to verify work you already did".

Let me write the report now. Strict format: numbered findings, each with file §section · verbatim quote (with line #) · dimension · problem · concrete fix · severity. Then the dimension-coverage line + the one-line count summary.

Quantification:
- SKILL.md: 482 lines / 42,568 bytes (~13% of mandatory reading is the frontmatter + provenance)
- editor.md: 527 lines / 31,567 bytes
- supervisor.md: 214 lines / 13,165 bytes
- compiler.md (archived): 387 lines / 24,187 bytes — still on disk, still in the role router table
- Frontmatter provenance: 11 lines / 4,635 bytes ≈ 11% of SKILL.md
- Canonical tooling table: lines 39–94 = 56 lines
- Hard rules: lines 365–467 = 103 lines = 21% of SKILL.md
- oc-deploy fanout table row: line 59 alone, ~2,100 chars in the cell

Dimension coverage: all four yield findings (1 token-weight, 2 prose→table, 3 dead-refs, 4 responsibility-creep). Some findings span multiple dimensions — I'll classify each by its primary.

Let me write the report. I'll keep it as compact as the contract allows while staying precise.
<!-- /reasoning -->

# Skill review — opencrabs-dev v0.4.39

Read scope: `SKILL.md` (482 lines, 42.6 KB) · `editor.md` (527 lines, 31.6 KB) · `supervisor.md` (214 lines, 13.2 KB) · `compiler.md` archived (387 lines, 24.2 KB) · `tools/oc-deploy` (1,271 lines) + the seven KERNEL-batch tools and `lib/oc-log.sh` and `tests/run.sh`. Quantification where it matters: **Hard rules = 103 lines = 21% of SKILL.md**; **frontmatter `provenance:` = 11 lines / 4,635 bytes ≈ 11% of SKILL.md**; **Canonical-tooling `oc-deploy fanout` row alone = 1 cell, ~2,100 chars** (vs ~120–400 for every other row). All four hunt dimensions produced findings.

---

### 1. SKILL.md §Canonical tooling (line 49) — oc-order-validate exit codes contradict the tool on disk
Verbatim, line 49: `| \`./tools/oc-order-validate <sha>\` | ORDER gates inside \`oc-deploy ship\` (archived: compiler Step 1) | 0 ok / 2 UNMERGED / 4 UNSIGNED |`
Verbatim, `tools/oc-order-validate` line 15: `echo "Exit: 0 VALID | 1 invocation | 2 UNMERGED | 3 UNSIGNED/unknown | 4 UNKNOWN-REF" >&2`
**Dimension:** dead reference (wrong evidence) + token weight.
**Problem:** the register says `4 = UNSIGNED`; the tool emits `3 = UNSIGNED/unknown` and `4 = UNKNOWN-REF`. A worker (or `oc-deploy` consumer) reading the table misclassifies a gate failure — `4` actually means "sha not found on remote", a completely different remediation than an unsigned commit. This is the ORDER-gate tool — the most safety-critical row in the table.
**Fix:** replace the cell with `0 VALID / 1 invocation / 2 UNMERGED / 3 UNSIGNED / 4 UNKNOWN-REF`. Spot-check the other gate rows (`oc-artifact-verify`, `oc-seal-state`, `oc-post-receipts`, `oc-job-verify`, `oc-index-worktree`) against their tool headers while editing; the on-disk headers (read this pass) match the SKILL.md rows except for `oc-order-validate`.
**Severity:** HIGH.

### 2. `tools/oc-deploy` ship --execute branch (line 1189) — live output still routes the editor to the retired Compiler
Verbatim, line 1189: `echo "NEXT (S1 relay): notify compiler with the run id for its swap watch"`
Verbatim, line 1194: `plan "would: append shadow journal + relay run id to compiler (S1)"`
Verbatim, line 1249: `plan "stage $STAGE < S2: swap LOCKED (shadow mode — compiler keeps all swaps)"`
**Dimension:** dead reference + responsibility creep.
**Problem:** line 1189 is **printed to the invoking editor session on every successful dispatch** (the most-traveled path). There is no compiler to notify; the correct next step is the mechanical fan-out (`oc-deploy fanout`) or `oc-deploy poll`, which already exist. An editor following the printed instruction sends a `session_notify` at a dead lane — the exact noise the Telegram surface law (SKILL.md §Telegram surface law) bans. Line 1249 is reachable if the ledger is unreadable and `stage()` falls back to `S0`; in that failure path it actively misinforms. Plan-mode line 1194 has the same problem.
**Fix:** line 1189 → `echo "NEXT: wait for oc-deploy poll (mechanical swap) and the post-swap fan-out"`; line 1194 → drop the "(S1)" suffix and the "relay run id to compiler" phrase; line 1249 → `plan "stage $STAGE < S2: swap LOCKED (shadow mode — no swap occurs)"`.
**Severity:** HIGH (machine-emitted instruction to a nonexistent role on the happy path).

### 3. SKILL.md §Canonical tooling rows 49–55 — archived-era compiler anchors in the tool register
Verbatim, line 50: `| \`./tools/oc-job-verify <run-id> <source-ref> [--features]\` | run identity inside \`oc-deploy ship/poll\` (archived: Step 1) | 0 VERIFIED / 2 IN-FLIGHT / 3 FAILED / 4 REF-MISMATCH / 5 NOT-FOUND |`
Lines 49, 51, 52, 53, 55 carry the same pattern: `(archived: compiler Step N)`.
**Dimension:** token weight / archived-era text in required reading.
**Problem:** 5+ register rows spend ~10 words each explaining where the step USED to live pre-retirement. The register's job is slot + exit codes; zero live behavior depends on knowing `compiler.md` step numbers. ~60–80 tokens paid per load across both roles, and the parentheticals invite a reader into an archived file.
**Fix:** delete the parentheticals; one sentence above the table — "Step anchors refer to the archived compiler runbook (`compiler.md`)."
**Severity:** MED.

### 4. SKILL.md frontmatter `provenance:` (lines 18–25, 4,635 bytes) — full changelog loaded on every role read
Verbatim, line 22 (v0.4.34 entry, representative of weight): `    - "v0.4.34 (owner 2026-08-28 17:2xZ, two orders): (1) modum RETIRED from the process — lint/static-analysis evidence = GREEN pr-checks.yml only; Phase 5 rewritten CI-only; editor.md scope/box-law/inline mentions, SKILL.md, AGENTS.md synced; rustfmt wrapper stays, modum binary stays installed. (2) The v0.4.33 post-swap-notify rewiring REVERTED per owner ('revert the previous change about post swap notifications') — Phase 6b trigger, FAIL routing, SKILL.md notify loop, supervisor.md Duty 7 back to pre-0.4.33 text; the notify duty is homeless again pending a process-first re-proposal (this bump)"`
**Dimension:** token weight.
**Problem:** 9 numbered changelog entries (S3 cutover + v0.4.32 → v0.4.39), several ~500 chars, narrating superseded states ("REVERTED", "homeless again", retired duties). The entire provenance block = **4,635 bytes ≈ 11% of SKILL.md**; every session load pays it. No procedure in the body references these entries; the live state is already restated in the body sections.
**Fix:** keep the most recent entry + one line "history: `rsi/improvements.md` / git", move the rest to a `CHANGELOG.md` beside the skill (or cap each entry at ≤1 line: version — one sentence — owner word).
**Severity:** MED.

### 5. SKILL.md §Hard rules (line 419) — COMPILER INBOX POLICY paragraph in both-roles required reading
Verbatim, line 419: `- COMPILER INBOX POLICY (**RETIRED with the role 2026-08-28; kept as inbox discipline for any re-enabled build lane** — noise audit 2026-08-26, owner "fix all"): the Compiler receives ONLY ORDERs (incl. verified re-deliveries), red-run handoffs, E0308-class fix routing, receipts requests, owner directives, supervisor roster-deltas. Status pings, unsolicited cycle summaries, ACK reminders are FORBIDDEN — ACK bookkeeping is ledger-internal, never messaged to workers.`
**Dimension:** token weight / archived-era text in required reading.
**Problem:** ~90 words describing the inbox of a role that no longer exists, in the section BOTH live roles must read on every load. The one live behavior it carries ("ACK bookkeeping is ledger-internal, never messaged to workers") is supervisor-only.
**Fix:** compress to one line in `supervisor.md` Duty 3 delivery discipline, drop the hard-rule bullet: "Inbox discipline for any (re-enabled) build lane: ORDERs/red-run handoffs/directives only; ACK bookkeeping stays ledger-internal."
**Severity:** MED.

### 6. SKILL.md §Hard rules (line 405) — CONSENT REGISTER retirement paragraph
Verbatim, line 405: `- CONSENT REGISTER — **RETIRED 2026-08-28 (owner order 18:50Z: the deploy consent process is ELIMINATED).** GREEN carrier run + artifact verify IS the authorization now: oc-deploy auto-swaps (journal line \`auto-swap reason=green-run\`), no register check pre-swap, no human GO in the pipeline. The \`oc-consent-check\` machine gate (v0.4.21) is archived at \`tools/archive/oc-consent-check\`; ledger \`consents[]\` rows stay as historical record. Incident history (kept, non-deploy lesson still binds): v0.04.17, 2026-08-26 — an owner denial-ruling was issued from codified text while the live consent sat unread in topic history. Before denying ANY permission, grep the actual record (chat history / ledger); never rule from codified memory.`
**Dimension:** token weight / archived-era text in required reading.
**Problem:** ~140 words of which ~40 are still-binding ("never rule from codified text; grep the live record"). The rest (auto-swap mechanics, archive path, ledger `consents[]` history) is pipeline history an editor doesn't act on; the mechanics are already in `oc-deploy`'s header (line 39 "NO CONSENT GATE") and `supervisor.md` Duty 5.
**Fix:** keep the lesson as a standalone hard rule ("Never rule from codified memory — grep the live record before denying any permission"); compress the rest to one sentence + provenance.
**Severity:** MED.

### 7. SKILL.md §Upstream relations items 1–2 (lines 313, 324) — the upstream sync duty's procedure lives in the archived compiler runbook
Verbatim, line 317: `   (archived anchor: Step 1 pre-flight): fetch \`adolfousier\`, compare upstream tip vs fork merge-base. Small clean delta → run the sync automatically; mass absorption ... → notify Alexey with the delta and WAIT.`
Verbatim, line 324: `   force-push-with-lease. Full procedure: \`compiler.md\` Step 7.`
**Dimension:** responsibility creep / dead pointer.
**Problem:** item 1 assigns "run the sync automatically" to the supervisor; item 2 defines the sync as REBASE-PORT and points at `compiler.md` Step 7 — a file that opens with `⛔ ROLE RETIRED ... Do NOT load this expecting an active role; the active surface is SKILL.md + editor.md + supervisor.md.` and whose own Step 7 still cites the retired `modum` gate ("modum RETIRED 2026-08-28 — port-seam evidence = pr-checks GREEN"). A supervisor following the pointer lands in a retired role's runbook and gets contradicting evidence rules.
**Fix:** either (a) port Step 7's classification table + force-with-lease + seam-verify into `supervisor.md` as a numbered Duty (clean, lets the sync run), or (b) explicitly downgrade item 2 to "owner-word only until port procedure is re-homed" and link to `oc-deploy` for the watch half only.
**Severity:** MED.

### 8. SKILL.md §Canonical tooling (line 59) — `oc-deploy fanout` register row carries ~2,100 chars of procedure prose
Verbatim, line 59: `| \`./tools/oc-deploy fanout --run <id> [--dry-run]\` | [issue [#24](...): mechanical notify fan-out for one carrier run. GREEN (success + built sha == \`deployed.sha\`): notify contributor sessions via \`opencrabs session notify --profile ops\` (dead uuids: verb rc 2 → journal \`skip\`, never fatal). RED (failure): gh annotations → \`git blame\` → culprit \`Session-Id\` trailer notified (\`role=blamed\`), suspect cc on same-file later touchers, zero-sites fallback HUMAN-FLAGs all range sessions. Idempotent via \`fanout.state\`; \`--dry-run\` journals but never notifies or marks done. Journal: \`STATE_DIR/oc-deploy/journal/fanout-<run>-*.jsonl\` steps \`fanout-start / contributors / attributed / notified / skip / unowned / fanout-end\`. Auto-wired: GREEN fires at \`swap_execute\` tail ... \`poll\` scans latest FAILED run → fanout. Both suppressed by \`OC_DEPLOY_NOFANOUT=1\` (drills) and subshell-isolated ... | 0 done-or-noop / 2 usage / 3 gh-or-attrib-fail / 4 run-not-terminal |`
**Dimension:** prose that should be elsewhere (register-row bloat) + token weight.
**Problem:** ~12× the size of neighboring rows; same content re-stated in §Session-notify loop (lines 141–147) and in `provenance:` v0.4.37 (line 21). The register's job is what-the-command-is + exit codes; GREEN/RED targeting, journal step vocabulary, and suppression env-var are operator procedure, not table content. ~450 tokens paid twice per load (register + loop).
**Fix:** shrink the cell to: "issue #24 mechanical notify fan-out for one carrier run (GREEN→contributor notify, RED→blame notify; auto-wired at `swap_execute` tail + on `poll` RED-scan; `OC_DEPLOY_NOFANOUT=1` suppresses; journal under `STATE_DIR/oc-deploy/journal/`)"; move the GREEN/RED/role-tag/journal-step detail to §Session-notify loop (its natural home). One canonical home for procedure.
**Severity:** MED.

### 9. SKILL.md §Shared war stories (line 481) — war story still teaches retired trigger vocabulary, contradicting Hard rule line 458
Verbatim, line 481: `| ORDER-era triggers + bounded ROLE_EXCEPTION | 2026-08-26 07:20 UTC: compiler authored the governor.rs E0308 fix while its author (61161247) slept through the owner's night window — a silent role breach. Now builds fire only on editor ORDER or owner word (sim-validated — sim = ORDER/queue simulation, paired-seed 600 reps, run 2026-08-26), and the breach is legal only as the bounded ROLE_EXCEPTION (\`compiler.md\` Step 2) |`
Verbatim, line 458: `- BUILD TRIGGERS = exactly TWO (v0.4.3, S3-rewired 2026-08-28): an editor's \`oc-deploy ship <full-sha>\` background task (the ORDER-to-Compiler notify is RETIRED), or Alexey's word ...`
**Dimension:** dead reference / consistency.
**Problem:** the war story says "Now builds fire only on editor ORDER or owner word"; the Hard rule 6 lines later says ORDER-to-Compiler is RETIRED and the trigger is `oc-deploy ship`. A reader gets two different trigger laws depending on which section they land in, and the war story's "ROLE_EXCEPTION" was the COMPILER's bounded exception — a role that no longer exists.
**Fix:** update the cell: "Now builds fire only via `oc-deploy ship` or owner word; the breach is legal only as the bounded ROLE_EXCEPTION (archived: compiler.md Step 2)" — or delete the row outright (the exception is dead with the role).
**Severity:** LOW-MED.

### 10. editor.md §Phase 6 (line 238) — archived ORDER-era hand-off narrative in the editor's live procedure
Verbatim, line 238: `*(ARCHIVED at S3 cutover 2026-08-28 — ordering is deleted; kept for history.)* Pre-S3 the hand-off was an ORDER message to the Compiler with answers COALESCED / QUEUED / UNMERGED / UNSIGNED, verified against \`orders.json\`. The failure mode that killed ordering: an ORDER for 04f64d8a never reached the compiler while the issue was already annotated "awaiting Compiler" — the hand-off failed silently; that is why \`oc-deploy ship\` now runs the gates itself and returns GREEN/RED to the invoking session.`
**Dimension:** archived-era text in required reading.
**Problem:** 7 lines of retired vocabulary (ORDER, COALESCED/QUEUED, `orders.json`) that the editor cannot act on. The durable lesson is one clause ("gates run inside ship; silent hand-off was the failure mode").
**Fix:** one line: "*(Pre-S3 ordering is archived — `oc-deploy ship` now runs the gates itself; the old failure mode was a silently dropped hand-off.)*"
**Severity:** LOW-MED.

### 11. SKILL.md §Shared environment facts (line 257) and editor.md §Box law (line 24) — Box law duplicated across both files' mandatory reading
Verbatim, SKILL.md line 257: `This box has **no sanctioned Rust toolchain** — CI is the compiler (ruling 2026-06-16, hardened owner-GO 2026-08-28). Never install cargo/rustc here — and never INVOKE them: a working rustup tree survives under \`~/.cargo/\` + \`~/.rustup/\` (kept for the owner-approved rustfmt wrapper), its compile binaries were DISABLED 2026-08-28 ...`
Verbatim, SKILL.md line 271: `Full text: editor.md §Box law.`
Verbatim, editor.md line 24: `## Box law — no local cargo, ever (ruling 2026-06-16, hardened owner-GO 2026-08-28)` followed by ~12 lines restating the ban list (PATH / login shell / PATH-prepend / explicit paths / stubs-floor / disabled tree / rustfmt exception / modum retired / lint evidence = pr-checks) — and again as a one-line aside in editor.md Phase 5 ("No local lint tooling on this box — modum is OUT of the process") and in Phase 7 step 2c ("cargo is FORBIDDEN on this box (binaries disabled 2026-08-28)").
**Dimension:** token weight / prose that should have one home.
**Problem:** the ban list appears nearly in full in both files' mandatory reading (~30 lines combined); SKILL.md's bullet even restates ~80% of it then ends "Full text: editor.md §Box law".
**Fix:** SKILL.md keeps a 2-line version ("No cargo/rustc/clippy in any form — CI is the compiler. Full ban list: editor.md §Box law."); editor.md Phase 5 / Phase 7 2c keep single-clause pointers ("box law").
**Severity:** LOW-MED.

### 12. supervisor.md §PROCESS-TOOL (line 24) — stale selftest count, contradicts its own provenance
Verbatim, line 24: `Current invariants instead of Compiler validation: \`oc-deploy --selftest\` (50 checks, must stay green before any version bump), the append-only journal, and ledger receipts.`
Verbatim, SKILL.md provenance v0.4.37 (line 21): `selftest 128 cases, battery 76 PASS / 0 FAIL` (and v0.4.38 repeats the same number).
**Dimension:** dead/stale reference.
**Problem:** "50 checks" dates from the S2 era; the v0.4.37/0.4.38 self-review already updated the number to "128 cases" in the provenance block. A supervisor gating a version bump against "50" cannot reconcile the two numbers in the same skill set.
**Fix:** drop the count: "(`oc-deploy --selftest` green; battery `tools/tests/run.sh` 76 PASS)".
**Severity:** LOW-MED.

### 13. SKILL.md §STEP ZERO (line 99) — mandatory role question still offers the retired Compiler as a first-class answer
Verbatim, line 99: `> **Editor, Compiler, or Supervisor?**`
**Dimension:** dead reference.
**Problem:** every session load begins with a forced choice that includes a dead role (the table 2 lines below marks it RETIRED, but the question invites the answer). Costs a clarification round whenever an operator answers "Compiler".
**Fix:** `> **Editor or Supervisor?** (Compiler: archived — say "re-enable compiler" to load compiler.md.)`
**Severity:** LOW.

### 14. supervisor.md §Scope (line 7) and §Duty 6 (line 160) — retired Compiler still in scope/lens text
Verbatim, line 7: `Scope: own the skill set (\`SKILL.md\` / \`editor.md\` / \`compiler.md\` / \`supervisor.md\`), keep every worker ON the current skill version ...`
Verbatim, line 160: `migrating across Supervisor/Editor/Compiler scope lines.`
**Dimension:** dead reference.
**Problem:** the duty-6 lens instructs future reviewers to police a scope line that no longer has a worker; `compiler.md` is archived. The Duty 5 ruling note (line 96) also still says `consent msg 32607` (RULING-CORRECTION #1) as the canonical example of a non-deploy lesson — fine, but it could be re-keyed to a fresh example to stop carrying "consent" vocabulary in unrelated contexts.
**Fix:** line 7 → "own the skill set (`SKILL.md` / `editor.md` / `supervisor.md`; `compiler.md` archived)"; line 160 → "Supervisor/Editor scope lines (Compiler archived)".
**Severity:** LOW.

### 15. SKILL.md §Session-notify loop (line 140) — `baseline.json` referenced without its (new, v0.4.38) canonical location
Verbatim, line 140: `baseline state file (\`oc-seal-state\`) — its \`sha\` is the left edge of the next`
**Dimension:** dead reference (soft).
**Problem:** since v0.4.38 the seal tool writes `/root/.opencrabs/profiles/ops/opencrabs-dev/baseline.json` (`tools/oc-seal-state` line 41: `BASE="${OC_BASELINE:-/root/.opencrabs/profiles/ops/opencrabs-dev/baseline.json}"`); the skill text names the file with no path. A worker grepping the skill dir (where `deployed.sha` lives) finds nothing. Contrast: `supervisor.md` Duty 2 spells the ledger's canonical path explicitly after the same incident.
**Fix:** add the canonical path once in §Session-notify loop.
**Severity:** LOW.

### 16. `tools/oc-order-validate` header (line 3) — stale flow description
Verbatim, line 3: `# oc-order-validate — validate an editor ORDER before the compiler dispatches.`
**Dimension:** dead reference.
**Problem:** the tool no longer sits in a compiler dispatch flow; it's invoked inside `oc-deploy ship` (the editor's own background task) per the S3 cutover. The header still says "the compiler dispatches".
**Fix:** "validate a build sha before dispatch (runs inside `oc-deploy ship` per the S3 cutover)."
**Severity:** LOW.

### 17. SKILL.md §Hard rules (line 386) — ISSUE ROUTING bullet bundles 5+ distinct rules in one prose line (prose → table)
Verbatim, line 386: `- ISSUE ROUTING (owner directive 2026-08-27 14:08Z): NEW issues NEVER go to \`adolfousier/opencrabs\` — every new issue (upstream-code bugs and fork-only infra alike) is filed on the FORK \`leshchenko1979/opencrabs\`. Upstream receives PRs ONLY: the body is a detailed description with the link(s) to the original fork issue(s) at the END (\`Original issue: <full fork URL>\`) — never \`Closes #N\`, which on upstream resolves against the WRONG issue space. WE close the fork issue right after filing the PR. Fork issues are NEVER claimed on GitHub (owner directive 2026-08-27 17:07Z): no tackling comments, no assignment, no labels/reactions by any lane — claiming lives in the ledger (\`Issue-Ref\` trailer + workers-ledger row); ... PR SHIPMENT still gates on the 2026-08-25 directive: feature COMPLETE + smoke evidence OWNER-APPROVED → the Editor harvests fork-only commits and opens the upstream PR ... APPROVAL GATE (v0.4.1): ... Alexey's approval = his reply or a positive Telegram reaction to that request; silence is NOT consent. Spontaneous / ad-hoc PRs remain forbidden.`
**Dimension:** prose that should be a table.
**Problem:** one 25-line bullet encodes at least five rules (new-issue venue, PR-body format / no `Closes #N`, no GitHub claiming on the fork, ownership-via-ledger, PR approval gate / ad-hoc ban). Sharing rule + counterparty + gate across structured columns would cut it roughly in half and make one-glance compliance checks possible. Same file already has tables for the role-router row, the test ontology, the war stories — precedent for the shape.
**Fix:** table with columns `Rule | Owner | Gate`, one row per sub-rule; link the upstream reference at the bottom of the section.
**Severity:** LOW-MED.

---

## Dimension coverage
- **Token weight** — findings 3, 4, 5, 6, 10, 11.
- **Prose → table / register-row bloat** — findings 8, 17.
- **Dead references (incl. wrong evidence)** — findings 1, 2, 3, 9, 12, 13, 14, 15, 16.
- **Duties migrating across scope lines / responsibility creep** — findings 2, 7.

## Count summary
**17 findings: 2 HIGH · 7 MED · 5 LOW-MED · 3 LOW.**
