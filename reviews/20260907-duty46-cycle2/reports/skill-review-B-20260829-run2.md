# Skill review — RUN 2 — Lens B (LLM efficiency + responsibility creep)
Agent 54effd9c · post-v0.4.42 · delivered 2026-08-29 · persisted by supervisor d72bd52d

1. supervisor.md §Duty 3 · quote: "BEND (v0.4.42, ruling event 1323): TOOL-written stamps (oc-deploy swap-execute etc. — flock write happens in a tool subprocess no session can commit from) are committed by the HOSTING session — the turn that observes the stamp on disk — bundling the tool stamp with its own adjacent stamp in one commit if both are pending (message names all stamps); oc-ledger self-commit code (oc-work/oc-ledger-design-20260829.md) is the end state." · dimension: responsibility-creep · problem: ownership is NOT unambiguous and two lanes can wait for each other. swap-execute runs under a systemd transient unit handed off by `poll --execute`, which in the live case ran inside the SHIPPING EDITOR's background task (ledger n=1318: lane 29 "swap chain watcher running (gh run watch + poll --execute)"; state-repo commit a1756956 names "hosting session d5863180" — an editor). So the "HOSTING session" that owes the state-repo commit is routinely an EDITOR, but editor.md/SKILL.md contain zero bytes of this regime (grep of editor.md for flock/git-history/state repo/v0.4.41/v0.4.42 = 0 matches; in SKILL.md it appears only as frontmatter provenance), and the router says "load ONLY the one matching the session's role". Observed outcome: the hosting editor committed tools.log appends but NOT the n=1320 tool stamp; the supervisor back-filled all three stamps as backlog commits, botching the first attempt (state-repo reflog: "v1 of this commit sliced from HEAD by mistake — redone"). No duty or tool detects pending uncommitted stamps, so if the supervisor assumes hosting-session ownership and the editor never learns it has any, stamps wait indefinitely. Also undefined: fallback when the hosting session dies before observing the stamp (sessions "wake and die within minutes" per Duty 2). · fix: name the owner per stamp class in BOTH role files: if hosting session = editor, add a state-repo stamp-commit clause to editor.md (Phase 6b/6c) or explicitly re-own tool-stamp commits to the supervisor with a mechanical pending-stamp sweep (git status + ledger max-n vs committed n) on the Duty-3/4 cadence; define the dead-hosting-session fallback. · severity: critical

2. editor.md §Phase 1 (step 3) · quote: "Claim record = `Issue-Ref: #N` trailer on commits/PR + your feature row in `workers-ledger.json` (first ledger timestamp wins; conflicts are supervisor rulings, never GitHub chatter)." · dimension: responsibility-creep · problem: editors WRITE ledger stamps directly (live events n=1307–1316/1322/1324 are all "by editor lane..."; the n=1322 writer re-indented the whole file), and the v0.4.41 base rule (supervisor.md Duty 3: "one commit per ledger stamp, inside the same flock as the write") physically attaches the commit duty to the stamp's WRITER — i.e. the editor. But editor.md never mentions flock discipline, the state repo, or commit-per-stamp, and the stamping mechanism itself (raw jq edit vs tool) is unspecified, so the editor performs undocumented state-dir file surgery plus an owed git commit it cannot know about; in practice the supervisor silently absorbs the commits (backlog 265d72e/76d2855/3af6981). Duty migrated across the scope line by omission. · fix: state in editor.md exactly how an editor stamps the ledger and who commits it (either "supervisor commits editor stamps at next sweep" or an editor-side one-liner command with flock), and route editor stamping through a tool so flock+commit can be mechanized (aligns with the oc-ledger design end state). · severity: major

3. skills/opencrabs-dev/.git/logs/HEAD (skill repo reflog) §commit f6f48fd4 · quote: "commit: runtime artifacts: ad6d9505 swap (run 33254863156, swapped 2026-08-29T13:45:01Z, artifact 7fe2334f, prev aa8d71cc, auth auto-swap): deployed.sha + deployed.meta.json markers, fanout.state (run 33254863156 delivery state), swap journal swap-ad6d9505-1788011092.jsonl + fanout journal (ledger stamp n=1320). Not a version bump — runtime-artifact commit class." · dimension: responsibility-creep · problem: the regime defines exactly two commit classes — supervisor.md Duty 3: "skill-dir repo: one commit per bump; state-dir repo: one commit per ledger stamp" — and the v0.4.42 BEND covers only state-repo ledger stamps. But tools write deployed.sha, deployed.meta.json, fanout.state, oc-deploy/journal/*, oc-deploy-shadow.log into the SKILL dir at swap time (STATE_DIR defaults to the skill dir, tools/oc-deploy line 64), so the skill repo accumulates tool-written churn between bumps with no documented owner or cadence; practice invented a third "runtime-artifact commit class" that exists only in git history, with no session named as owner. · fix: add a third row to the regime — skill-repo runtime artifacts: owner + cadence (natural fit: same hosting-session/sweep rule as tool stamps, committed alongside the state-repo stamp commit) — in supervisor.md (and editor.md if the owner is the editor). · severity: major

4. supervisor.md §Duty 3 · quote: "On any version bump the Supervisor's notify work is now: stamp ONE ledger event (version published, no per-worker notify rows) AND commit BOTH git repos — skill-dir repo: one commit per bump; state-dir repo: one commit per ledger stamp, inside the same same flock as the write (git-history regime, v0.4.41) — then BEND (v0.4.42, ruling event 1323): ... is the end state. target ONLY (a) lanes actively MID-CYCLE at publish time whose duties the change touches" · dimension: prose-not-table · problem: four parallel commit/notify rules (skill-repo bump commit; state-repo supervisor-stamp commit; state-repo tool-stamp hosting-session commit; notify targeting) are rendered as one run-on paragraph, and the v0.4.41/42 insertion split its host sentence — "target ONLY (a)..." now starts lowercase after a full stop, grammatically orphaned from "notify work is now:". The flat prose is what hides the ownership gaps in findings 1–3. (Quote rendered with the middle condensed; the "then / BEND" break and the lowercase "target ONLY" after "is the end state." are verbatim on disk, lines 79–91.) · fix: replace the block with a table — | repo | trigger | commit owner | timing | (rows: skill / version bump / supervisor / at bump; state / supervisor stamp / supervisor / inside write flock; state / tool stamp / hosting session, fallback TBD / next turn; skill / runtime artifacts / TBD / TBD) — and restore the notify-targeting sentence as its own paragraph. · severity: major

5. SKILL.md §frontmatter provenance (v0.4.42 entry) · quote: "Hosting session bundles the tool stamp WITH its own adjacent stamp in ONE commit IF both are pending at commit time (commit message names ALL stamps); no mid-run commits, no per-stamp hunting. Reconstructed intermediate states allowed for backlog (keep old flags)." · dimension: responsibility-creep · problem: ruling 1323 lives in two divergent copies. The SKILL.md frontmatter copy carries the "no mid-run commits, no per-stamp hunting" and "reconstructed intermediate states allowed for backlog" clauses; the supervisor.md Duty 3 BEND (the operative home, the only one a supervisor loads) omits them. Canonical ownership of the ruling text straddles the scope line: an editor's only view of the ruling is changelog prose in frontmatter, a supervisor's only view is incomplete; the two files can drift again on the next amendment. · fix: move the full ruling text verbatim into supervisor.md Duty 3 (single canonical home) and reduce the SKILL.md frontmatter entry to a one-line pointer ("v0.4.42: tool-stamp commit ownership = hosting session — supervisor.md §Duty 3"). · severity: major

6. SKILL.md §frontmatter provenance · quote: "- \"v0.4.42: per-stamp commit discipline amendment (editor d5863180 #29 IDEA) — commit-per-stamp BENDED for TOOL-written stamps (oc-deploy swap-execute / any tool subprocess: flock write happens inside tool, session cannot git-commit from subprocess; oc-ledger self-commit code does not exist per design doc oc-work/oc-ledger-design-20260829.md). RULING (event 1323): option (b) — the HOSTING session (the turn that observes the tool's stamp on disk) owns the commit. ... Gates: oc-deploy selftest 128 PASS / battery 76 PASS (pre-bump).\"" · dimension: token-weight · problem: the provenance block (lines 17–30, 12 entries S3-cutover + v0.4.32–v0.4.42, ≈10 kB ≈ ~3k tokens) is pure changelog with zero procedural content, yet it loads on EVERY SKILL.md read for BOTH roles, and editor.md Phase 1 mandates "re-read `SKILL.md` + `editor.md` in FULL from disk" at every claim — so every editor task pays ~3k tokens of history. v0.4.41/42 added ~2.5 kB more after the move was already queued (v0.4.40 KERNEL item B4 "provenance move", still awaiting owner word). · fix: execute B4 now — move provenance to a CHANGELOG.md outside the load path, keep `version:` + one-line pointer in frontmatter. · severity: major

7. supervisor.md §Duty 3 / Cycle-aware deferral, roster-delta propagation, freeze (v0.4.17) · quote: "Freeze history (pause/lift x2, 2026-08-26/27) lives in workers-ledger events #6/#8/#14. Current state: NONE active - the v0.4.19 gate above is the only notify policy; `[ALL]` reserved for consent-register-class gates." · dimension: dead-reference · problem: both halves are contradicted by the live record. (a) events[] is windowed to the last 100 since v0.4.41 (frontmatter: "JSON events[] is windowed to the last 100"); the canonical ledger's window starts at ts 2026-08-26 22:36Z / n≈1223 — events #6/#8/#14 no longer exist in the file (grep confirms), and no imported git ancestor covers them (ancestors start at n=1266). (b) The ledger's live meta says the opposite of "Current state: NONE active": `notification_freeze` = `"state": "ACTIVE"` with one recorded pause ("second freeze; workers hold 0.4.17, file stands 0.4.18") and no lift. · fix: repoint the history to the `notification_freeze` meta block + state-repo git history; reconcile the meta flag (stamp the lift event or reset the field to match the NONE-active ruling). · severity: major

8. SKILL.md §Upstream relations (item 2) · quote: "**Sync model = REBASE-PORT** (merge-sync retired 2026-08-26): backup ref FIRST → classify fork-only commits (absorbed / superseded / survivor) → port survivors chronologically onto `adolfousier/main` → pr-checks green (zero errors in ported lines) → force-push-with-lease. The old procedure home (`compiler.md` Step 7) is ARCHIVED — until the port procedure is re-homed as a supervisor duty, the sync runs ONLY on owner word." · dimension: token-weight · problem: §Upstream relations spans lines 318–374 (~57 lines) of the shared file that editor.md Phase 1 forces every editor to re-read in full, but items 1–3 (~19 lines), the item-6 parity clause and the item-7 sweep mechanics (~15 lines) are supervisor-only or ownerless procedure no editor ever executes; the port procedure additionally double-resides here and in archived compiler.md Step 7 with NO owning lane pending re-home — procedure weight with nobody to run it. · fix: move items 1–3 (+6 parity) into supervisor.md, leave one pointer line in SKILL.md; decide the re-home so the port text has exactly one owner. · severity: minor

9. SKILL.md §Session-notify loop · quote: "Journal: `STATE_DIR/oc-deploy/journal/fanout-<run>-*.jsonl`, steps `fanout-start / contributors / attributed / notified / skip / unowned / fanout-end`." · dimension: dead-reference · problem: `STATE_DIR` is never defined anywhere in SKILL.md or editor.md; it silently resolves to the SKILL dir (tools/oc-deploy line 64: `STATE_DIR="${OC_DEPLOY_STATE_DIR:-/root/.opencrabs/profiles/ops/skills/opencrabs-dev}"`), which is counter-intuitive after v0.4.38 moved canonical state to the state dir. The only definition lives in s2-swap-journal-spec.md, a file no role loads. The same section already inlines the canonical baseline.json path (v0.4.40 fix) — this one was missed. · fix: expand to the literal path `/root/.opencrabs/profiles/ops/skills/opencrabs-dev/oc-deploy/journal/fanout-<run>-*.jsonl`. · severity: minor

10. compiler.md §Scope · quote: "NEVER touches FEATURE branches/worktrees — TWO sanctioned touches, both bounded in their own steps: the Step 6 integration sweep and Step 7 rebase-port fixups (SKILL.md §Role router)." · dimension: dead-reference · problem: SKILL.md has no section named "Role router"; the router is the STEP ZERO role table / the "Owns:" preamble (the literal phrase appears only in body prose "This file = shared facts + role router only"). A re-enable reader following the anchor finds no heading. · fix: re-anchor to "(SKILL.md §STEP ZERO)". · severity: minor

11. s2-swap-journal-spec.md §Marker writes · quote: "`STATE_DIR/deployed.meta.json` — `{sha, run_id, artifact_sha256, features, swapped_at, consent_msgid, prev_sha}`" · dimension: dead-reference · problem: the field list still teaches `consent_msgid` although the spec's own AMENDED note supersedes it ("`deployed.meta.json` carries `auth:\"auto-swap\"` instead of `consent_msgid`") and the live marker confirms the new shape (deployed.meta.json on disk: `"auth":"auto-swap"`, no consent_msgid). Same staleness in vocabulary line 2 (`consent`). A future tool author reading the table rebuilds the dead field. · fix: update the Marker writes list to `{sha, run_id, artifact_sha256, features, swapped_at, auth, prev_sha}` and mark line 2 `auto-swap` per the amendment. · severity: minor

Tally (as delivered, 11 findings — agent output truncated after finding 11; supervisor resumed the agent to confirm no findings were lost): severity critical 1 (F1), major 6 (F2–F7), minor 4 (F8–F11).
