[2026-09-07T20:26:27Z] NOTE: same-day overwrite of prior version
# DUTY-6 SKILL REVIEW — Lens E (INTERFACE/TOPOLOGY), v0.4.95 @ b0776e81. Scope: tools/ 31 executables + SKILL/editor/RC-CONTRACT rows. Cycle-2 report.
F1 (a+d) ship->poll is one ritual split across two invocations; approved fusion flag unshipped AND ship today SILENTLY IGNORES --wait: arg loop mode-agnostic (oc-deploy:1668) but WAIT_SECS/NOTIFY_SESSION read only in poll branch (:1777,1814-1832); ship branch (:1733ff) never touches them — `ship --sha S --execute --wait 900` exits rc0 at dispatch silently dropping the flag while caller believes it waited. Latent trap: a lane pre-writing the future one-command shape gets a false done-receipt (false-receipt class oc-waiter was built to kill #74). Fix: land owner-approved fusion (ship tail chains poll body when WAIT_SECS>0) OR until then ship dies rc1 on --wait/--notify-session.
F2 (answer to fresh-change check) oc_session_trailer unification 14243a18 LEFT duplicated semantics-divergent extraction in oc-deploy red_leg — 4 inline copies with OLD first-wins head -1 convention (:1105,1109,1123-1124,1137-1138) while oc-order-validate:95 + oc-attrib migrated to lib/oc-log.sh:130-137 last-wins. Not just dead copy: surviving half of the exact bug unification killed — quoted-trailer message: order-validate credits LAST, red_leg BLAMES FIRST -> RED fan-out can wake the WRONG LANE. Fix: helper at all 4 sites (oc-deploy already sources lib/oc-log.sh).
F3 (b) --contributors projection verb in two tools again: oc-deploy fanout green_leg:1066-1067 carries private awk {print $1,$4,$5} copy of projection E6 moved into oc-attrib (v0.4.78). Retirement killed the documented entry point, not the implementation copy; future column-order change in oc-attrib TSV silently re-breaks fanout attribution. Fix: call $ATTRIB --contributors, delete awk.
F4 (c+d) bounded-wait+guaranteed-wake verb covered in half by two tools: oc-deploy notify_session (:975) + oc-waiter notify_lane (:119) both privately encode identical session_notify exit-code contract + single --interrupt retry; poll --wait loop (:1793-1832) lacks journal/stall-escalation/orphan-sweep legs; poll timeout msg (:1816) "wake FAILED" with no journal row + no HQ countersigned wake (waiter:275 does both). Fix: extract tools/lib/oc-notify.sh oc_notify_lane() sourced by both; longer term oc-waiter arm --backend oc-deploy-poll.
Adjacent O1: RC-CONTRACT.md:31 poll --wait die 2 claim contradicts code (oc-deploy:1777 die 1) AND battery (run.sh:399-400 asserts 1). O2 fresh-change checks CLEAN: order-validate hotfix 5b0b59c5 on unified helper; waiter parse 9aa55738 selftests 3b/3c; prchecks ref-equality c6706c89 selftest 353-357; featdef 41865aec selftest 4c + escape hatch; previously-folded chains single-implementation (oc-commit->oc-issue-log, ledger-sync->shadow-rotate, oc-wt->index-worktree, harvest-sweep->attrib, fault-scope delegation).
Count: 4 findings + 2 adjacent. Highest: F2 (wrong-lane RED blame), F1 silent --wait ignore (false-receipt class).
