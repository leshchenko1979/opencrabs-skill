# DUTY-6 SKILL REVIEW — Lens F (TOOL CODE REVIEW), v0.4.95 @ b0776e81. All 31 executables, RC-CONTRACT, SKILL tool table, tools/tests interface. Verbatim-quoted, severity-tagged.
F1 [P0 CRITICAL] oc-prchecks:525 `OC_PR_SKIPPED_DISPATH=1` — ONE assignment to the WRONG variable name (misspelled DISPATH vs consumers' DISPATCH at :529, :602 which compare != 1 -> true) — after printing "adopting instead of re-dispatching" + flock -u 8 (lock released!), tool falls through into full workflow dispatch + LATEST-wins adoption. Re-creates the exact concurrency-cancel failure class C-F2/#115B was built to kill; fresh adoption can adopt the NEW run instead of the witnessed one; comment on :524 now a lie. Note: lens-F morning batch (c6706c89) fixed the CONSTANT on the consumer side but the assignment side kept the typo — the "truncation fixed" claim was verified on consumers only. Fix: correct spelling + selftest assertion.
F2 [P1] Selftest cannot see F1: case (b) asserts rc5+resume line only; gh shim dispatch arm silent+unlogged — double dispatch produces zero selftest signal. Fix: GH_LOG-style call counter, assert workflow run NOT invoked on C-F2-adopt path.
F3 [P1] oc-deploy:1777 --wait non-integer die 1; RC-CONTRACT.md:31 documents die 2 for exactly this case; sibling flag validation :1661 uses die 2. Untested (no selftest case). Lanes keyed on rc2+backoff misclassify rc1. Fix: die 2 (or amend contract + say so).
F4 [P2] oc-deploy:1267 features-compat gate die 4 — RC-CONTRACT rc-4 register says only "stage-gate-launch", no features-compat drop gate (HQ law 2026-09-04) or --allow-features-drop override. Fix: amend rc-4 entry.
F5 [P2] --allow-features-drop missing from both oc-deploy usage strings (:1652,:1672) — escape hatch discoverable only by hitting the die. Fix: append to usage.
F6 [P2] oc-deploy parses --wait/--notify-session/--dry-run/--artifact-dir in EVERY mode, silently no-op outside their mode (ship --wait zzz --dry-run exits 0 ignoring both) contra "usage errors print WHY" convention. Fix: validate flag<->mode pairing, die 2 cross-mode.
F7 [P3] featdef mismatch shadow row writes FEATURES into sha column: shadow_log ship "$FEATURES" "$FEATURES" at :1710 — col 2 is sha column everywhere else, watch-covered greps it for shas. Fix: "-" in col 2.
F8 [VERIFIED-OK + 2 nits] oc-order-validate:96 structural UUID pattern 8-4-4-4-12 correct (hotfix class closed); nits: ? over-permissive non-hex (fail-open, harmless — ledger join is real gate); set -u trailing valueless flag dies bare rc1 without usage block (:61-70).
F9 [VERIFIED-OK + 2 latent siblings] oc-waiter RUN_ID parse covered by selftests 3b/3c, injection-safe (7c); (i) rc-5 arm parses only run=[0-9]+ — RUN <url> shaped in-flight line would silently degrade re-attach to unseeded re-polls; (ii) rc-6 branch skips sleep + deadline check — supersede storm fires up to CHURN_CAP(3) back-to-back dispatches zero pacing. Fix: reuse rc0/rc3 matcher at rc5; sleep before rc-6 continue.
F10 [VERIFIED-OK + 1 edge] oc-branch-sweep --keep rc-2 fail-closed correct (selftest bad-keep-rc2 + bad-keep-no-delete); edge: --keep '' passes -> every branch matches -> all PROTECTED -> silent no-op rc0 (fail-closed direction but silent). Fix: die 2 on empty before grep.
F11 [P3] contributors retirement date disagree: SKILL.md:56-58 v0.4.90 vs RC-CONTRACT.md:31 + oc-deploy:24 v0.4.91; also SKILL.md:57 says poll --wait timeout dies rc5 "with the run URL" — actual rc-5 paths (:1816/:1827) print sha/message, no URL. Fix: normalize v0.4.91, drop "run URL".
F12 [P3] RC-CONTRACT oc-review-persist row lists only "0 persisted" — tool header + body implement 3 re-read sha256 mismatch / 4 write failure (die 3/die 4). Contract rule says per-tool codes documented here + in header.
VERIFIED-CLEAN: journaling completeness exemplary (journal_init $$-suffix collision fix; every swap step jlines before next; failed-restore journals rollback exit=1 truthfully, selftest 11b; oc-waiter journal-first; prchecks _pr_exit_guard re-journals budget-kill as rc5+anomaly). featdef guard order matches spec + selftest 4c. Help register: all 31 tools --help rc0. Dead verbs: one — oc-prchecks CANCELLED message says "re-arm: oc-prchecks gate <sha>", no gate verb exists.
Count: 12 findings (1xP0, 2xP1, 4xP2, 5xP3), 3 verified-clean notes. P0 is fleet-blocking: silently defeats #115B fail-closed dispatch guard, invisible to current battery — fix before next waiter-driven lane arms.
