# Lens I - cycle 20260919-cycle
## Verdict: The catalog text is intact, but every machine that reads it still encodes the pre-split topology — gate source, gate selftest, and the prose counts all say "11" while the gate now derives 10 and certifies the cycle clean.

## Findings

**[HIGH] The census gate's selftest fixtures the vacated location, so it cannot catch the standing-lens loss.** locator `tools/oc-review-persist:193` (`SS --selftest`). QUOTE: `printf '# fd\n## Review lens \`brain-scrub\` (owner order)\nbody\n' > "$CR/fleet-directives.md"`, asserted as `expected=3 lens(es): A B brain-scrub`. The fixture writes the standing lens into a MOCK `fleet-directives.md` — exactly the file the 2026-09-18 split vacated (brief is now `####` at `review-lenses.md:284`). The test stays green while the live catalog no longer satisfies the derivation, so the one test guarding the gate is structurally blind to the regression. Same defect in the gate's own spec comment, `tools/oc-review-persist:30`: "standing lenses from the `## Review lens <name>` headings in fleet-directives.md." Fix: add a fixture with the standing lens in `review-lenses.md`, and repoint the comment; derive from both files regardless of heading level.

**[HIGH] Count divergence — gate derives 10, hq.md/SKILL.md assert 11, and the gate is the cited coverage proof.** locator `hq.md:241` (`SS Duty 6`) + `SKILL.md:51`. QUOTE: "DONE = all 11 lens reports persisted via oc-review-persist …"; SKILL.md: "Duty 6 periodic 11-lens reviews". HQ's verdict tables cite the gate AS the coverage evidence — `reviews/20260912-cycle/hq-verdict-table.md`: "`OK (11 persisted, 0 waived, 0 unreceipted)`". Post-split the identical command yields 10 and exits 0, so the automated check is now weaker than the human formula: a 10-report cycle certifies clean against a DONE demanding 11. Fix: derive the count once and assert gate == formula; never hardcode 11 in prose.

**[MED] The moved brief points at a "review-battery boundary law above" absent from its new file.** locator `review-lenses.md:292`. QUOTE: "Same mechanics as every other lens: verdict consolidated → stamped (`review-battery` boundary law above applies unchanged)." The split moved the brief out of `fleet-directives.md`; the law stayed behind (now `hq.md:285`). `review-lenses.md` contains no such section, so "above" is a dangling relative anchor created by the move. Fix: cite the absolute home.

**[MED] The machinery's output carries a report under a lens identity the catalog does not define.** locator `reviews/20260918-cycle/reports/lens-S-staleness-archaeology-20260918.md:1`. QUOTE: "# Lens S — staleness-archaeology — 2026-09-18". The catalog defines exactly eleven lenses (A–J + brain-scrub) and `oc-review-persist`'s whitelist has no `S`; the filename is also non-canonical (`lens-S-…`, not `skill-review-S-…`). Catalog and output disagree on lens identity, and the census can never see this report. Fix: register the lens in catalog+whitelist, or re-persist under a catalogued key; gate on report lens-token ⊆ derived set.

**[MED] Persisted reports are corrupted by harness debris, not clean reports.** locator `reviews/20260918-cycle/reports/lens-B-redundancy-20260918.md` (preamble). QUOTE: `\n## Remotes &","s":true}] -->`. The recovery note claims harness comment blocks were stripped, yet raw JSON/tool-log fragments remain in the body — the artifact is transcript, not a report, and cannot be quoted reliably downstream. Fix: strip all scaffolding or re-persist from the session's final text; gate on known debris tokens.

**[LOW] The standing lens has no family slot; its H4 heading masquerades as a FAMILY heading.** locator `review-lenses.md:284`. QUOTE: "#### Review lens `brain-scrub` (owner order 2026-09-05)". It sits at the same level as `#### FAMILY: …`, but no family claims it (catalog or `hq.md` step-2 map "DOCS=A,B,G · TOOLS=C,E,F · MECHANICAL=J · ARTIFACTS=D+H · META=I"). Boundary: skill-root `.md` organization is also G's; filed here because the catalog is I's object.

## Coverage
Read in full: `review-lenses.md`; `tools/oc-review-persist`; `hq.md` §Duty 6 (L170–245); `fleet-directives.md` heading census (all `^## `/`^### `/`^#### `) + targeted greps; SKILL.md/README.md lens refs; `reviews/20260912-cycle/hq-verdict-table.md` (+ verdict-table part), `20260915-c17|18/verdict.md`+`state.json`, `20260919-cycle/state.json`, `20260918-cycle` lens-B + lens-S reports (part), `reviews/skill-review-index.log`, `oc-lint-laws` corpus line.
Machinery scan (the META question): things keyed off `fleet-directives.md`/a heading level are the gate derivation (`tools/oc-review-persist:71`, `sed … '^## Review lens'`), its selftest fixture, and its spec comment; things keyed off a hardcoded historical count are `hq.md:241` (11), `hq.md` step-1 spawn set + step-2 family map, `SKILL.md:51` (11), `README.md:18` (A–J). `oc-lint-laws` includes both files but has no heading-level dependence (content lint only). What the split broke that no single lens owns: the catalog↔tool contract — A's (c)/(d) sees the stale path/count, G's load-path sees the anchor, J sees the hardcoded set, but no lens owns "the catalog moved; the readers did not."
NOT read: full bodies of the 20260907/09/10/11/12 persisted reports; `war-stories.md`; CHANGELOG.md in full; `tools/oc-ledger` body; `oc-lint-laws` logic beyond its corpus line; `20260915-c18/reports` (listed empty). No persisted lens finding was re-litigated; the known `check-cycle` omission was used as input, not re-filed.
