[2026-09-19T04:19:57Z] NOTE: same-day overwrite of prior version
# Lens A — cycle 20260919-cycle

## Verdict
The 2026-09-18 split landed cleanly on content (no leftover duplicate law text in `fleet-directives.md`), but it left four locators pointing at the old file, one law with no home, and one section whose rules sit under a foreign heading.

## Findings

**[MED] The night-window cadence law has no live home; its only surviving citation points at a section that no longer exists.** locator `triage.md:194` · VERBATIM: "PHASE — **Phase 3, Idle-Lane Issue Triage** (`fleet-directives.md`). Same" — plus the split's pointer table (`fleet-directives.md` §"Shape after the 2026-09-18 split"): "| intake & assignment, dispatch hygiene, topic domain alignment, parked issues, Decision Rollcall, creating new editors | `triage.md` |". No heading `Phase 3, Idle-Lane Issue Triage` exists anywhere (grep: `triage.md:194` + CHANGELOG only), and the parent law it belongs to — "Daytime-Editing & Nighttime-Batch-Sync Cadence … §Cadence & Trigger Law", whose "SOLE trigger for the Night Shift (Batch Merge, Harvest & Issue Triage Window) is an explicit operator command" (quoted at `reviews/20260912-cycle/reports/skill-review-A-20260912.md:77` as `fleet-directives.md:603/606`) — is gone from the live corpus: grep for `Daytime-Editing|Cadence & Trigger Law|Batch Merge|SOLE trigger|Operator-Initiated` over all skill `.md` hits only `CHANGELOG.md` and old reviews. So the window's trigger law was neither re-homed nor given a pointer row, and the one live sentence describing it cites the vacated file. Fix: state the night-window law's current owner in the pointer table and drop the `(fleet-directives.md)` parenthetical.

**[MED] Intra-file reference to a section the split moved out.** locator `fleet-directives.md:48` · VERBATIM: "4. **This is NOT the carrier FREEZE of §Remotes & sync guard (2).** That one is mechanical (no sync while a carrier chain sits between dispatch and swap)." — `§Remotes & sync` now lives only at `upstream-merge-runbook.md:254` ("## Remotes & sync"), which the pointer table itself names as the sync-law home. A lane reading the freeze law alone cannot resolve `§Remotes & sync guard (2)`. Fix: qualify the ref as `upstream-merge-runbook.md §Remotes & sync`.

**[MED] Direct-dispatch's operative rules sit under the Domain-Affinity heading.** locator `fleet-directives.md:397` (§Designated Domain Affinity & Topic Context Focus Law) · VERBATIM: "- **Rule 1 — Direct dispatch:** the sender of a work order notifies the lane that owns the resource directly. An intermediary may name the target, never carry the payload." (Rules 2–5 + "Exceptions" follow; the heading 5 lines above is "## Direct dispatch — no relay hops", whose body is rationale only). Result: two concepts under one heading and a `§Direct dispatch` heading that contains none of the dispatch rules. Fix: move Rules 1–5 under `## Direct dispatch`.

**[MED] Two identical headings in one file; one is a pointer to its own name.** locator `editor.md:76` · VERBATIM: "*(Waiter-discipline items — poll floor, --wait ceiling, invocation verify, notify wiring, log-window cuts, REST casing — live canonically in: editor.md §CI-wait discipline & actor attribution.)*" — the substantive W1–W6 list is the second "## CI-wait discipline & actor attribution" at `editor.md:590`, which itself ends "Procedure detail: `editor.md §CI-wait discipline & actor attribution`." The locator resolves to two headings and the stub tells the reader to read the section they are already in. Fix: delete the stub at `:76` (and the self-pointer at `:600`).

**[MED] Retired role named in present tense as an actor.** locator `editor-upstream-pr.md:196` · VERBATIM: "| Prerequisite unharvested (feature depends on fork-only changes from another issue) | **Editor / Harvester (v0.4.174)** |". Harvester was folded into Triage — `SKILL.md:52`: "upstream lifecycle tracking (delta watch, PR census, dependency tracking folded from Harvester v0.4.176)" — so the actor column names a role that no longer exists, with no RETIRED marker (churn-drift (e)). Fix: replace with the current owner (Editor / Triage) or mark `Harvester RETIRED v0.4.176`.

**[LOW] The repo map omits two of the four role files.** locator `README.md  SS## Layout` · VERBATIM (the map's role rows): "| `hq.md` | HQ role — worker roster, duty cadence, CI-wait & waiter discipline, review lenses |" then "| `review-lenses.md` | Full Duty-6 lens briefs (A–J) — split from hq.md v0.4.78 |" — `triage.md` and `toolsmith.md` have no row, though the same README's Roles table lists TRIAGE and TOOLSMITH as live and `SKILL.md:6` says "procedures live in FOUR role files". Fix: add both rows.

**[LOW] A section name that matches no heading is cited twice.** locator `SKILL.md:194` (also `toolsmith.md:99`) · VERBATIM: "Escalation ladder canonical: fleet-directives.md §Cross-lane delivery" — the heading is "## Cross-lane message delivery discipline" (`fleet-directives.md:370`), and `SKILL.md:191` four lines above uses that full name. Two names for one gate violate (a); grep by heading fails. Fix: use the canonical heading in both refs.

## Coverage
Read in full: `fleet-directives.md` (36 live `##` sections + pointer table), `editor.md` (heading map + §CI-wait/§CI-Watcher/§No-auto-rollback/§Swap-sha/§Hotfix/§Inherited-claim blocks), `editor-upstream-pr.md` (Phase-7 tables), `hq.md` (Duty 6 + tail), `triage.md` (T1–T7 + tail), `toolsmith.md` (§Escalation + staging law), `SKILL.md` (§Test ontology, §Glossary, §Hard rules, tool register rows), `README.md`, `s2-swap-journal-spec.md`, `war-stories.md` (headings), `review-lenses.md` (Lens A brief). Grep sweeps: all `fleet-directives.md` § refs, heading maps, `Idle-Lane|Phase 3|Batch Merge|Cadence|Night|Harvester|oc-waiter|Supervisor|Remotes & sync`. NOT read: `CHANGELOG.md` in full (278 KB — sampled v0.4.200–205 + targeted greps), `tools/**` code and `tools/RC-CONTRACT.md`/`HEALTH-*.md` contents, `upstream-merge-runbook.md` §Process/§Gates bodies, `war-stories.md` bodies, `reviews/**` (prior-cycle evidence only, used as corroboration).
