---
name: opencrabs-dev
description: >
  House rules for ANY work on OpenCrabs source code (~/opencrabs): upstream issue
  claiming, per-task worktrees, modum gate, push + sha hand-off (EDITOR role);
  quick-build dispatch, artifact fetch, binary swap, health check, red-build
  protocol (COMPILER role). Use when editing/fixing OpenCrabs Rust code, debugging
  its CI runs, dispatching quick-build-linux, fetching CI artifacts, or swapping
  /usr/local/bin/opencrabs.
  (/opencrabs-dev)
version: 0.3.1
author: leshchenko1979
metadata:
  tags: [opencrabs, rust, ci, quick-build, binary-swap, worktree, modum]
  references:
    - https://github.com/adolfousier/opencrabs (upstream — issues only)
    - https://github.com/leshchenko1979/opencrabs (fork — push target)
---

# opencrabs-dev — OpenCrabs source procedure

**Owns:** everything touching `~/opencrabs` source, its GitHub Actions runs, or the
installed `opencrabs` binary. This file = shared facts + role router only. Actual
procedures live in TWO role files; load ONLY the one matching the session's role.

## STEP ZERO — establish the role (mandatory on every load)

Ask the operator which role this session employs before doing anything:

> **Editor or Compiler?**

| Role | Owns | Procedure file |
|------|------|----------------|
| **EDITOR** | Commits + error fixes: claim issue → worktree → code → modum → push → report sha | `editor.md` |
| **COMPILER** | Getting the new binary onto this box: dispatch CI → verify sha → watch run → swap → health check | `compiler.md` |

Roles **DO NOT intersect**:

- The Editor NEVER installs/swaps binaries, NEVER restarts daemons, NEVER dispatches
  CI runs, NEVER reads or watches run results.
- The Compiler NEVER writes, commits, or fixes code, NEVER touches branches or
  worktrees.
- Hand-off point: the Editor produces (pushed branch + reported sha); the Compiler
  takes it from there (dispatch → watch → swap). If the run is RED, the Compiler
  reports evidence and stops — fixing code is always Editor work.

If the request mixes both (e.g. "fix X and deploy it"), split into two sessions/two
role loads — do not fuse the roles in one pass without Alexey saying so explicitly.

## Shared environment facts (both roles)

- Checkout `~/opencrabs`: remote **`origin`** = fork `leshchenko1979/opencrabs`
  (push target) · remote **`adolfousier`** = sync source (upstream).
- This box has **no Rust toolchain by design** — CI is the compiler. Never install
  cargo/rustc here; never "just compile locally" as a shortcut.
- Live binary: `/usr/local/bin/opencrabs`. Daemons run as systemd **user** units
  (`systemctl --user`) — system-scope queries (`systemctl`, `/etc/systemd`) find nothing.
- Builds are MINIMAL-FEATURE by design: `cargo build --locked --release
  --no-default-features --features "<picked set>"`. Upstream #1186 (missing #[cfg]
  gates) CLOSED 2026-08-25 — feature subsets compile clean. The picked set lives IN
  the target branch's `quick-build-linux.yml`; currently `telegram,browser,rtk`.
  Anything outside the set (local-stt/local-tts voice, whatsapp/discord/slack/trello,
  pdfium…) is absent from the swapped binary — missing-feature behavior afterward is
  expected, not a bug.

## Hard rules (both roles)

- Upstream `adolfousier/opencrabs` accepts **issues/comments ONLY — NEVER PRs**
  (rule set 2026-08-22). Contributions land via upstream maintainers reading issues.
- Restart scope: **`opencrabs-ops` ONLY.** The `family` and default-profile daemons
  require Alexey's explicit approval EVERY time.
- Never trust watcher exit codes alone — read the run's own `conclusion` via API.

## Shared war stories (why these rules exist)

| Rule | Incident |
|------|----------|
| Inputs from branch YAML, verify headSha | run #4 dispatched blind; run #2 built main |
| Distrust watcher exit codes | watchers reported success on failed runs, 3× |
| Foreign-WIP diff vs merge-base | branch carried another agent's dropped WIP |
| User-unit awareness | system-scope queries found nothing; wrong restart path nearly used |
| Inner-match exhaustiveness | E0004 cost one full CI cycle (run #5 fixed it) |

*Source of truth for procedure = these skill files. AGENTS.md carries only the pointer.*
